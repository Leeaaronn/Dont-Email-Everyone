---
phase: quick-260911-l0p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dont_email_everyone/plots.py
  - tests/test_plots.py
  - reports/figures/policy_curve_womens_visit_spend.png
  - reports/figures/policy_curve_womens_visit_visit.png
  - .planning/phases/06-streamlit-app-deployment/06-UI-SPEC.md
autonomous: false
requirements: [UI-SPEC-V18, ROADMAP-06-C2]

must_haves:
  truths:
    - "On every policy curve the legend box sits entirely outside the plot area — it overlaps neither the curve, the anchor rule, the selection rule nor the hatched covers-zero spans"
    - "The legend is complete and unclipped in the committed PNG, which is written by savefig with NO bbox_inches, exactly as pipeline.py writes it"
    - "The legend is complete and unclipped in the app path, which Streamlit writes with bbox_inches='tight' and dpi=200"
    - "The plot area is no narrower in drawn pixels than it was before the change"
    - "The dead upper headroom that existed only to clear the in-axes legend is gone, and the comment that justified it no longer claims a constraint that has ceased to exist"
    - "git status --short data/processed reports/figures lists exactly the two policy-curve PNGs after a full pipeline run, and nothing else"
    - "The full pytest suite is green with no -m deselection"
    - "The app still passes no figsize, dpi, fontsize or pad to any factory (V18 intact)"
  artifacts:
    - path: "dont_email_everyone/plots.py"
      provides: "policy_curve_plot with an outside-axes legend and symmetric y padding"
      contains: "bbox_to_anchor"
    - path: "tests/test_plots.py"
      provides: "a rendered-extent regression test proving the legend clears the axes and fits the canvas, for both the five-entry and six-entry cases"
      contains: "get_window_extent"
    - path: ".planning/phases/06-streamlit-app-deployment/06-UI-SPEC.md"
      provides: "dated in-place amendments at all five sites that pin the old geometry or record the superseded do-not-restyle position"
      contains: "Amended 2026-09-11"
  key_links:
    - from: "dont_email_everyone/plots.py::policy_curve_plot legend call"
      to: "dont_email_everyone/plots.py::_fit_titles (tight_layout)"
      via: "tight_layout reserves canvas width for the outside legend; the legend is placed BEFORE _fit_titles runs"
      pattern: "bbox_to_anchor.*\\n.*\\n?.*_fit_titles"
    - from: "dont_email_everyone/plots.py::policy_curve_plot"
      to: "reports/figures/policy_curve_womens_visit_{spend,visit}.png"
      via: "pipeline.py savefig(path, dpi=150) with no bbox_inches"
      pattern: "policy_curve_womens_visit_(spend|visit)\\.png\", dpi=150"
---

<objective>
Move the six-entry legend on `policy_curve_plot` out of the plot area so it stops
crossing the selection rule and covering the curve, regenerate the two committed
policy-curve PNGs, and amend `06-UI-SPEC.md` in place to record that the earlier
"do not restyle `plots.py`" position has been superseded by an explicit user decision.

Purpose: the user reports, from the deployed app, that "the selection rule crosses
through the legend box and the box covers part of the curve — visible at every
shallow depth, on both rankings." This was recorded at the 06-07 legibility
checkpoint as a known cosmetic defect and deferred on the belief that no remedy
existed short of a `plots.py` restyle. The user has now asked for the real fix.

Output: a corrected `policy_curve_plot`, a rendered-extent regression test, two
regenerated PNGs committed alongside the source that produced them, and five dated
in-place amendments to `06-UI-SPEC.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

@dont_email_everyone/plots.py
@dont_email_everyone/pipeline.py
@tests/test_plots.py
@.planning/phases/06-streamlit-app-deployment/06-UI-SPEC.md

<established_facts>
These were verified before planning. Re-verify anything you depend on; do not
re-litigate them.

**F1. `plots.py` is not locked, and D-06's gate does not forbid this change.**
D-06's gate is: run `.venv/Scripts/python.exe -m dont_email_everyone.pipeline all`,
then require `git status --short data/processed reports/figures` to print nothing.
That is a DRIFT check — it catches a `plots.py` change whose regenerated output was
never committed. It is explicitly NOT a byte or checksum pin:
`tests/test_plots.py:1677-1697` states that no committed-PNG checksum test exists and
why, and `tests/test_artifacts.py:6-9` records the repo-wide policy that figure and
Parquet bytes are never asserted. The correct move is therefore: change the source,
regenerate, and commit source and regenerated PNGs TOGETHER — after which the gate
finds nothing uncommitted and passes. Plan 05-03 is the in-repo precedent for
regenerating a closed-phase artifact.

**F2. The two render paths disagree, and this is the trap.**
- App: `streamlit_app.py::render` calls `st.pyplot(fig)` with no kwargs. Streamlit
  1.63's `_DEFAULT_SAVEFIG_OPTIONS` is `bbox_inches="tight"`, `dpi=200`
  (`.venv/Lib/site-packages/streamlit/elements/pyplot.py:38-43`), so an outside-axes
  legend SURVIVES there whatever the figure geometry is.
- Committed PNG: `pipeline.py:2613` and `:2632` call
  `figure.savefig(path, dpi=150)` with no `bbox_inches`.

A fix that leans on the crop would look right in the app and ship a CLIPPED legend
into the PNGs. Phase 7's README embeds those PNGs, so a clipped legend there is worse
than the overlap it replaces. **The space must be reserved inside the figure canvas.
Do not add `bbox_inches` anywhere, and do not depend on it.**

**F3. Measured baseline geometry** (real committed data, headline ranking, spend
outcome, matplotlib 3.11.1, `python -m` from the repo root):

| case | canvas px | axes px | axes share | legend px | legend entries |
|------|-----------|---------|-----------|-----------|----------------|
| `selected=None` | 800 x 560 | 666.5 x 372.4 | 83.3% | 309 x 109 | 5 |
| `selected=0.05` | 800 x 560 | 666.5 x 372.4 | 83.3% | 309 x 125 | 6 |

ylim in both cases: `(-0.261135, 0.656421)`.

**F4. `tight_layout` DOES reserve canvas width for an axes legend anchored outside
the axes, on matplotlib 3.11.1.** Measured over three consecutive `tight_layout()`
calls on a six-entry legend at `bbox_to_anchor=(1.02, 1.0)`; the layout converges by
the second call and the legend is never clipped:

| figsize | canvas px | axes px after convergence | axes share | legend clipped |
|---------|-----------|---------------------------|-----------|----------------|
| 8.0 x 5.6 | 800 | 420.7 | 52.6% | no |
| 10.6 x 5.6 | 1060 | 675.6 | 63.7% | no |
| 11.2 x 5.6 | 1120 | 734.4 | 65.6% | no |

At the current 8.0in width the legend fits but the plot area collapses from 666.5px
to 420.7px, which trades one legibility defect for another. **10.6 x 5.6 is the
width at which the plot area does not shrink** (675.6px against the 666.5px baseline
in F3) and the legend gets its own column.

`requirements.txt:45` pins `matplotlib==3.11.1`, so the serve-time environment on
Community Cloud is the same version this was measured on.

**F5. `_fit_titles(fig)` calls `fig.tight_layout()` internally** (`plots.py:1183`),
once per candidate title size, returning at the first size that fits. Any
`subplots_adjust` applied BEFORE it would be undone.

**F6. The upper headroom exists only for the in-axes legend.** `plots.py:1431-1436`
sets `y_hi = max(highs) + 0.34 * unit_span` against `y_lo`'s `0.10`, and its own
comment says the asymmetry exists solely so "the legend box lands on the curve it is
describing" does not happen. Once the legend is outside the axes that headroom is
dead weight compressing the curve into the lower two-thirds of the panel.

**F7. The two published rankings** (what the app's selector offers, `unproven_*`
excluded at `streamlit_app.py:705`) are `uplift_womens_visit` (headline) and
`uplift_womens_conversion` (sensitivity). Both are drawn on both outcomes, `spend`
and `visit`. The two COMMITTED PNGs are the headline ranking only, with no
`selected` passed.

**F8. Nothing in the suite pins the geometry being changed.** No test references
`figsize`, `5.6`, `0.34` or `"upper left"`. `tests/test_plots.py:1730` asserts
`bare_ax.get_ylim() == marked_ax.get_ylim()`, which stays true because ylim is
derived from the data, not from the legend. The PNG size assertions in the suite are
FLOORS (`st_size > 5000`), and a wider figure only grows.

**F9. Scratchpad scripts need `PYTHONPATH`.** The package is not pip-installed;
`python -m dont_email_everyone.pipeline` works from the repo root because `-m` puts
cwd on `sys.path`, but a script under the scratchpad does not. Set
`PYTHONPATH=C:/Users/leeaa/Dont-Email-Everyone`.
</established_facts>

<scope_boundary>
IN scope: `policy_curve_plot` only — the two figures the app displays under "Where
the gain is, and is not, detectable" and the two committed
`reports/figures/policy_curve_womens_visit_{spend,visit}.png`.

OUT of scope, explicitly:
- The cost exhibit's legend (`plots.py:1757`, `loc="upper right"`).
- The seven other legends in the module.
- `streamlit_app.py` — nothing in the app changes. V18 ("the app passes no colour,
  `figsize`, `dpi`, `fontsize` or `pad` to any factory") constrains the APP, not
  `plots.py`; changing the factory's own defaults does not violate it.
- `reports/policy.md` — its claims about these figures ("shade every depth whose 95%
  band covers zero across the full height of the axes") remain true. Confirm, do not
  edit.

Do not widen this into a module-wide legend restyle. The user reported the policy
curves.
</scope_boundary>

<interfaces>
The exact call sites this plan touches.

`dont_email_everyone/plots.py` ~1422:
```
fig, ax = plt.subplots(figsize=(8.0, 5.6))
```

`dont_email_everyone/plots.py` ~1428-1436:
```
lows = [0.0, float(np.min(drawn)), float(np.min(drawn_lo))]
highs = [0.0, float(np.max(drawn)), float(np.max(drawn_hi))]
span = max(highs) - min(lows)
unit_span = span if span > 0.0 else 1.0
y_lo = min(lows) - 0.10 * unit_span
# More headroom above than below, and the asymmetry is not cosmetic: the
# legend sits in the upper left and its longest entry is three lines, so
# without it the legend box lands on the curve it is describing.
y_hi = max(highs) + 0.34 * unit_span
```

`dont_email_everyone/plots.py` ~1590-1601 (comment block plus the two calls):
```
# A sixth legend entry when a selection is passed. 06-RESEARCH rendered
# and inspected that case at `selected=0.37` on the spend curve and
# found the upper-left box still clears the curve: the
# `y_hi = max(highs) + 0.34 * unit_span` headroom above absorbs it,
# because the new entry is ONE line where the anchor's is three. That
# is an argument, not a verification -- this figure still goes to the
# phase's UI legibility checkpoint, because 05-08 found three real
# defects by opening PNGs that every automated check had passed.
ax.legend(loc="upper left", fontsize=7.5, framealpha=0.92)
_fit_titles(fig)
return fig
```

`tests/test_plots.py` ~1585-1589 (comment inside
`test_policy_curve_marks_the_selected_point_at_the_artifact_value`):
```
# 0.37 is the depth 06-RESEARCH rendered and inspected when it checked
# that a six-entry legend still clears the curve, so the case a human
# actually looked at is the case pinned here.
```

`dont_email_everyone/pipeline.py` ~2613 and ~2632 — the two savefig calls. **These
do not change.** They are quoted so it is clear what the PNG path is:
```
figure.savefig(config.FIGURES / "policy_curve_womens_visit_spend.png", dpi=150)
figure.savefig(config.FIGURES / "policy_curve_womens_visit_visit.png", dpi=150)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move the legend outside the axes, reclaim the dead headroom, and pin both with a rendered-extent test</name>
  <files>dont_email_everyone/plots.py, tests/test_plots.py</files>
  <behavior>
    New test in tests/test_plots.py, run for BOTH the five-entry case
    (`selected=None`) and the six-entry case (`selected=0.37`), on real committed
    policy-curve data:
    - The legend's rendered window extent and the axes' rendered window extent do
      not intersect. Assert on the bboxes, not on `loc`: a `loc` assertion passes on
      a legend that is nominally outside and visually on top of the curve.
    - The legend's extent lies wholly inside the figure canvas:
      `0 <= x0`, `x1 <= fig.bbox.width`, `0 <= y0`, `y1 <= fig.bbox.height`.
      This is the half that proves the committed PNG is not clipped, because the
      PNG is written with no `bbox_inches` and the canvas is all there is.
    - The axes' drawn width is at least 666.0 px — the F3 baseline, so the fix
      cannot pay for the legend by shrinking the plot.
    - The failure messages name the measured numbers, in the 05-08 style: a reviewer
      reading a failure should see how far outside the canvas the legend landed, not
      just that an assertion tripped.
  </behavior>
  <action>
Three source edits in `dont_email_everyone/plots.py`, all inside `policy_curve_plot`,
plus one test-comment correction and the new test.

1. **Widen the canvas.** Change `plt.subplots(figsize=(8.0, 5.6))` to
   `figsize=(10.6, 5.6)`. Add a comment carrying the F3/F4 measurement: the legend
   needs its own column, the plot area measured 666.5 px of an 800 px canvas before
   this change, and at 10.6 in `tight_layout` leaves it 675.6 px, so the plot does
   not pay for the legend. Say that 8.0 in was measured too and collapses the plot
   area to 420.7 px, which is why the width moved rather than the legend alone.

2. **Anchor the legend outside the axes.** Replace
   `ax.legend(loc="upper left", fontsize=7.5, framealpha=0.92)` with a call adding
   `bbox_to_anchor=(1.02, 1.0)` and `borderaxespad=0.0`, keeping `fontsize=7.5` and
   `framealpha=0.92` unchanged. The call stays where it is — BEFORE `_fit_titles(fig)`
   — because `_fit_titles` runs `tight_layout`, and on matplotlib 3.11.1 (pinned in
   `requirements.txt`) `tight_layout` reserves canvas width for an axes legend
   anchored outside the axes (F4: measured, converges by the second call, never
   clipped at 8.0 / 10.6 / 11.2 in).

   **Do not add `bbox_inches` anywhere, and do not rely on it** (F2). If your own
   measurement contradicts F4 — if the new test reports a legend extending past
   `fig.bbox.width` — the fallback is an explicit `fig.subplots_adjust(right=...)`
   placed AFTER `_fit_titles(fig)` returns, never before it (F5), and never
   `bbox_inches="tight"`. Record in the SUMMARY which mechanism was used and what
   you measured.

3. **Reclaim the dead headroom.** Change `y_hi = max(highs) + 0.34 * unit_span` to
   `0.10 * unit_span`, matching `y_lo`. Rewrite the comment above it: the asymmetry
   existed only to keep the in-axes legend off the curve, that constraint no longer
   exists because the legend is no longer in the axes, and the padding is now
   symmetric. Do not leave a comment describing a constraint that has ceased to
   exist.

4. **Rewrite the obsolete argument at ~1590-1599.** The block reasons that
   06-RESEARCH inspected `selected=0.37` and found "the upper-left box still clears
   the curve" because "the `y_hi = ... 0.34 * unit_span` headroom above absorbs it."
   Both premises are now false, and its conclusion was wrong in practice — the
   06-07 checkpoint and then the deployed app both found the overlap it predicted
   away. Replace it with an honest note: the sixth entry makes the legend taller,
   the legend is outside the axes so its height costs the plot nothing, the geometry
   is asserted by the rendered-extent test rather than argued, and the 2026-09-11
   user report is why the earlier argument was not trusted. Keep the 05-08 point
   that a figure still goes to a human.

5. **Correct `tests/test_plots.py` ~1585-1589.** The comment claims 0.37 is pinned
   because it is "the depth 06-RESEARCH rendered and inspected when it checked that a
   six-entry legend still clears the curve." That check reached the wrong answer.
   Update the comment rather than deleting it: 0.37 is kept as the pinned six-entry
   depth, and the sentence should record that the original in-axes claim was
   falsified by the deployed app on 2026-09-11 and that the geometry is now asserted
   by the rendered-extent test rather than assumed. Do not weaken or delete the
   surrounding assertions, including
   `assert bare_ax.get_ylim() == marked_ax.get_ylim()` at ~1730, which stays true
   (F8).

6. **Add the new test** per `<behavior>`, in the policy-curve section of
   `tests/test_plots.py`. This is the class-level repair in the 05-08 mould, where
   `_fit_titles` plus a title-extent test replaced an instance-level fix: the defect
   is "an artist is drawn where it cannot be read", and the answer is a measurement
   against the canvas, not a nudge.

Do not touch `pipeline.py`, `streamlit_app.py`, the cost exhibit's legend or any
other legend in the module.
  </action>
  <verify>
    <automated>cd /c/Users/leeaa/Dont-Email-Everyone &amp;&amp; .venv/Scripts/python.exe -m pytest tests/test_plots.py -q</automated>
  </verify>
  <done>
`tests/test_plots.py` passes in full. The new test fails if the legend is returned to
`loc="upper left"` inside the axes, and fails if the figure is narrowed back to 8.0 in
(confirm at least the first of these by reverting the legend line in-memory or by
reasoning from the measured numbers in the failure message — do not commit a test you
have not seen discriminate). `grep -n "bbox_inches" dont_email_everyone/plots.py`
prints nothing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Render the inspection matrix, look at it, and have a human look at it</name>
  <files>(scratchpad renders only — no repo file is written by this task)</files>
  <action>
`policy_curve_plot` now draws its legend outside the plot area on a 10.6 x 5.6 canvas,
with the upper y padding reduced from 0.34 to 0.10 of the data span. Before this is
regenerated into the committed PNGs, the change is verified by LOOKING — the 05-08 and
06-07 checkpoints both found real defects in figures that every automated check had
passed, and this defect is itself one the automated suite never caught.

Render the inspection matrix first, then look at it yourself, then hand it over.

**Render.** Write a throwaway script under the scratchpad
(`C:/Users/leeaa/AppData/Local/Temp/claude/C--Users-leeaa-Dont-Email-Everyone/81859918-1870-4f48-bd15-768475033cab/scratchpad/policy_legend_check/`)
and run it with
`PYTHONPATH=C:/Users/leeaa/Dont-Email-Everyone .venv/Scripts/python.exe ...` (F9).
It must read `data/processed/policy_curve.parquet` and `policy_bands.parquet` and
call `plots.policy_curve_plot` with the same arguments `pipeline.py` uses
(`contrast=pipeline.POLICY_FIGURE_CONTRAST`, `unit=ate.OUTCOMES[outcome]`,
`anchor=economics.HEADLINE_CAPACITY`, `title=pipeline._policy_curve_title(outcome)`),
across:

  - rankings: `uplift_womens_visit` and `uplift_womens_conversion` (F7 — both
    published rankings, which is where the user reports the defect)
  - outcomes: `spend` and `visit`
  - `selected`: `None`, `0.01`, `0.05`, `0.10`, `0.20`

= 20 PNGs, each saved with `savefig(path, dpi=150)` and **no `bbox_inches`**, so what
you inspect is byte-for-byte the call shape `pipeline.py` uses.

Then render the two headline `selected=None` cells a SECOND time as `*_appmode.png`
with `bbox_inches="tight", dpi=200` — Streamlit's own savefig options (F2) — so both
render paths are inspected, not just one.

**Look.** Open at least these ten with the Read tool, and say what you see in each:
  - both rankings x both outcomes at `selected=0.01` (4) — the shallowest depth, the
    case the user calls worst
  - both rankings x both outcomes at `selected=0.20` (4) — where the selection rule
    lands on the pre-registered anchor and the legend carries six entries including
    the anchor's three-line one
  - the two headline `selected=None` cells (2) — the ones that become the committed
    PNGs

Check, in each: the legend box touches neither the curve, the anchor rule, the
selection rule nor the hatched covers-zero spans; the legend is complete, with no
entry cut off at the right or bottom edge; the curve now uses the vertical space the
0.34 headroom was consuming; the title is not clipped. Also open at least one
`*_appmode.png` and confirm the app path agrees.

If any of that fails, fix it and re-render before handing over. Do not hand a
defect to the checkpoint with a note about it.
  </action>
  <what-built>
The 22 renders described above, under the scratchpad path, produced by the corrected
`policy_curve_plot`. Twenty are written exactly as `pipeline.py` writes the committed
PNGs (`dpi=150`, no crop); two are written with Streamlit's own savefig options
(`bbox_inches="tight"`, `dpi=200`) so both render paths are on the table.
  </what-built>
  <how-to-verify>
1. The agent will list the scratchpad directory holding the 22 rendered PNGs and will
   have reported what it saw in the ten it opened.
2. Open the four `selected=0.01` renders — both rankings, both outcomes. This is the
   shallow-depth case you reported. Confirm the legend box no longer sits on the
   curve and that the selection rule no longer crosses it.
3. Open the four `selected=0.20` renders. This is the six-entry legend with the
   anchor's three-line entry, and the depth where the selection rule and the
   pre-registered anchor coincide. Confirm both that the legend is clear of the plot
   and that the solid-green-diamond / dashed-purple-circle pair is still
   distinguishable where they overlap.
4. Open the two `selected=None` renders. These two are what gets committed to
   `reports/figures/` and what Phase 7's README will embed. Confirm the legend is
   complete — nothing cut off at the right edge.
5. Confirm the curve reads better with the reclaimed headroom, not worse. If the
   extra vertical space makes the band or the hatching harder to read, say so; that
   part of the change is reversible independently of the legend move.
  </how-to-verify>
  <resume-signal>Type "approved" to regenerate and commit, or describe what is still wrong.</resume-signal>
  <verify>
    <automated>cd /c/Users/leeaa/Dont-Email-Everyone &amp;&amp; ls "C:/Users/leeaa/AppData/Local/Temp/claude/C--Users-leeaa-Dont-Email-Everyone/81859918-1870-4f48-bd15-768475033cab/scratchpad/policy_legend_check"/*.png | wc -l   # 22 before the human is asked to look</automated>
    <human-check>A human opened the shallow-depth and anchor-depth renders on both published rankings and both outcomes, and approved.</human-check>
  </verify>
  <done>
Twenty-two renders exist under the scratchpad. The agent has opened at least ten and
reported what it saw in each. The human has typed "approved", or the reported defects
have been fixed and the matrix re-rendered and re-approved.
  </done>
</task>

<task type="auto">
  <name>Task 3: Regenerate under the D-06 drift gate, run the full suite, and amend 06-UI-SPEC.md in place</name>
  <files>reports/figures/policy_curve_womens_visit_spend.png, reports/figures/policy_curve_womens_visit_visit.png, .planning/phases/06-streamlit-app-deployment/06-UI-SPEC.md</files>
  <action>
**1. Regenerate.** Run the full pipeline — it takes several minutes and gets its own
step rather than being buried in a per-task verify, exactly as 06-03 Task 3 did:

```
cd /c/Users/leeaa/Dont-Email-Everyone && .venv/Scripts/python.exe -m dont_email_everyone.pipeline all
```

**2. Drift gate.** `pipeline all` regenerates EVERY figure and Parquet, not just the
two. Run:

```
git status --short data/processed reports/figures
```

Exactly two paths may appear: `reports/figures/policy_curve_womens_visit_spend.png`
and `reports/figures/policy_curve_womens_visit_visit.png`. **Anything else listed is
a FINDING — stop and diagnose it. Do not commit past it, and do not run
`git checkout` to hide it** (threat T-L0P-02; 05-03 recorded the same prohibition and
had the same gate come back clean). Report the diagnosis before continuing.

**3. Full suite, no deselection.**

```
.venv/Scripts/python.exe -m pytest -q
```

No `-m` flag. `tests/test_artifacts.py` asserts pinned canary values against files
that were just rewritten, and a deselected suite would not check them.

**4. Confirm V18 still holds.** `streamlit_app.py` is untouched by this plan; prove
it rather than assert it — confirm the app still passes no `figsize`, `dpi`,
`fontsize` or `pad` to any factory (the source scan that enforces V18 lives in
`tests/test_app.py` and ran in step 3; name the test in the SUMMARY).

**5. Confirm `reports/policy.md` is still true.** Its claims about these two figures
— that they "shade every targeting depth at which the 95% band covers zero" "across
the full height of the axes", and the legend text it quotes verbatim — are unchanged
by this plan. Confirm by reading, and edit nothing.

**6. Amend `06-UI-SPEC.md` in place, dated 2026-09-11, at five sites.** Match the
existing house style: an italic parenthetical that quotes the original wording and
says what superseded it, so the amendment is legible AS an amendment. Every one of
these must say plainly that the earlier position — `layout="wide"` as the one
pre-approved remedy, and `plots.py` not to be restyled — has been **superseded by an
explicit user decision on 2026-09-11**, rather than quietly contradicting it.

  - **~line 94, "Figure geometry is inherited and must not be overridden."** The
    pinned `figsize=(8.0, 5.6)` is now `(10.6, 5.6)` for `policy_curve_plot`;
    `cost_sweep_plot` is unchanged and still 8.0 x 5.6, so the sentence can no longer
    speak of both factories with one number. `pad=28` is unchanged. The app still
    overrides nothing, so the rule itself survives — only its quoted value moves.
  - **~lines 124-139, the "Formal exemption" paragraph and its 2026-09-10 note.**
    This paragraph states that "restyling `plots.py` would alter the four committed
    PNGs, which D-06 forbids on pain of a byte-identity test failure." That is the
    claim this plan disproves: D-06's gate is a drift check, not a byte pin (F1), and
    no byte-identity test exists (`tests/test_plots.py:1677-1697`). Amend with the
    correction and the reason.
  - **~line 285, the Layout Contract amendment.** `layout="wide"` is kept and still
    correct; record that it was a mitigation of this defect and not a fix, that the
    overlap survived it and was reported from the deployed app, and that the real
    remedy was the one the 2026-09-10 note said not to take.
  - **~line 679, the dated contract row** `figure geometry 8.0x5.6in, pad=28, legend
    7.5pt, _TITLE_SIZES`. Amend the row's value and date, or add a superseding row
    dated 2026-09-11 — whichever the table's own convention supports. Do not silently
    overwrite a 2026-09-10 row with a 2026-09-11 value and leave the date reading
    2026-09-10.
  - **~lines 715-745, "Human-Judgment Only".** Item 1's instruction ends "do not
    re-style `plots.py`", and the 2026-09-10 outcome note records item 1 as fixed by
    `layout="wide"`. Add a 2026-09-11 note: item 1 was NOT actually closed, the user
    found it again in production, and the checkpoint in this plan (Task 2) rendered
    20 figures across both published rankings, both outcomes and five depths. This
    is the 05-08 pattern a third time — recorded as such.

**7. Commit.** Source, tests, both regenerated PNGs and the UI-SPEC amendment in one
commit, or in small sequential commits per this project's frequent-commit preference,
but the `plots.py` change and the two PNGs it produced must land **together** — that
co-location is what makes the D-06 drift gate pass (F1).
  </action>
  <verify>
    <automated>cd /c/Users/leeaa/Dont-Email-Everyone &amp;&amp; .venv/Scripts/python.exe -m pytest -q &amp;&amp; git status --short data/processed reports/figures</automated>
  </verify>
  <done>
The full suite is green with no `-m` deselection. After the pipeline run and the
commit, `git status --short data/processed reports/figures` prints nothing — the D-06
gate passes. `06-UI-SPEC.md` carries five dated 2026-09-11 amendments, each quoting
what it supersedes. `git diff --stat HEAD~1` (or the equivalent across the commit
series) shows `plots.py`, `tests/test_plots.py`, the two policy-curve PNGs and
`06-UI-SPEC.md`, and no other file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `plots.py` factory -> committed PNG on disk | a figure change silently alters a published artifact that `reports/policy.md` and Phase 7's README describe in words |
| `plots.py` factory -> two render paths | the app crops with `bbox_inches="tight"`, the pipeline does not; a fix can be correct in one path and broken in the other |
| `pipeline all` -> the whole of `data/processed` and `reports/figures` | one command rewrites every artifact, not only the two in scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-L0P-01 | Information disclosure | committed policy-curve PNGs | mitigate | the legend must be complete in the NO-`bbox_inches` path; Task 1's test asserts the legend's rendered extent lies inside `fig.bbox`, and Task 2 inspects PNGs written with the pipeline's exact savefig call shape. A legend clipped in the README is worse than the overlap it replaces (F2) |
| T-L0P-02 | Tampering | `git status` after `pipeline all` | mitigate | exactly two paths may be listed; anything else stops the plan and is diagnosed. `git checkout` to restore an unexpected file is forbidden — it would hide a real change (05-03 precedent) |
| T-L0P-03 | Repudiation | `06-UI-SPEC.md` | mitigate | the 2026-09-10 position ("`layout="wide"` is the one pre-approved remedy"; "do not re-style `plots.py`") is quoted and explicitly marked superseded by a dated user decision, never silently overwritten |
| T-L0P-04 | Denial of service | `reports/policy.md` prose | accept | the report's claims about these figures are geometric-invariant ("full height of the axes", verbatim legend text) and are confirmed by reading in Task 3 step 5; no edit is needed |
| T-L0P-SC | Tampering | package installs | n/a | this plan installs nothing; no `npm`/`pip`/`cargo` install task exists, so no legitimacy gate applies |
</threat_model>

<verification>
1. `.venv/Scripts/python.exe -m pytest -q` — full suite, no `-m` deselection.
2. `git status --short data/processed reports/figures` prints nothing after the commit.
3. `grep -n "bbox_inches" dont_email_everyone/plots.py` prints nothing.
4. `grep -n "upper right" dont_email_everyone/plots.py` still finds the cost exhibit's
   legend at ~1757, unchanged — the out-of-scope legends were not touched.
5. `git diff --stat` across this plan's commits lists only `dont_email_everyone/plots.py`,
   `tests/test_plots.py`, the two `policy_curve_womens_visit_*.png` files and
   `06-UI-SPEC.md`.
6. The Task 2 checkpoint was approved by a human who opened the renders.
</verification>

<success_criteria>
- On every rendered policy curve, at every inspected depth, on both published
  rankings and both outcomes, the legend box sits entirely outside the plot area and
  overlaps nothing it describes.
- The legend is complete and unclipped in BOTH render paths — the pipeline's
  `savefig(path, dpi=150)` with no crop, and Streamlit's `bbox_inches="tight"`,
  `dpi=200`.
- The plot area is no narrower than its 666.5 px baseline.
- The upper y padding matches the lower, and no comment claims a constraint that no
  longer exists.
- The D-06 drift gate passes: full pipeline run, then an empty
  `git status --short data/processed reports/figures`.
- The full suite is green with no deselection.
- `06-UI-SPEC.md` records the superseded position as superseded, dated, in its own
  amendment style.
</success_criteria>

<commit_discipline>
Small, frequent commits per this project's standing preference. The one hard
constraint: the `plots.py` change and the two regenerated PNGs land together, because
their co-location is what makes the D-06 drift gate pass.
</commit_discipline>

<output>
Create `.planning/quick/260911-l0p-move-the-policy-curve-legend-outside-the/260911-l0p-SUMMARY.md` when done.

The SUMMARY must record: which layout mechanism was used (`tight_layout` alone, per
F4, or an explicit post-`_fit_titles` `subplots_adjust`) and the numbers measured;
the final figure width and axes width; exactly what `git status --short` listed after
`pipeline all`; and what the human reported at the Task 2 checkpoint.
</output>

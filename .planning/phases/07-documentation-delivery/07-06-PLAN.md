---
phase: 07-documentation-delivery
plan: 06
type: execute
wave: 5
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05"]
files_modified:
  - README.md
  - tests/test_readme.py
  - reports/validity.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [DOC-01, C7-1, C7-2, C7-3, C7-4, C7-5]

must_haves:
  truths:
    - "A human has read the whole README top to bottom in its rendered form and confirmed the things no test in this repository can decide: that the prose is true, that a non-technical reader can follow it, that the depth is linked rather than restated, and that the two screenshots still show what the text beside them claims"
    - "Each of ROADMAP Phase 7's five success criteria is discharged with named evidence -- a test node ID, a command, or a recorded human check -- never with an assertion that it is met"
    - "DOC-01 is marked complete in `.planning/REQUIREMENTS.md` in both places it appears, and no other requirement's status is touched"
    - "The forward-reference sentences that promised a reader-facing overview 'arrives in Phase 7' are gone from every file that carried one, because the overview has arrived"
    - "Phase 7 closing while Phase 6 remains open on two human-only verifications is stated as a fact in the record rather than papered over by marking Phase 6 or the milestone complete"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "DOC-01 marked complete in the Documentation checklist and in the Traceability table"
      contains: "DOC-01"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7's plan list, plan count and Progress row, with the five criteria discharged"
      contains: "07-06-PLAN.md"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: "the five Phase 7 criteria"
      via: "a discharge table naming the test node or command that proves each"
      pattern: "07-0[1-6]-PLAN\\.md"
    - from: ".planning/REQUIREMENTS.md"
      to: "DOC-01"
      via: "the checklist checkbox and the Traceability row, both moved to complete together"
      pattern: "DOC-01"
---

<objective>
Have a human read the finished README, then close the phase against its own criteria.

Purpose: two jobs that belong together and cannot be done in either order alone.

The first is the phase's only prose checkpoint. `tests/test_readme.py` now carries roughly a dozen
assertions, and not one of them can decide whether a sentence is TRUE, whether a reader who does not
know what a T-learner is actually follows the method section, or whether the method section restated a
depth document instead of linking it. `tests/test_reports.py` recorded the same limit for
`reports/model.md` and discharged it with an explicit human checkpoint; plan 07-02's module docstring
promised the README would get the same treatment. This is it. The checkpoint waits until now because
reviewing the first screen before the limitations section exists reviews half a document.

The second is the close-out. ROADMAP Phase 7 has five success criteria, and a phase that closes by
asserting they are met has learned nothing from five phases of this project's own practice. Each one
is discharged here with a node ID, a command, or a recorded human check -- and where the evidence is a
human check, it is recorded as one rather than dressed up as a pass. That is the disposition plan
06-08 took when two deploy-time observations went uncaptured, and it is the reason 06-09 exists.

**This plan closes Phase 7. It does not close Phase 6 and it does not close the milestone.** Phase 6
is still open on the 12-hour cold-start check and the incognito visual check. A close-out that
quietly tidies those away would be the exact failure the project's own record was built to prevent.

Output: an approved README, five discharged criteria, DOC-01 marked, and an honest statement of what
is still open.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-documentation-delivery/07-CONTEXT.md
@.planning/phases/07-documentation-delivery/07-01-SUMMARY.md
@.planning/phases/07-documentation-delivery/07-02-SUMMARY.md
@.planning/phases/07-documentation-delivery/07-03-SUMMARY.md
@.planning/phases/07-documentation-delivery/07-04-SUMMARY.md
@.planning/phases/07-documentation-delivery/07-05-SUMMARY.md
@README.md

<interfaces>
<!-- ROADMAP Phase 7's five success criteria, and which plan owns the mechanism for each.
     This mapping is the close-out's skeleton; Task 2 turns it into a table with evidence. -->

  1  business question + headline dollar answer on the first screen, before ATE/Qini/T-learner
       -> 07-03 (first screen, ordering test), 07-04 (the jargon that makes the ordering real)
  2  every number traces to a committed artifact, verified by regenerating and diffing
       -> 07-01 (the regeneration half, ruled on in its summary), 07-03 (the provenance half),
          07-04 (the method section's own provenance), 07-05 (the limitations section's)
  3  a limitations section a skeptic would have written, six named items
       -> 07-05
  4  repo-wide grep clean in README and app + a static screenshot of the key output
       -> 07-02 (the screenshots), 07-03 (the grep test and the embed assertion)
  5  `pipeline all` on a fresh clone reproduces every artifact and figure
       -> 07-01 (tests/test_fresh_clone.py), 07-04 (the README's statement of it)

<!-- .planning/REQUIREMENTS.md carries DOC-01 in TWO places, and they must move together:
       the "### Documentation" checklist:  `- [ ] **DOC-01**: README written for a ...`
       the Traceability table row:         `| DOC-01 | Phase 7 — Documentation & Delivery | Pending |`
     APP-01 is ALSO Pending in both places. Do not touch it -- Phase 6 owns it and is open. -->

<!-- The forward-reference sentence, in TWO files. 07-03 removed the README's; the other
     is still there, found 2026-09-13: -->

  README.md line 7        "A non-technical, reader-facing overview ... arrives in Phase 7"   <- removed by 07-03, VERIFY
  reports/validity.md:3   "A non-technical, reader-facing overview arrives in Phase 7; this
                           document is the technical evidence Phase 7 will link to rather
                           than re-derive."                                                  <- still present

<!-- What Phase 6 still owes, from .planning/STATE.md. Neither is this plan's to discharge
     and neither may be marked done here:
       - the 12-hour cold-start wake check (a wall-clock human action)
       - the incognito VISUAL check (curl proved HTTP 200 on the shell; the rendered page
         was never observed, because content arrives over a websocket into an SPA) -->

<!-- The README's four marked regions as the phase leaves them:
       headline:begin/end     07-03    the first screen
       method:begin/end       07-04    the method section and the depth links
       limitations:begin/end  07-05    the six skeptic items
       then "## Setup and reproduction" and "## Data foundation decisions" -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Read the finished README as a reviewer would</name>
  <files>README.md (read only at this task; repairs, if any, are made after the report)</files>
  <read_first>
    - `README.md` -- the whole finished file, in its rendered form, not the raw source
    - `.planning/phases/07-documentation-delivery/07-CONTEXT.md` §Implementation Decisions -- D-01 through D-07, so the reviewer knows which properties are locked decisions rather than preferences
    - `docs/app_headline.png` and `docs/app_policy_curve.png` -- opened, not merely confirmed present
  </read_first>
  <what-built>
The README is complete. Four plans wrote it and roughly a dozen tests hold it in place:

- **07-03** wrote the first screen -- the targeting rule, both contrasts with their detectability
  verdicts, the versus-everyone sentence, the live link and both screenshots -- with the provenance
  test, the bounded literal sweep, the ordering test and the classification-metric ban.
- **07-04** wrote the method section and the four depth links, corrected three stale claims in the
  setup section, recorded both interpreters per D-07, and added the method-provenance,
  link-resolution, depth-link and two-interpreter tests.
- **07-05** wrote the limitations section and the six-item skeptic test.

**What those tests prove:** every number on the first screen, in the method section and in the
limitations section is derived from a committed artifact at test time; the three jargon terms appear
only below the first screen; every relative link resolves to a git-tracked path; all four depth
documents are linked; both screenshots are embedded on the first screen; the four
classification-metric tokens appear nowhere; all six of criterion 3's named items are present; and
`pipeline all` on a fresh clone reproduces every artifact and figure.

**What no test in this repository can prove, which is why this checkpoint exists:**

1. Whether a sentence is TRUE. A test can prove the README quotes `+$0.102` and that the manifest
   carries it. It cannot prove the sentence around it describes what that number means.
2. Whether a non-technical reader follows it. That is criterion 1's second clause --
   "understandable without knowing those terms" -- and it is a judgment, not an assertion.
3. Whether the method section restated a depth document rather than linking it. D-04 is a
   discipline; no regex measures it.
4. Whether the screenshots still show what the text beside them claims. `docs/SCREENSHOT-CAPTURE.md`
   records in terms that there is no honest automated currency test for a raster, and names the three
   substitutes that were rejected because each fails on correct code.
  </what-built>
  <action>
Present the ten checks below to the reviewer and pause. Do not auto-approve this checkpoint under any
workflow setting: the four properties it decides are the four no assertion in `tests/test_readme.py`
can reach, and an agent approving its own prose is the failure this task exists to prevent.

Before presenting, run `.venv/Scripts/python.exe -m pytest -q` and report the result alongside the
checks, so the reviewer knows the mechanical half is already green and is being asked only for the
judgment half.

If the reviewer reports issues: repair exactly what was named, re-run
`.venv/Scripts/python.exe -m pytest -q` to zero failures, and re-present the same ten checks. Do not
widen the repair beyond the findings, and do not proceed to Task 2 on a conditional approval. Record
every finding verbatim in the summary, including any the reviewer raised and then judged acceptable --
a finding that was considered and left is evidence; a finding that was silently dropped is not.
  </action>
  <how-to-verify>
1. Open `README.md` rendered, not raw -- GitHub's view of the pushed branch, or a local markdown
   preview. Both screenshots and every relative link must be checked as a reader sees them.

2. **Read the first screen and stop there.** Before scrolling, answer three questions from what you
   have read: who should be emailed, how much more it is worth, and whether that extra revenue is
   something this data can actually detect. If the third answer is not already clear, D-02 is not
   being kept and the label needs to move up, not be reworded.

3. Confirm the not-detectable statement sits in the **same visual block** as the dollar figure -- not
   under a rule, not in a later section. D-02 is locked and `07-CONTEXT.md` calls it the phase's
   load-bearing judgment.

4. Confirm the versus-everyone sentence is on or immediately after the first screen (D-03). A reader
   must not leave it believing targeting beat a blanket send.

5. **Open both images.** Confirm `docs/app_headline.png` renders inline, is legible at the size
   GitHub shows it, and that the numbers in it match the numbers in the text beside it. Confirm
   `docs/app_policy_curve.png` renders and includes its legend and caption. Confirm neither carries
   browser chrome, an address bar or a window title -- this repository is public.

6. **Read the method section as though you have never met the vocabulary.** After it, can you say in
   your own words what an average treatment effect is, what uplift is and why it cannot be observed
   for one person, and why accuracy is not the yardstick here? If any of the three does not land, say
   which -- that is criterion 1's "understandable without knowing those terms" failing, and it is
   repairable prose rather than a structural problem.

7. **Check for restatement.** Follow one of the four depth links and compare. If the README is
   re-arguing what the report already argues rather than pointing at it, name the paragraph. D-04's
   whole rationale is that duplicated prose drifts from its source.

8. **Read the limitations section as a skeptic.** Does it read as though someone trying to break the
   claim wrote it, or as though the author is managing an objection? Check specifically that no bullet
   argues its way back out -- the automated sweep catches `but`, `however`, `that said` and
   `nevertheless`, and catches nothing else.

9. **Check the setup section against what you would actually do.** Would the commands work from a
   fresh clone? Does the interpreter statement leave you clear that 3.11 is what you reproduce on and
   3.14.7 is what the deployment serves, with neither reading as a correction of the other (D-07)?

10. **Click every link**, including the live app link. A link that 404s in the one file every reviewer
    opens is worse than no link.

11. Report: approve, or list what needs changing with the section and the reason. Be specific about
    which of the ten checks failed -- "reads oddly" is not actionable and the next agent will guess.
  </how-to-verify>
  <verify>
    <human-check>The whole README has been read in rendered form; the first screen answers who to email, how much more it is worth and whether the revenue gain is detectable; the not-detectable label is in the same block as the dollar figure; both images render, are legible, match the text beside them and carry no browser chrome; the method section's three concepts are understandable to someone who did not know them; no section restates a depth document; the limitations read as a skeptic's list; the interpreter statement is clear in both directions; and every link resolves.</human-check>
  </verify>
  <acceptance_criteria>
    - The reviewer has opened the rendered README and both PNGs, not only the raw markdown
    - Each of the ten checks above is reported as pass or as a named, located issue
    - Any issue is recorded with the section, the problem and the reason, specific enough to act on without re-reading the whole file
    - If issues were found: they are repaired, `.venv/Scripts/python.exe -m pytest -q` is re-run to zero failures, and the checkpoint is re-reported before Task 2 begins
    - The reviewer's verdict and the full list of findings are recorded verbatim in the summary, including findings that were judged acceptable and left alone
  </acceptance_criteria>
  <resume-signal>Type "approved", or list the sections needing change with the specific problem in each.</resume-signal>
  <done>A human has read the finished document end to end in the form a reviewer sees, and the four properties no test can decide have been decided by someone qualified to decide them.</done>
</task>

<task type="auto">
  <name>Task 2: Discharge the five criteria, mark DOC-01, and state what is still open</name>
  <files>README.md, tests/test_readme.py, reports/validity.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>
    - Task 1's checkpoint report -- the verdict and every finding, including any repaired during the checkpoint
    - `.planning/ROADMAP.md` §"Phase 7: Documentation & Delivery" -- the five criteria verbatim, the `**Plans**: TBD` placeholder, and the Progress table at the foot of the file
    - `.planning/REQUIREMENTS.md` -- the `### Documentation` checklist and the Traceability table, both of which carry DOC-01, and both of which also carry APP-01, which is NOT this plan's to touch
    - `.planning/STATE.md` §Current Position -- the three items Phase 6 handed to 06-09 and which of them are still outstanding
    - `reports/validity.md` line 3 -- the forward-reference sentence that still promises an overview "arrives in Phase 7"
    - `.planning/phases/07-documentation-delivery/07-01-SUMMARY.md` through `07-05-SUMMARY.md` -- the evidence each criterion's discharge cites
  </read_first>
  <action>
Close the phase against its own criteria. Change no published number and no pipeline code.

**(a) Verify the phase-wide gates, and record the outputs rather than the verdicts.**

Run and record each:

- `.venv/Scripts/python.exe -m pytest -q` -- zero failures, zero xfailed, no `-m` deselection.
  Record the collected count; 07-04 removed the phase's only xfail, and a non-zero xfail count here
  means something re-introduced one.
- `.venv/Scripts/python.exe -m pytest tests/test_fresh_clone.py -q` -- criterion 5's own test, run
  explicitly rather than assumed to have run inside the suite.
- `grep -nE "accuracy_score|roc_auc|\.score\(|classification_report" README.md streamlit_app.py` --
  no output. This is criterion 4's first half, run as the criterion literally words it, in addition
  to the test that enforces it.
- `git status --short data/processed reports/figures` -- zero lines. The phase boundary in
  `07-CONTEXT.md` forbids Phase 7 from moving a published number; this is the proof it did not.
- `git diff --stat streamlit_app.py dont_email_everyone/` against the phase's first commit -- empty.
  No plan in this phase was permitted to touch the app or the package.

If any gate fails, stop and report. Do not close a phase around a failing gate.

**(b) The forward-reference sentence, in the file that still carries it.**

`grep -c "arrives in Phase 7" README.md` must return 0 -- plan 07-03 removed it. **Verify; do not
remove it again.** If it returns non-zero, 07-03's own acceptance criterion did not hold and that is a
finding to report, not to quietly repair.

`reports/validity.md` line 3 still carries "A non-technical, reader-facing overview arrives in Phase 7;
this document is the technical evidence Phase 7 will link to rather than re-derive." The overview has
arrived. Amend that sentence to the present tense, naming the README as the overview that now exists
and keeping the true half -- that this document is the technical evidence the README links to rather
than re-derives. Follow `06-UI-SPEC.md`'s dated in-place amendment idiom: state the new fact and date
the change. Keep the edit to that one sentence; `reports/validity.md` is a Phase 2 deliverable with
its own assertions in `tests/test_reports.py`, and a wider edit risks a test that has nothing to do
with this phase. Re-run `.venv/Scripts/python.exe -m pytest tests/test_reports.py -q` immediately
after and record the result.

Then sweep the rest of the repository for the same promise: `grep -rn "arrives in Phase 7" --include="*.md"
--include="*.py" . | grep -v "^./.planning/"`. Report anything else it finds. Do not edit `.planning/`
documents -- planning records are historical and a record that describes what was true when it was
written is not stale.

**(c) Mark DOC-01 complete, in both places, and touch nothing else.**

In `.planning/REQUIREMENTS.md`:

- `### Documentation` checklist: `- [ ] **DOC-01**` becomes `- [x] **DOC-01**`.
- Traceability table: the `DOC-01` row's status moves from `Pending` to `Complete`.

**APP-01 is Pending in both of those same places and is NOT yours.** Phase 6 owns it, Phase 6 is open,
and a close-out that tidies a neighbouring requirement is how a status table stops meaning anything.
An acceptance criterion greps for APP-01 still reading Pending afterwards.

Add nothing to the Out of Scope table and change no requirement text.

**(d) Update ROADMAP.md's Phase 7 entry.**

- Replace `**Plans**: TBD` with the plan count and the wave-structured plan list, following the format
  Phases 2 through 6 use in this same file -- wave headings, one `- [x]` line per plan with a one-line
  objective. Six plans across five waves: 07-01 and 07-02 in wave 1, 07-03 in wave 2, 07-04 in wave 3,
  07-05 in wave 4, 07-06 in wave 5.
- Under the five success criteria, add a dated discharge table: one row per criterion, carrying the
  criterion number, the plan that owns its mechanism, and the **evidence** -- a pytest node ID, a
  command, or "recorded human check, 07-0N-SUMMARY.md". Use the mapping in this plan's
  `<interfaces>` block as the skeleton. Where a criterion's evidence is a human check, say so in those
  words; do not write it as a test.
- Mark Phase 7 `- [x]` in the Phases list with the completion date, and set its Progress-table row to
  `Complete` with the plan count and date.

**(e) State what is still open, because it is.**

Phase 6's Progress row stays `In Progress` and its checkbox stays unchecked. Do not mark 06-09
complete and do not mark the milestone complete. Add a short dated note to ROADMAP Phase 7's entry
recording the situation plainly: Phase 7's deliverables are complete and its five criteria are
discharged, while Phase 6 remains open on 06-09's 12-hour cold-start wake check and the incognito
visual check, so the v1.0 milestone is not closeable yet. Name both outstanding items.

That note is the point of this sub-step rather than bookkeeping. A phase completing out of order
against an open dependency is a real fact about this project's state, and the record either carries it
or the next person rediscovers it.

Update `.planning/STATE.md` Current Position to match: Phase 7 complete, the milestone blocked on
06-09's two human-only verifications, both named.

**(f) Commit.**

One commit for the `reports/validity.md` amendment, one for the planning-document updates. The second
commit's body carries the five-criterion discharge table, so the discharge is in the git record and
not only in a summary file.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest -q` reports zero failures and zero xfailed, and the collected count is recorded
    - `.venv/Scripts/python.exe -m pytest tests/test_fresh_clone.py -q` passes, run explicitly
    - `grep -nE "accuracy_score|roc_auc|\.score\(|classification_report" README.md streamlit_app.py` returns nothing
    - `git status --short data/processed reports/figures` prints nothing
    - `git diff --stat streamlit_app.py dont_email_everyone/` against the phase's first commit is empty
    - `grep -c "arrives in Phase 7" README.md` returns 0, and the result was VERIFIED rather than re-applied
    - `grep -c "arrives in Phase 7" reports/validity.md` returns 0, and `.venv/Scripts/python.exe -m pytest tests/test_reports.py -q` passes after the amendment
    - `grep -n "DOC-01" .planning/REQUIREMENTS.md` shows `- [x]` on the checklist line and `Complete` in the Traceability row
    - `grep -n "APP-01" .planning/REQUIREMENTS.md` still shows `- [ ]` and `Pending` -- untouched
    - `.planning/ROADMAP.md` Phase 7 carries a six-plan wave-structured list and a five-row discharge table in which every row names a node ID, a command, or a recorded human check
    - `.planning/ROADMAP.md` Phase 6's checkbox is still `- [ ]` and its Progress row still reads `In Progress`
    - The dated note naming both outstanding Phase 6 verifications appears in ROADMAP Phase 7's entry and in `.planning/STATE.md`
  </acceptance_criteria>
  <done>Every Phase 7 criterion is discharged with evidence a reader can re-run, DOC-01 is complete in both places it is recorded, the last forward reference to Phase 7 is gone, and the two things still outstanding are written down instead of assumed away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated assertions -> the claim that the document is good | the tests bound the document's shape and say nothing about whether its sentences are true |
| a phase close-out -> the project's status record | a close-out is the one moment where marking something done is cheaper than checking it |
| Phase 7 completing -> Phase 6 still open | a dependency closing after its dependent is a real inconsistency that a tidy record would hide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-22 | Repudiation | a criterion marked discharged without evidence | mitigate | the ROADMAP discharge table requires a node ID, a command or a named recorded human check per row, and an acceptance criterion checks every row carries one |
| T-07-23 | Elevation of Privilege | a close-out marking a neighbouring requirement or phase complete | mitigate | APP-01 and Phase 6 are named as out of bounds, and two acceptance criteria assert both still read Pending / In Progress afterwards |
| T-07-24 | Spoofing | prose that passes every test and is still false | mitigate | Task 1 is a blocking human read of the rendered document, with the four undecidable properties enumerated so the reviewer knows what only they can settle; it is never auto-approvable |
| T-07-25 | Tampering | a one-sentence amendment to a Phase 2 deliverable breaking a Phase 2 assertion | mitigate | the `reports/validity.md` edit is bounded to one sentence and `tests/test_reports.py` is re-run immediately after, with the result recorded |
| T-07-26 | Information Disclosure | the milestone reading complete while two human-only verifications are outstanding | mitigate | a dated note in ROADMAP and STATE names both outstanding items; Phase 6's checkbox and Progress row are asserted unchanged |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no installs. No package-manager invocation occurred anywhere in Phase 7; `requirements*.txt` are untouched by all six plans |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` -- zero failures, zero xfailed, no `-m` deselection
- `.venv/Scripts/python.exe -m pytest tests/test_fresh_clone.py tests/test_readme.py tests/test_reports.py -q` -- the three modules this phase created or amended
- `grep -nE "accuracy_score|roc_auc|\.score\(|classification_report" README.md streamlit_app.py` -- no output
- `grep -rn "arrives in Phase 7" --include="*.md" --include="*.py" . | grep -v "^./.planning/"` -- no output
- `git status --short data/processed reports/figures` -- zero lines
- `git diff --stat streamlit_app.py dont_email_everyone/` -- empty across the whole phase
</verification>

<success_criteria>
A human has read the finished README in the form a reviewer sees it and approved it. All five ROADMAP
Phase 7 criteria are discharged in the record with re-runnable evidence. DOC-01 is complete in both
places it is tracked and no other requirement moved. The last forward reference to a Phase 7 that has
now happened is gone. Phase 6's two outstanding human-only verifications are named in the record, and
neither Phase 6 nor the milestone is marked complete.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-delivery/07-06-SUMMARY.md` when done, carrying the
checkpoint reviewer's verdict and every finding verbatim, the five-criterion discharge table with its
evidence, the recorded output of each phase-wide gate, the `reports/validity.md` before-and-after
sentence, and the statement of what Phase 6 still owes.
</output>

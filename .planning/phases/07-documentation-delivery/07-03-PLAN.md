---
phase: 07-documentation-delivery
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - README.md
  - tests/test_readme.py
autonomous: true
requirements: [DOC-01, C7-1, C7-2, C7-4]

must_haves:
  truths:
    - "A reader who opens the repository and reads only the first screen learns who to email, what the extra revenue is, that this data cannot distinguish that revenue gain from zero, that the site-visit gain IS detectable, and that targeting does not beat emailing everyone when email is free"
    - "The dollar figure and the words that say its interval contains zero sit in the same visual block, never in a later caveats section"
    - "Nothing on the first screen requires knowing what ATE, Qini or a T-learner is -- none of those three terms appears before the headline block, and a test enforces the ordering"
    - "Every number inside the first-screen block is derived from data/processed/manifest.json at test time through the app's own display_value, so README and app print one story at one grain and a moved artifact fails the suite"
    - "No numeric literal appears inside the first-screen block that is not either traceable to the manifest or on a short, individually justified exception list"
    - "The repo-wide grep for accuracy_score|roc_auc|.score(|classification_report still returns nothing in README.md or streamlit_app.py, and a test now keeps it that way"
    - "Both committed screenshots are embedded on the first screen, beside the live app link"
  artifacts:
    - path: "README.md"
      provides: "the first screen: the targeting rule, both contrasts with their verdicts, the versus-everyone sentence, the live link and both screenshots"
      contains: "Not detectable"
    - path: "tests/test_readme.py"
      provides: "the D-05 provenance test, the bounded literal sweep, the first-screen ordering test and the classification-metric ban"
      contains: "def test_readme_headline_numbers_trace_to_the_manifest"
  key_links:
    - from: "tests/test_readme.py"
      to: "data/processed/manifest.json"
      via: "streamlit_app.display_value over headline.per_outcome[outcome].vs_random{,_lo,_hi}"
      pattern: "display_value"
    - from: "README.md"
      to: "docs/app_headline.png"
      via: "a markdown image embed on the first screen"
      pattern: "docs/app_headline\\.png"
    - from: "README.md"
      to: "the deployed app"
      via: "the live https link, already asserted by tests/test_app.py::test_readme_carries_the_live_app_link"
      pattern: "streamlit\\.app"
---

<objective>
Write the first screen, and make it impossible for it to drift from the artifacts.

Purpose: ROADMAP Phase 7 criteria 1, 2 and 4, and decisions D-01, D-02, D-03 and D-05, which
`07-CONTEXT.md` marks LOCKED. The first screen is where this project either is or is not the thing it
claims to be. A portfolio piece that leads with **+$0.102** and leaves the interval for a caveats
section further down is the exact failure the project exists to avoid -- and the deployed app already
refuses to do it, which is why D-01 requires the README to mirror the app's framing rather than
invent a second one.

D-05 says number provenance is enforced by a test, not by generating the README. This plan builds
that test the strong way: the expected strings are produced by calling the app's own
`display_value` on `manifest.json`'s own values, so the test holds no copy of any number and the two
reader-facing surfaces cannot print the same quantity at two grains.

Output: the README's first screen, and four tests behind it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-documentation-delivery/07-CONTEXT.md
@.planning/phases/07-documentation-delivery/07-01-SUMMARY.md
@.planning/phases/07-documentation-delivery/07-02-SUMMARY.md
@README.md

<interfaces>
<!-- The manifest fields the first screen publishes. All at frame.capacity_k = 0.20, the
     pre-registered anchor, which is also the depth the app opens on and the depth the
     screenshots were captured at. -->

  manifest["frame"]["n_customers"]   21347
  manifest["frame"]["n_targeted"]     4269
  manifest["frame"]["capacity_k"]      0.2
  manifest["headline"]["contrast"]    "vs_random_send_of_the_same_size"

  headline.per_outcome["spend"]["vs_random"]      0.10159329179744209
  headline.per_outcome["spend"]["vs_random_lo"]  -0.029910549491731792
  headline.per_outcome["spend"]["vs_random_hi"]   0.3034145172623786
  headline.per_outcome["visit"]["vs_random"]      0.00616480067456786
  headline.per_outcome["visit"]["vs_random_lo"]   0.0024350025764744523
  headline.per_outcome["visit"]["vs_random_hi"]   0.010484377195858896

  headline["caveat"]  the versus-everyone paragraph D-03 rests on, in full

<!-- The app's display grain. streamlit_app.display_value(outcome, value), lines 529-579.
     THREE decimals, sign before the currency sign, raw rate for non-currency outcomes.
     Deliberately three and not six: reports/policy.md keeps six as the checkable record,
     the reader-facing surfaces print only the digits the data supports. The README is a
     reader-facing surface and takes three. -->

  display_value("spend", 0.10159329179744209)   ->  "+$0.102"
  display_value("spend", -0.029910549491731792) ->  "-$0.030"
  display_value("spend", 0.3034145172623786)    ->  "+$0.303"
  display_value("visit", 0.00616480067456786)   ->  "+0.006"
  display_value("visit", 0.0024350025764744523) ->  "+0.002"
  display_value("visit", 0.010484377195858896)  ->  "+0.010"

<!-- The app's two verdict strings, verbatim, streamlit_app.py lines 400-414.
     The README's labels mirror these; the test keys on the distinctive stem of each. -->

  VERDICT_COVERS_ZERO  "**Not detectable at this depth:** the 95% interval includes zero,
                        so this data cannot distinguish this from no gain at all."
  VERDICT_ABOVE_ZERO   "**Detectable at this depth:** the 95% interval lies entirely
                        above zero."

<!-- Already asserted elsewhere; do not duplicate:
     tests/test_app.py::test_readme_carries_the_live_app_link
       - an https://<sub>.streamlit.app link exists
       - "12 hours without traffic" appears
       - "get this app back up" appears
       - the wake note is within 10 lines of the link
     Keep all four true. The link and the sleep note move INTO the first-screen block. -->

<!-- Committed by plan 07-02, to be embedded here:
     docs/app_headline.png       the bordered headline block
     docs/app_policy_curve.png   the spend policy curve at the 1100 px cap -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the first screen</name>
  <files>README.md</files>
  <read_first>
    - `.planning/phases/07-documentation-delivery/07-CONTEXT.md` §Implementation Decisions -- D-01's four-item ordering, D-02 and D-03, all LOCKED
    - `streamlit_app.py` lines 925-1018 -- the headline block's own copy, which D-01 mirrors; and lines 679-691, `VERSUS_EVERYONE_SENTENCE`
    - `data/processed/manifest.json` -- `frame`, `headline.per_outcome`, `headline.caveat`
    - `README.md` -- the whole current file, including the sentence at line 7 that this task removes
    - `.planning/phases/07-documentation-delivery/07-02-SUMMARY.md` -- the two committed image paths
  </read_first>
  <action>
Rewrite the top of `README.md` so the first screen carries D-01's four items in D-01's order. Leave
everything from "## Setup and reproduction" downward alone -- plan 07-04 owns it. Do not touch
`streamlit_app.py`, any artifact, or any number.

**The order is fixed by D-01 and is not open:**

1. **The targeting rule as a plain instruction.** With a budget of 4,269 sends to a 21,347-person
   list, email the top 20% ranked by predicted uplift rather than 4,269 people picked at random.
2. **Extra revenue vs that random send: +$0.102 per customer, 95% interval -$0.030 to +$0.303**,
   labelled **not detectable at this depth -- this data cannot distinguish the gain from zero.**
3. **Extra site visits vs that random send: +0.006 per customer, 95% interval +0.002 to +0.010**,
   labelled **detectable -- the interval lies entirely above zero.**
4. The live app link and the embedded screenshots.

**D-02 is the load-bearing constraint.** The not-detectable label goes in the same visual block as
the dollar figure. Not in a footnote, not below a horizontal rule, not in the limitations section
plan 07-04 writes. A reader who reads the number and stops must already have read that its interval
contains zero. Mirror the app's own two verdict sentences rather than inventing new wording -- they
are human-reviewed, and D-01's whole point is that a reviewer who clicks the live link finds no
discrepancy between the two surfaces.

**D-03: the versus-everyone result goes on the first screen or immediately after it.** Say plainly
that no depth in this data produces a versus-everyone interval that excludes zero from above; that
with genuinely free email the correct action is to email everyone; and that the whole question only
bites under a fixed budget, which is why the comparator is a random send of the same size. The
source is `manifest.json`'s `headline.caveat` and `streamlit_app.VERSUS_EVERYONE_SENTENCE`. A reader
must not leave the first screen believing targeting beat a blanket send.

**Criterion 1's ordering constraint.** Nothing above or inside the headline block may mention
**ATE**, **Qini** or **T-learner**. "Uplift" is permitted and is D-01's own word -- the recommendation
sentence says "ranked by predicted uplift", and that phrase is understandable from context. The three
banned terms are jargon a non-technical reader cannot decode; they belong in plan 07-04's method
section, below.

**The numbers are transcribed from the artifact, not from this plan.** Read them out of
`data/processed/manifest.json` and format them with `streamlit_app.display_value`. The literals quoted
above are for orientation and were read on 2026-09-13; the artifact is the source. Task 2's test will
fail on any disagreement, which is the point.

**Delimit the block.** Wrap the first-screen content in `<!-- headline:begin -->` and
`<!-- headline:end -->` HTML comments. These are invisible in rendered markdown and they turn "the
first screen" from a matter of eye into a region a test can bound. Task 2's literal sweep needs
exactly that boundary, and a marker that survives rewording is better than a heuristic that guesses
where a section ends. Record the choice in the summary so it reads as deliberate.

**Move the live link and the sleep/wake note inside the block**, keeping them adjacent to each other
-- `tests/test_app.py::test_readme_carries_the_live_app_link` requires the wake note within 10 lines
of the link, and it must stay green.

**Embed both screenshots** from plan 07-02 with descriptive alt text, and put a one-line caption
under the headline image saying it is the app's own headline block, so a reader on a cold app knows
what they are looking at.

**Delete the sentence at line 7** -- "A non-technical, reader-facing overview ... arrives in Phase 7;
this README currently covers environment setup and pipeline reproduction only." That sentence is this
phase's entry point and the overview has now arrived. `07-CONTEXT.md` records that it must be gone at
the end.

Commit as its own atomic commit. The body explains what the first screen is for and why the
not-detectable label is inside the same block as the number, citing D-02.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_app.py::test_readme_carries_the_live_app_link -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "arrives in Phase 7" README.md` returns 0
    - `grep -c "headline:begin" README.md` and `grep -c "headline:end" README.md` each return 1
    - The four D-01 items appear inside the markers in D-01's order
    - The not-detectable label is inside the same block as the dollar figure, above the `headline:end` marker
    - `grep -cE "\bATE\b|Qini|T-learner" README.md` returns 0 for the text above `headline:end`
    - `grep -c "docs/app_headline.png" README.md` and `grep -c "docs/app_policy_curve.png" README.md` each return at least 1
    - `.venv/Scripts/python.exe -m pytest tests/test_app.py::test_readme_carries_the_live_app_link -q` passes
    - `git status --short data/processed reports/figures` prints nothing
  </acceptance_criteria>
  <done>The first screen answers the business question in plain language with the honest version of the dollar answer, links and shows the live app, and the Phase 7 placeholder sentence is gone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The D-05 provenance test, both directions</name>
  <files>tests/test_readme.py</files>
  <read_first>
    - `tests/test_reports.py` lines 862-1010 -- `_manifest`, `_quote`, and `test_policy_report_traces_its_headline_numbers_to_the_artifact`, the direct precedent for this test's forward half
    - `streamlit_app.py` lines 529-579 -- `display_value` and the recorded argument for three decimals against `policy.md`'s six
    - `tests/test_app.py` lines 50-68 and 325-330 -- `import streamlit_app` is already an established test-time import, and `_committed_manifest` is the existing reader
    - `.planning/phases/07-documentation-delivery/07-01-SUMMARY.md` -- the criterion 2 ruling this test is the first link of
  </read_first>
  <behavior>
    - Forward: each of the six first-screen scalars, formatted by `streamlit_app.display_value` from `manifest.json`, appears verbatim in `README.md`
    - Forward: `n_customers`, `n_targeted` and `capacity_k` appear at the manifest's values, formatted with thousands separators and as a percentage respectively
    - Reverse: every numeric literal inside the `headline:begin`/`headline:end` region is either one of the manifest-derived strings above or on a short, individually justified exception list
    - The exception list is closed and each entry carries a reason -- a bare number with no justification fails
    - Negative control: perturbing one manifest value in a copy makes the forward test fail naming that field; adding an unjustified number inside the markers makes the reverse test fail naming it
  </behavior>
  <action>
Extend `tests/test_readme.py` with the D-05 provenance block. This is the test that makes the README
checkable, and it is the first link of the chain plan 07-01 ruled on -- restate that chain in a
comment above the block so a reader of this file alone can see where the numbers come from.

**`test_readme_headline_numbers_trace_to_the_manifest` (the forward direction).**

Derive every expected string; type none of them. For each of the six `(outcome, key)` pairs --
`spend`/`visit` crossed with `vs_random`, `vs_random_lo`, `vs_random_hi` -- compute
`streamlit_app.display_value(outcome, manifest["headline"]["per_outcome"][outcome][key])` and assert
the result appears in `README.md`.

**Formatting through the app's own function is the design, not a convenience**, and the docstring must
say why. D-01 requires the README and the app to tell one story; if the README formatted these
independently, the two surfaces could print one quantity at two grains and both tests would pass.
Routing through `display_value` makes the grain a property of the app module rather than of a literal
in this file, so a precision change in the app either propagates to the README or fails here. That is
the same move `streamlit_app.curve_caption` makes by formatting its anchor from
`economics.HEADLINE_CAPACITY` rather than typing the percentage.

Add the three frame quantities the recommendation sentence carries: `f"{n_customers:,}"`,
`f"{n_targeted:,}"` and `f"{capacity_k:.0%}"`. Assert `headline["contrast"]` is
`"vs_random_send_of_the_same_size"` as a guard -- if the artifact's headline contrast ever changed,
every sentence on the first screen would be about a different comparison while still quoting numbers
that matched.

Each failure message names the manifest field and states the consequence in the style
`tests/test_reports.py` uses: a headline the artifact carries that the README does not quote is a
headline nobody can check.

**`test_readme_first_screen_publishes_no_untraceable_number` (the reverse direction).**

This is the half criterion 2 actually asks for and the half a forward test cannot reach. The forward
test proves the manifest's numbers are in the README; it says nothing about a number in the README
that came from nowhere.

Slice `README.md` between `<!-- headline:begin -->` and `<!-- headline:end -->`. Assert both markers
exist and that begin precedes end -- a missing marker must fail loudly rather than silently sweep an
empty string, which is the failure mode that turns this test into a no-op.

Extract every numeric literal from that region with a regex over optional sign, optional currency
sign, digits with thousands separators, optional decimal part, optional trailing percent. Assert each
match is in an allowed set built as:

- the six `display_value` strings and the three frame quantities from the forward test, and
- a module-level `FIRST_SCREEN_NON_RESULT_NUMBERS` tuple, where each entry is paired with the reason
  it is not a result. Expect entries like `"95"` (the interval's confidence level, a convention and
  not a measurement), `"2008"` (the experiment's year), `"12"` (the Community Cloud sleep window in
  hours), `"64,000"` (the experiment's arm-total, if the first screen names it). Add nothing to that
  tuple without a reason beside it -- an unreasoned entry is how this test degrades into an
  allowlist of whatever happens to be in the file.

The failure message must print the offending literal and the surrounding 80 characters, and must say
what to do: either trace the number to `manifest.json` and quote it through `display_value`, or add it
to the non-result tuple **with its reason**. Do not tell the reader to widen the regex.

State the scope limit honestly in the docstring: this sweep covers the first-screen region only. It
is bounded there because that is where criteria 1 and 2 bite hardest and where the markers make the
boundary exact; the rest of the README is covered by the forward test and by plan 07-04's own
assertions. A sweep over the whole file would have to allowlist Python versions, artifact counts and
section numbers, and an allowlist that long stops being evidence.

**Both negative controls before committing.** Perturb one manifest value in a copied fixture and
confirm the forward test fails naming the field. Insert an unjustified `$1.23` inside the markers and
confirm the reverse test fails naming it and printing its context. Restore both, record both messages
in the summary.

Commit as its own atomic commit whose body states the criterion 2 chain in full.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_readme.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_readme.py -q` passes
    - `grep -v '^ *#' tests/test_readme.py | grep -c "display_value"` returns at least 1 -- the expected strings are produced by the app's function, not typed
    - No literal `0.102`, `0.030`, `0.303`, `0.006`, `0.002` or `0.010` appears as an expected value anywhere in the test file
    - `FIRST_SCREEN_NON_RESULT_NUMBERS` exists and every entry carries a stated reason
    - Both negative controls were executed and their messages are recorded in the summary
    - `.venv/Scripts/python.exe -m pytest -q` reports zero failures
  </acceptance_criteria>
  <done>Every number on the README's first screen is derived from the committed manifest at test time, and no number appears there that is not either derived or explicitly justified as a non-result.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The ordering test, the classification-metric ban, and the screenshot embeds</name>
  <files>tests/test_readme.py</files>
  <read_first>
    - `tests/test_reports.py` lines 1460-1484 -- `test_policy_report_has_no_classification_metric_headline`, including the halved-token construction that stops the test file tripping the grep it enforces
    - `tests/test_reports.py` lines 924-970 -- `test_policy_report_states_the_anchor_before_any_result`, the offset-comparison idiom for an ordering assertion
    - `.planning/phases/07-documentation-delivery/07-CONTEXT.md` §Established Patterns -- criterion 4's grep is already clean and must be KEPT clean, not established
  </read_first>
  <behavior>
    - The first appearance of `ATE` (word-boundary, case-sensitive), `Qini` or `T-learner` in README.md is at a greater offset than the `headline:end` marker
    - The ordering test is non-vacuous: at least one of the three terms actually appears later in the document, so the assertion has something to order against
    - Neither `README.md` nor `streamlit_app.py` contains `accuracy_score`, `roc_auc`, `classification_report` or `.score(`
    - Both `docs/app_headline.png` and `docs/app_policy_curve.png` are referenced from inside the first-screen region
    - Negative control: moving a banned term above the marker fails the ordering test naming both offsets
  </behavior>
  <action>
Close criteria 1 and 4 with three more tests in `tests/test_readme.py`.

**`test_readme_states_the_result_before_any_jargon`.** Criterion 1 requires the business question and
the headline dollar answer to appear "before any mention of ATE, Qini, or T-learner, and
understandable without knowing those terms". Find the offset of `<!-- headline:end -->`; find the
first offset of each banned term; assert each is greater.

Match `ATE` with a word boundary and case-sensitively -- `\bATE\b`. An unanchored substring match
hits "estimate", "rate" and "generated", which would fail on correct prose and get the test weakened.
Say that in a comment, because it is exactly the kind of thing a later reader repairs in the wrong
direction.

Then guard against vacuity: assert at least one of the three terms appears somewhere in the document.
If none does, the ordering assertion is true of any file and is testing nothing -- the same argument
`tests/test_reports.py` records for why `policy.md`'s ordering test was deliberately not written in
plan 05-01. Plan 07-04 writes the method section that introduces these terms, so if this assertion
fails at the end of 07-03, it is scheduled rather than broken; record that in the docstring, and if
it fails at this point mark it `xfail` with a reason naming 07-04 rather than deleting it.

**`test_readme_and_app_have_no_classification_metric`.** Criterion 4's first half. Sweep both
`README.md` and `streamlit_app.py` for `accuracy_score`, `roc_auc`, `classification_report` and
`.score(`. Assemble each token by concatenation exactly as
`tests/test_reports.py::test_policy_report_has_no_classification_metric_headline` does, so this file
does not trip the repository-wide grep it exists to enforce.

The docstring must record the state accurately: this grep was verified clean in both files on
2026-09-13, so this test **keeps** a property rather than establishes one. That distinction matters to
whoever reads a future failure -- it means something was added, not that something was never fixed.
And it must say why the property matters: accuracy is the wrong yardstick for uplift, and a
classification metric presented as a headline result would be this project asserting the exact
confusion it was built to avoid.

**`test_readme_embeds_both_app_screenshots`.** Assert both `docs/app_headline.png` and
`docs/app_policy_curve.png` are referenced from inside the `headline:begin`/`headline:end` region, not
merely somewhere in the file. Criterion 4's purpose is that the result survives a cold app; an image
below the fold is one the reviewer who bounced off the sleep page never reaches. This is the
assertion plan 07-02 deliberately deferred to here, and the comment should say so.

Run the negative control on the ordering test: insert `Qini` above the marker, confirm the failure
names both offsets, remove it. Record the message.

Commit as its own atomic commit.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_readme.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_readme.py -q` passes
    - `grep -nE "accuracy_score|roc_auc|\.score\(|classification_report" README.md streamlit_app.py` returns nothing
    - The banned-token list in the test file is assembled by concatenation, so `grep -nE "accuracy_score|roc_auc|classification_report" tests/test_readme.py` returns nothing
    - The ordering test uses `\bATE\b` with case sensitivity, and carries the vacuity guard
    - Both screenshot paths are asserted from inside the marker region
    - The ordering-test negative control was executed and its message is recorded in the summary
    - `.venv/Scripts/python.exe -m pytest -q` reports zero failures
  </acceptance_criteria>
  <done>Criterion 1's ordering constraint and criterion 4's grep are both enforced by tests, and the cold-app screenshots are pinned to the first screen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed artifacts -> README prose | a hand-written document quoting machine-generated numbers is where transcription error lives |
| README -> a reviewer who reads only the first screen | the only surface most readers see; anything omitted here is effectively not said |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Spoofing | README headline numbers | mitigate | `test_readme_headline_numbers_trace_to_the_manifest` derives every expected string from `manifest.json` through `streamlit_app.display_value`; the test holds no copy of any number and an acceptance check greps the test file to prove it |
| T-07-09 | Information Disclosure | a number on the first screen with no artifact behind it | mitigate | `test_readme_first_screen_publishes_no_untraceable_number` sweeps the marker-bounded region and requires every literal to be manifest-derived or on a reasoned non-result list |
| T-07-10 | Repudiation | a dollar figure quoted without its interval | mitigate | D-02 puts the not-detectable label inside the same block; the ordering test proves no jargon precedes the block and Task 1's acceptance greps the label's presence above `headline:end` |
| T-07-11 | Tampering | a classification metric re-entering the README or the app | mitigate | `test_readme_and_app_have_no_classification_metric`, tokens assembled by concatenation so the test cannot trip its own grep |
| T-07-12 | Denial of Service | Community Cloud sleeping the app | transfer | mitigated by the committed screenshots from plan 07-02, asserted here to sit inside the first-screen region; the sleep/wake note remains asserted by `tests/test_app.py` |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | no installs; this plan edits one markdown file and one test file |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` -- zero failures, no `-m` deselection
- `grep -nE "accuracy_score|roc_auc|\.score\(|classification_report" README.md streamlit_app.py` -- no output
- `git status --short data/processed reports/figures` -- zero lines
- `git diff --stat streamlit_app.py dont_email_everyone/` -- empty; this plan changes no app and no pipeline code
</verification>

<success_criteria>
A reader of the first screen alone learns the targeting rule, the extra revenue, that the revenue
gain is not distinguishable from zero in this data, that the visit gain is, and that targeting does
not beat emailing everyone when email is free -- without meeting the word ATE, Qini or T-learner, and
with a static image of the result that does not need the app to be awake. Every number in that block
is derived from a committed artifact at test time.
</success_criteria>

<output>
Create `.planning/phases/07-documentation-delivery/07-03-SUMMARY.md` when done, carrying the marker
convention, the contents of `FIRST_SCREEN_NON_RESULT_NUMBERS` with each reason, and all three
negative-control failure messages.
</output>

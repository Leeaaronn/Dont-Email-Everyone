---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - .gitignore
  - pyproject.toml
  - requirements.txt
  - requirements-dev.txt
autonomous: true
requirements: [DATA-01, DATA-04]
must_haves:
  truths:
    - "git reports `text: unset` for data/raw/*.csv, so the CSV can be committed byte-exact"
    - "A Python 3.11 venv exists with the exact pinned stack installed and importable"
    - "pytest can import the flat `dont_email_everyone` package with no install step and no sys.path hacks"
    - "A .duckdb file cannot be accidentally committed"
  artifacts:
    - path: ".gitattributes"
      provides: "byte-stability gate for vendored raw data"
      contains: "data/raw/*.csv -text"
    - path: ".gitignore"
      provides: "prevents committing venv, caches, and DuckDB files"
      contains: "*.duckdb"
    - path: "pyproject.toml"
      provides: "pytest config making the flat layout importable"
      contains: "pythonpath"
    - path: "requirements.txt"
      provides: "pinned runtime dependency set"
      contains: "pandas==3.0.5"
    - path: "requirements-dev.txt"
      provides: "pinned dev dependency set (pytest)"
      contains: "pytest==9.1.1"
  key_links:
    - from: ".gitattributes"
      to: "data/raw/hillstrom.csv"
      via: "git attribute applied at `git add` time"
      pattern: "data/raw/\\*\\.csv\\s+-text"
    - from: "pyproject.toml"
      to: "dont_email_everyone package"
      via: "pytest pythonpath resolution"
      pattern: "pythonpath\\s*=\\s*\\[\"\\.\"\\]"
    - from: "requirements-dev.txt"
      to: "requirements.txt"
      via: "-r include"
      pattern: "^-r requirements\\.txt"
---

## Objective — CONTEXT.md decisions implemented: D-04, D-05, D-07, D-09

<objective>
Establish the repo's byte-stability gate, ignore rules, test configuration, and the exact pinned Python 3.11 environment — **before any data file is ever staged in git**.

Purpose: `.gitattributes` must exist in HEAD before `data/raw/hillstrom.csv` is added, or git stores an LF-normalized blob while the working tree keeps CRLF. RESEARCH.md §Pitfall 1 reproduced this defect on this exact machine (`core.autocrlf=true` is set globally, confirmed). A SHA-256 recorded over bytes git will later rewrite is not provenance. Everything else in this phase is downstream of this one ordering constraint.

Output: `.gitattributes`, `.gitignore`, `pyproject.toml`, `requirements.txt`, `requirements-dev.txt`, and a working `.venv` on Python 3.11 with the verified stack installed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-PATTERNS.md
@.planning/phases/01-data-foundation/01-VALIDATION.md

<interfaces>
<!-- Greenfield repo: zero existing source files, zero analogs. -->
<!-- Every pattern below is seeded from 01-RESEARCH.md §Code Examples, which were -->
<!-- EXECUTED and VERIFIED in a Python 3.11.5 venv on this machine against the real -->
<!-- 3,964,977-byte CSV. Where 01-RESEARCH.md conflicts with .planning/research/ -->
<!-- PITFALLS.md or ARCHITECTURE.md, 01-RESEARCH.md wins. -->

Pinned dependency set (01-RESEARCH.md §Standard Stack — installed together with
`--only-binary=:all:`, zero conflicts, zero source builds, all cp311-win_amd64 wheels):

  runtime:  pandas==3.0.5, numpy==2.4.6, pandera[pandas]==0.32.1, duckdb==1.5.5,
            pyarrow==25.0.1, scipy==1.17.1, scikit-learn==1.9.0,
            statsmodels==0.15.0, matplotlib==3.11.1
  dev:      -r requirements.txt, pytest==9.1.1

Version-cap facts that must not be "helpfully upgraded":
  - numpy >=2.5 requires Python >=3.12 -> 2.4.6 is the newest cp311 build
  - scipy >=1.18 requires Python >=3.12 -> 1.17.1 is the newest cp311 build
  - pandera 0.33.1 was published one day before research -> pin 0.32.1 (§Pitfall 7)
  - pyarrow is NOT auto-installed by pandas; `to_parquet` raises ImportError without it (§Pitfall 8)
  - streamlit is deliberately absent until Phase 6 (D-07)

pytest config (01-RESEARCH.md §Code Example 5 — verified resolving the flat package
with no install step):

  [tool.pytest.ini_options]
  pythonpath = ["."]
  testpaths  = ["tests"]
  addopts    = "--strict-markers -q"
  markers    = ["slow: long-running integration tests"]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the byte-stability gate and ignore rules before any data lands</name>
  <files>.gitattributes, .gitignore</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Architecture Patterns / Pattern 1 (byte-stability gate), §Pitfall 1 (verified CRLF reproduction), §Pitfall 2 (commit ordering), §Runtime State Inventory (gitignore contents)
    - .planning/phases/01-data-foundation/01-PATTERNS.md — the `.gitattributes` and `.gitignore` sections
    - .planning/phases/01-data-foundation/01-CONTEXT.md — D-05 (project-local .venv), D-09 (no .duckdb file is ever committed)
  </read_first>
  <action>
Create `.gitattributes` at repo root containing exactly one active rule plus a comment explaining it: `data/raw/*.csv -text -diff`. `-text` unsets git's line-ending conversion (the correctness-critical half); `-diff` stops git from attempting textual diffs of a 4 MB data file (ergonomic only). Do not use `* text=auto` or any broader normalization rule anywhere in this file — a global normalization rule would defeat the specific `-text` unset.

Create `.gitignore` at repo root with these entries: `.venv/`, `__pycache__/`, `*.py[cod]`, `.pytest_cache/`, `*.duckdb`, `*.duckdb.wal`. `*.duckdb` is the load-bearing entry — it enforces CONTEXT.md D-09 (DuckDB is an in-process engine, never a committed artifact) structurally rather than by discipline, for Phases 2-7 as well as this one.

Do NOT ignore `data/`, `data/raw/`, `data/processed/`, `*.csv`, or `*.parquet`. The vendored CSV, its checksum sidecar, and the processed Parquet artifacts are all deliberately committed (DATA-01, D-09). If a generic Python `.gitignore` template is used as a starting point, strip any data/CSV/parquet exclusion lines from it.

Commit these two files as their own isolated commit, with no other files staged, before this plan's remaining tasks run. Per RESEARCH.md §Pitfall 2, git attribute rules are applied at `git add` time — a CSV staged before this commit keeps a normalized blob permanently, and adding `.gitattributes` afterward does not retroactively rewrite it. Suggested message: `chore(01): pin CRLF-safe attributes for vendored raw data`.
  </action>
  <verify>
    <automated>git check-attr text diff data/raw/hillstrom.csv</automated>
  </verify>
  <acceptance_criteria>
    - `git check-attr text diff data/raw/hillstrom.csv` prints `data/raw/hillstrom.csv: text: unset` and `data/raw/hillstrom.csv: diff: unset` (this works before the file exists — attributes are path-based)
    - `git ls-files .gitattributes .gitignore` lists both files, i.e. both are committed, not merely written
    - `git log --oneline -1 --name-only` shows the most recent commit touching only `.gitattributes` and `.gitignore`
    - `grep -v '^#' .gitignore | grep -c '^\*\.duckdb$'` returns 1
    - `grep -v '^#' .gitignore | grep -cE '^(data/|\*\.csv|\*\.parquet)'` returns 0
    - `grep -v '^#' .gitattributes | grep -c 'text=auto'` returns 0
  </acceptance_criteria>
  <done>`.gitattributes` and `.gitignore` are committed in an isolated commit; git reports `text: unset` for `data/raw/*.csv`; no data path is ignored.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the dependency set and pytest configuration</name>
  <files>requirements.txt, requirements-dev.txt, pyproject.toml</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Standard Stack (the exact pinned set and the cp311 version caps), §Code Example 5 (pytest ini options), §Open Question 1 (pyarrow admissibility), §Open Question 2 (dev-file split), §Pitfall 7 (pandera 0.32.1 vs 0.33.1), §Pitfall 8 (pyarrow not bundled)
    - .planning/phases/01-data-foundation/01-PATTERNS.md — the `requirements.txt` / `requirements-dev.txt` and `pyproject.toml` sections
    - CLAUDE.md — the closed library allowlist and the Python-only constraint
    - .gitignore (from Task 1) — confirm nothing here is ignored
  </read_first>
  <action>
Create `requirements.txt` with exact `==` pins only (never `>=` or `~=`), one per line, in this set: `pandas==3.0.5`, `numpy==2.4.6`, `pandera[pandas]==0.32.1`, `duckdb==1.5.5`, `pyarrow==25.0.1`, `scipy==1.17.1`, `scikit-learn==1.9.0`, `statsmodels==0.15.0`, `matplotlib==3.11.1`. Do not add `streamlit` — CONTEXT.md D-07 requires `dont_email_everyone/` never import it, and Phase 6 owns that dependency. Do not add `pytest` here (see the dev file below).

Add a short comment block at the top of `requirements.txt` recording two decisions so they are explicit rather than silent:
  (a) RESEARCH.md Open Question 1 / PATTERNS.md conflict C6 — `pyarrow` is not named in CLAUDE.md's allowlist, but `pandas.to_parquet` raises `ImportError` without it and CONTEXT.md D-09 mandates Parquet as the committed artifact format. Resolution: read the allowlist as governing **modeling/analysis** libraries (its stated rationale is "demonstrates the technique from first principles, not library calls"); `pyarrow` is admitted as an I/O engine, in the same category as the `joblib`/`patsy`/`pydantic` transitives that arrive with scikit-learn/statsmodels/pandera.
  (b) `numpy` and `scipy` are capped by Python 3.11: numpy >=2.5 and scipy >=1.18 both require Python >=3.12. Pinning them makes the cap visible instead of incidental.

Create `requirements-dev.txt` containing exactly `-r requirements.txt` on the first line and `pytest==9.1.1` on the second. Per RESEARCH.md Open Question 2, the split happens now rather than in Phase 6: Streamlit Community Cloud reads `requirements.txt` directly and has no dev-dependency concept, and Phase 6's ROADMAP criterion 4 requires the serve-time file to exclude scikit-learn/statsmodels/DuckDB/Pandera — starting the separation now means Phase 6 edits its own file rather than restructuring this one.

Create `pyproject.toml` at repo root containing a `[tool.pytest.ini_options]` table with `pythonpath = ["."]`, `testpaths = ["tests"]`, `addopts = "--strict-markers -q"`, and `markers = ["slow: long-running integration tests"]`. `pythonpath = ["."]` is the mechanism that makes CONTEXT.md D-07's flat layout importable with no build/install step, for both pytest now and Streamlit Community Cloud in Phase 6 — do not substitute `sys.path` manipulation in `conftest.py`, and do not add a `[build-system]` or `[project]` table (this repo is not a distributable package).

Commit as its own commit, e.g. `chore(01): pin python 3.11 dependency set and pytest config`.
  </action>
  <verify>
    <automated>py -3.11 -c "import tomllib,pathlib; c=tomllib.loads(pathlib.Path('pyproject.toml').read_text())['tool']['pytest']['ini_options']; assert c['pythonpath']==['.'], c; assert c['testpaths']==['tests'], c; print('pyproject ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` parses as TOML and `[tool.pytest.ini_options].pythonpath == ["."]`, `.testpaths == ["tests"]`, and `"slow: long-running integration tests"` is in `.markers`
    - `grep -v '^#' requirements.txt | grep -c '=='` returns 9, and `grep -v '^#' requirements.txt | grep -cE '(>=|~=|^streamlit|^pytest)'` returns 0
    - `grep -v '^#' requirements.txt` contains each of: `pandas==3.0.5`, `numpy==2.4.6`, `pandera[pandas]==0.32.1`, `duckdb==1.5.5`, `pyarrow==25.0.1`, `scipy==1.17.1`, `scikit-learn==1.9.0`, `statsmodels==0.15.0`, `matplotlib==3.11.1`
    - `head -1 requirements-dev.txt` is `-r requirements.txt` and `grep -c 'pytest==9.1.1' requirements-dev.txt` returns 1
    - `grep -ci pyarrow requirements.txt` returns at least 2 (the pin plus the recorded-decision comment)
    - `git ls-files requirements.txt requirements-dev.txt pyproject.toml` lists all three
  </acceptance_criteria>
  <done>Three config files exist and are committed; the dependency set is exactly the nine verified runtime pins plus pytest in the dev file; the pyarrow allowlist reading is recorded in-repo as a comment.</done>
</task>

<task type="auto">
  <name>Task 3: Create the Python 3.11 venv and install the pinned stack</name>
  <files>(no tracked files — creates the gitignored .venv/ directory)</files>
  <read_first>
    - requirements.txt, requirements-dev.txt (from Task 2) — the exact set to install
    - .gitignore (from Task 1) — confirm `.venv/` is ignored before creating it
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Standard Stack "Installation" block, §Environment Availability (Python 3.11.5 confirmed at `C:\Users\leeaa\AppData\Local\Programs\Python\Python311\python.exe`, no C/Rust toolchain needed), §State of the Art (`import pandera.pandas as pa`)
    - .planning/phases/01-data-foundation/01-CONTEXT.md — D-04, D-05
  </read_first>
  <action>
Create the project-local virtual environment with `py -3.11 -m venv .venv` at repo root (CONTEXT.md D-05: project-local, never a global/system install; D-04: Python 3.11, because pip on 3.9 resolves pandera to 0.26.1 which predates the `pandera.pandas` module).

Activate it (`.\.venv\Scripts\Activate.ps1` in PowerShell, or `.venv/Scripts/python.exe` invoked directly from the Bash tool), upgrade pip, then install with `python -m pip install --only-binary=:all: -r requirements-dev.txt`. The `--only-binary=:all:` flag is deliberate and doubles as a supply-chain control (threat T-01-SC): it refuses source distributions, so no arbitrary `setup.py` build code executes. RESEARCH.md verified all ten packages resolve to `cp311-win_amd64` wheels with zero conflicts and zero source builds — if pip reports that it needs to build any wheel from source, stop and report rather than falling back to allowing sdists.

Confirm the resolved versions equal the pins exactly (a resolver drifting off a pin is the observable symptom of a bad pin set), and confirm the two import facts the rest of the phase depends on: `import pandera.pandas as pa` works (the bare `import pandera as pa` path emits a `FutureWarning` announcing removal — a warning here is expected and is not a failure), and `pyarrow` is importable so `to_parquet` will not raise later.

This task creates no tracked files. `.venv/` is gitignored by Task 1. There is nothing to commit; note the resolved `pip freeze` output in the plan SUMMARY instead.

Network note for the SUMMARY: this `pip install` is the only network access in the entire phase, and it is installer-time, not pipeline-time. It does not conflict with ROADMAP criterion 1's "no network-fetch code path exists anywhere in the repo" — that constraint governs the `dont_email_everyone` package, which is enforced by `tests/test_no_network.py` in plan 01-02.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import importlib.metadata as m; pins={'pandas':'3.0.5','numpy':'2.4.6','pandera':'0.32.1','duckdb':'1.5.5','pyarrow':'25.0.1','scipy':'1.17.1','scikit-learn':'1.9.0','statsmodels':'0.15.0','matplotlib':'3.11.1','pytest':'9.1.1'}; bad={k:(v,m.version(k)) for k,v in pins.items() if m.version(k)!=v}; assert not bad, bad; import pandera.pandas, pyarrow, duckdb; import sys; assert sys.version_info[:2]==(3,11), sys.version; print('env ok', sys.version.split()[0])"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe --version` reports a `3.11.x` version
    - The verify command exits 0, proving all ten installed versions equal their pins exactly
    - `.venv/Scripts/python.exe -c "import pandera.pandas as pa; print(pa.DataFrameSchema)"` prints a class and exits 0
    - `.venv/Scripts/python.exe -c "import pyarrow; print(pyarrow.__version__)"` prints `25.0.1`
    - `.venv/Scripts/python.exe -m pytest --version` prints `pytest 9.1.1`
    - `git status --porcelain .venv` produces no output (the venv is ignored, not staged)
    - The install log contains no `Building wheel for` line (all wheels were prebuilt)
  </acceptance_criteria>
  <done>`.venv` exists on Python 3.11 with all ten pinned packages at their exact versions; `pandera.pandas` and `pyarrow` import cleanly; nothing from `.venv/` appears in git status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> local venv | Third-party code crosses into the execution environment at install time. This is the only network boundary in the phase. |
| git object store -> future clone | Bytes written here are what a Linux clone (including Streamlit Community Cloud in Phase 6) will receive; the local working tree is not the artifact. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | git line-ending normalization of `data/raw/*.csv` | mitigate | `.gitattributes` `data/raw/*.csv -text -diff` committed in an isolated commit **before** any CSV is staged (Task 1). Verified defect on this machine: `core.autocrlf=true` is set globally and the CSV is 64,001 CRLF / 0 bare LF. Verified by `git check-attr` returning `text: unset`. |
| T-01-SC | Tampering | PyPI package installation | mitigate | Exact `==` pins for all ten packages; `--only-binary=:all:` refuses source distributions so no `setup.py` build code executes; RESEARCH.md §Package Legitimacy Audit ran `slopcheck 0.6.1` against the full set with 10/10 `[OK]`, no `[SUS]`, no `[SLOP]`. All ten are prescribed by CLAUDE.md's closed allowlist, so the hallucinated-package vector does not apply. **No blocking human checkpoint is required** because there are zero `[ASSUMED]` or `[SUS]` packages. |
| T-01-02 | Tampering | accidental commit of a `.duckdb` file | mitigate | `*.duckdb` and `*.duckdb.wal` in `.gitignore` (Task 1), enforcing D-09 structurally for Phases 2-7. |
| T-01-03 | Information Disclosure | secrets in the repo | accept | No secrets, no `.env`, no credentials exist or are needed in this phase (RESEARCH.md §Runtime State Inventory verified). Nothing to protect. |
| T-01-04 | Denial of Service | dependency resolver drift breaking a future clean install | mitigate | Exact `==` pins plus the recorded cp311 caps (numpy <2.5, scipy <1.18); Task 3 asserts resolved versions equal the pins rather than assuming pip complied. |

ASVS L1 categories in scope for this plan: **V14 Configuration** only (pinned, wheel-only dependency set; ignore rules). V5 Input Validation and V6 Cryptography are exercised in plan 01-02 and 01-03. No HIGH-severity threat is left unmitigated.
</threat_model>

<verification>
- `git check-attr text diff data/raw/hillstrom.csv` reports both attributes unset
- `git log --oneline` shows `.gitattributes` committed before any `data/` path appears in history
- `.venv/Scripts/python.exe -m pytest --version` reports 9.1.1
- All ten installed package versions equal their pins
</verification>

<success_criteria>
- ROADMAP criterion 2 precondition satisfied: git will store `data/raw/*.csv` byte-exact, so the SHA-256 recorded in plan 01-02 is meaningful across operating systems
- The STATE.md blocker "Stack research was skipped project-wide... needs a research pass to pin exact library versions, resolve Python 3.9 vs. current library floors" is fully closed in-repo: versions are pinned, installed, and asserted
- VALIDATION.md Wave 0 items `pyproject.toml` and "Framework install: pytest==9.1.1" are complete
- CONTEXT.md D-04, D-05, D-07 (via `pythonpath`), and D-09 (via `*.duckdb` ignore) are honored
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-01-SUMMARY.md` when done. Include the resolved `pip freeze` output and the exact `git check-attr` output as evidence.
</output>

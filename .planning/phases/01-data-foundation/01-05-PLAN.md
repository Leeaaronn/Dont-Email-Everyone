---
phase: 01-data-foundation
plan: 05
type: execute
wave: 5
depends_on: [01-04]
files_modified:
  - .planning/phases/01-data-foundation/01-VALIDATION.md
autonomous: false
requirements: [DATA-01, DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "A fresh clone of the repo, with no access to this working tree, reproduces every artifact and passes the full test suite"
    - "The clone's vendored CSV has the same SHA-256 as this machine's"
    - "The developer has seen the evidence and either accepted the local Linux-checkout simulation or run a real second-machine clone"
  artifacts:
    - path: ".planning/phases/01-data-foundation/01-VALIDATION.md"
      provides: "per-task verification map with every row marked green"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "fresh clone"
      to: "data/processed/*.parquet"
      via: "python -m dont_email_everyone.ingest reproducing artifacts from the vendored CSV alone"
      pattern: "ingest"
---

<objective>
Prove the phase goal on a clean checkout rather than on the machine that built it, then hand the developer the evidence for ROADMAP criterion 2's "fresh clone on a second machine" clause.

Purpose: every test written so far runs in a working tree that has been continuously present while the code was written. ROADMAP criterion 5 says "`pytest` passes on a clean checkout" and criterion 2 says "a fresh clone on a second machine produces the identical SHA-256". RESEARCH.md Open Question 3 records that no second machine is available, and recommends the local `git -c core.autocrlf=input clone` simulation as the automated stand-in — while noting the developer should decide whether that simulation counts as satisfying a criterion that literally says "second machine."

Output: a clean-clone verification run, a fully green VALIDATION.md, and a developer decision on the second-machine question.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-01-SUMMARY.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md
@.planning/phases/01-data-foundation/01-03-SUMMARY.md
@.planning/phases/01-data-foundation/01-04-SUMMARY.md

<interfaces>
<!-- Everything under test already exists. This plan writes no production code. -->

Full expected suite after plan 01-04 (42 tests):
  tests/test_config.py       3    allowlist exactness, post-treatment leakage, cwd independence
  tests/test_no_network.py   2    no network imports, no streamlit import
  tests/test_ingest.py       9    checksum verify/tamper/missing, vendored shape,
                                  load shape/dtypes/order, no .duckdb file, ConversionException
  tests/test_provenance.py   4    git blob digest, check-attr, blob size, Linux-style clone
  tests/test_schemas.py     10    real file validates, 6 parametrized corruptions,
                                  lazy-reports-all, no-coerce, cross-column checks
  tests/test_frames.py       9    mutual exclusivity, control counts x2, treated counts,
                                  no `T` column, treatment matches segment, no mutation, disjoint arms
  tests/test_artifacts.py    5    exist+tracked, shapes, dtype round-trip,
                                  readable without duckdb/pandera, committed control counts

Ground truth the clean clone must reproduce:
  data/raw/hillstrom.csv  3,964,977 bytes
  sha256  0e5893329d8b93cefecc571777672028290ab69865718020c78c7284f291aece
  analysis_table.parquet     (64000, 12)
  mens_vs_control.parquet    (42613, 13)   control n = 21,306
  womens_vs_control.parquet  (42693, 13)   control n = 21,306
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reproduce the phase from a clean Linux-style clone</name>
  <files>(no repo files modified — verification run in a temporary directory)</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Open Question 3 (the `git -c core.autocrlf=input clone` simulation and why it reproduces Linux checkout behavior), §Pitfall 2 (the `git cat-file` blob check)
    - .planning/phases/01-data-foundation/01-VALIDATION.md — the eleven-row Per-Task Verification Map that must end fully green
    - .planning/ROADMAP.md — Phase 1 success criteria 1 through 5, each of which this run must produce evidence for
    - .planning/phases/01-data-foundation/01-04-SUMMARY.md — the artifact shapes and gate output to compare against
  </read_first>
  <action>
Confirm the working tree is clean and fully committed first (`git status --porcelain` empty). An uncommitted file would make the clone diverge from the working tree in a way that has nothing to do with the defect being tested.

Clone the repository into a temporary directory outside the project with `git -c core.autocrlf=input clone --quiet <repo-root> <tmp>/clean-clone`. The `core.autocrlf=input` setting reproduces the checkout behavior of Linux and macOS — and therefore of Streamlit Community Cloud, which Phase 6 deploys to. This is the local stand-in for "a second machine" per RESEARCH.md Open Question 3.

In the clone, gather evidence for each of the five ROADMAP success criteria and record the literal output of each for the SUMMARY and the checkpoint in Task 2:

  Criterion 2 (byte stability): hash `<clone>/data/raw/hillstrom.csv` and confirm it equals `0e5893329d8b93cefecc571777672028290ab69865718020c78c7284f291aece`, and confirm the file size is 3,964,977 bytes (not 3,900,976, which would indicate 64,001 stripped `\r` characters). Also confirm `git check-attr text data/raw/hillstrom.csv` inside the clone reports `text: unset`.

  Criterion 5 (clean checkout): create a fresh venv for the clone (`py -3.11 -m venv .venv` inside the clone), install `requirements-dev.txt` with `--only-binary=:all:`, and run the full suite with `python -m pytest -q --strict-markers`. Record the pass count. Expect 42 passed, 0 failed. Note that this install is the second and last network access of the phase and is installer-time, not pipeline-time.

  Criterion 1 (tamper abort, no network path): inside the clone, append one plausible-looking data row to `data/raw/hillstrom.csv` and run `python -m dont_email_everyone.ingest`. Confirm it aborts with `ChecksumMismatchError` and does **not** attempt to fetch a replacement. Then `git checkout -- data/raw/hillstrom.csv` to restore it. Also run a repo-wide grep for network-capable tokens across the whole clone's Python sources (not just the package) and confirm zero hits outside `tests/test_no_network.py`, which contains the token list by design — filter comment lines out of the count so the guard file's own pattern string is not miscounted as a violation.

  Criteria 2 and 3 (pipeline reproduces artifacts): delete `data/processed/*.parquet` in the clone, re-run `python -m dont_email_everyone.ingest`, and confirm all three artifacts are regenerated with shapes (64000, 12), (42613, 13), (42693, 13). Record the gate-by-gate progress output.

  Criterion 4 (frames not pooled): in the clone, read the two regenerated frame artifacts and confirm each has exactly 21,306 rows with `treatment == 0` and exactly two distinct `segment` values.

Delete the temporary clone when done. Make no commits inside it and copy nothing back from it — its only product is evidence.

If any check fails, do not patch the clone. Fix the source repo, commit the fix, and re-run this task from a fresh clone; a fix applied only in a throwaway clone is not a fix.

Then update `.planning/phases/01-data-foundation/01-VALIDATION.md`: mark every row of the Per-Task Verification Map green with its automated command confirmed, tick all Wave 0 Requirements checkboxes, tick every Validation Sign-Off item, set `nyquist_compliant: true` and `wave_0_complete: true` in the frontmatter, and set `status` to `verified`. Commit that update, e.g. `docs(01-05): mark phase 1 validation map green after clean-clone verification`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q --strict-markers</automated>
  </verify>
  <acceptance_criteria>
    - `git status --porcelain` is empty before the clone is taken
    - The clean clone's `data/raw/hillstrom.csv` hashes to `0e5893329d8b93cefecc571777672028290ab69865718020c78c7284f291aece` and is 3,964,977 bytes
    - `git check-attr text data/raw/hillstrom.csv` inside the clone reports `text: unset`
    - `python -m pytest -q --strict-markers` inside the clone reports 42 passed, 0 failed
    - `python -m dont_email_everyone.ingest` inside the clone, against a tampered CSV, exits nonzero with `ChecksumMismatchError` in the traceback and makes no network request
    - After deleting `data/processed/*.parquet` in the clone, `python -m dont_email_everyone.ingest` regenerates all three files with shapes (64000, 12), (42613, 13), (42693, 13)
    - Both regenerated frame artifacts in the clone have exactly 21,306 rows with `treatment == 0` and exactly 2 distinct `segment` values
    - `grep -rv '^#' --include=*.py <clone> | grep -cE '\b(requests|urllib|httpx|aiohttp|urlretrieve|socket)\b'` returns 0 outside `tests/test_no_network.py`
    - `01-VALIDATION.md` frontmatter has `nyquist_compliant: true` and `wave_0_complete: true`, and every row of the Per-Task Verification Map is marked green
  </acceptance_criteria>
  <done>A clean Linux-style clone reproduces the identical CSV digest, regenerates all three artifacts from the vendored data alone, aborts on a tampered file, and passes all 42 tests; VALIDATION.md is fully green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the phase goal and decide the second-machine question</name>
  <files>(none — verification checkpoint, no files modified)</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-05-SUMMARY.md — the clean-clone evidence produced by Task 1
    - .planning/ROADMAP.md — Phase 1 success criteria 1 through 5, the exact claims being confirmed
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Open Question 3 (why the local simulation is offered in place of a second machine), §Open Question 1 (the pyarrow allowlist reading)
    - README.md — the "Data foundation decisions" C1-C6 table the developer is asked to confirm
  </read_first>
  <action>
Present the clean-clone evidence from Task 1 to the developer and block until they respond. Do not auto-approve and do not proceed to phase verification without an explicit reply — `workflow.auto_advance` is false and this checkpoint carries a real decision, not a rubber stamp.

Two things are being asked. First, confirmation that the phase goal holds: the developer spot-checks the `git cat-file` digest and the test suite themselves rather than taking the SUMMARY's word for it. Second, a decision on ROADMAP criterion 2's literal "second machine" wording, which was satisfied by a `git -c core.autocrlf=input clone` simulation because no second machine was available (RESEARCH.md §Environment Availability). Record whichever answer is given — `accept-simulation` or `real-clone` — in the SUMMARY so the choice is auditable rather than implicit.

If the developer answers `real-clone`, provide the commands for the path they prefer: push to a remote and clone from a second machine, or `wsl --exec git clone` into a Linux filesystem, or a container clone. Then re-run the digest comparison there and report before closing the phase.

Also surface the C6 decision (pyarrow admitted as an I/O engine despite not appearing in CLAUDE.md's allowlist) for explicit confirmation, since it is the one recorded decision that reads against a stated project constraint. If the developer reads the allowlist strictly, note that the only alternative Parquet engine is `fastparquet`, which is equally absent from the list and less standard, and escalate rather than silently substituting.
  </action>
  <what-built>
The complete Phase 1 data foundation, verified from a clean clone rather than from the working tree that produced it:

- `.gitattributes` marking `data/raw/*.csv` as `-text`, committed before the CSV was ever staged, so the committed blob is byte-identical to the working tree on every OS
- `data/raw/hillstrom.csv` (3,964,977 bytes) and `data/raw/CHECKSUMS.sha256`, verified on every pipeline run by a raising gate with no fallback and no network code path anywhere in the package
- A DuckDB explicitly-typed load producing exactly 64,000 x 12, and a Pandera `RawHillstrom` schema with `coerce=False` proven to reject six distinct corruptions and to report three simultaneous violations in a single raise
- Two mutually exclusive analysis frames with a control group of exactly 21,306 rows each — not the 42,693 the pooled-control bug produces
- Three committed Parquet artifacts readable with pandas alone, and 42 passing tests
  </what-built>
  <how-to-verify>
Everything below is already automated and green. This checkpoint exists because ROADMAP criterion 2 says the words "a second machine", and no second machine was available (RESEARCH.md §Environment Availability). Your call on whether the local simulation settles it.

1. Read `.planning/phases/01-data-foundation/01-05-SUMMARY.md` — it contains the literal output from the clean-clone run: the CSV digest, the byte count, the `git check-attr` result, the pytest pass count, the tamper-abort traceback, and the regenerated artifact shapes.

2. Spot-check the highest-value claim yourself, in this repo:
   `git cat-file -p HEAD:data/raw/hillstrom.csv | sha256sum`
   Expect `0e5893329d8b93cefecc571777672028290ab69865718020c78c7284f291aece`. This hashes the bytes git actually stored, which is what a cloner receives — not the file already sitting on your disk.

3. Run the suite: `.venv\Scripts\python.exe -m pytest -q`. Expect 42 passed.

4. Regenerate from scratch: delete `data/processed/*.parquet`, run `python -m dont_email_everyone.ingest`, and confirm `git status` shows the three files restored with equivalent content.

5. **The decision.** ROADMAP criterion 2 reads "A fresh clone on a second machine produces the identical SHA-256." What was done instead is a `git -c core.autocrlf=input clone` — which reproduces the exact Linux/macOS checkout behavior that causes the CRLF defect, on this machine. RESEARCH.md verified this simulation catches the real defect. Choose one:
   - **accept-simulation** — the local Linux-checkout simulation satisfies criterion 2. Phase 1 closes now. (Note: Phase 6 deploys to Streamlit Community Cloud, which is a real Linux clone, so the claim gets independently tested there anyway.)
   - **real-clone** — you want to clone this repo onto a second machine (or a WSL/Linux container, or push to a remote and clone from there) and confirm the digest yourself before Phase 1 closes. Say so and the exact commands will be provided.

6. Optional, and worth a moment: confirm the six recorded decisions in the README's "Data foundation decisions" table (C1-C6) match your intent — especially **C6**, where `pyarrow` was admitted as an I/O engine despite not appearing in CLAUDE.md's library allowlist. The reasoning: the allowlist's stated rationale is about demonstrating modeling technique from first principles, `pandas.to_parquet` raises `ImportError` without pyarrow, and D-09 mandates Parquet as the committed artifact format. If you read the allowlist more strictly, say so — the only alternative engine is `fastparquet`, which is equally absent from the list and less standard.
  </how-to-verify>
  <resume-signal>Reply "approved" (or "accept-simulation"), or "real-clone" to run the second-machine check, or describe any issue found</resume-signal>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q --strict-markers</automated>
    <human-check>Developer has replied with accept-simulation, real-clone, or a described issue</human-check>
  </verify>
  <acceptance_criteria>
    - The developer has replied; execution did not proceed on silence or on an assumed approval
    - The reply is recorded verbatim in `01-05-SUMMARY.md` as either `accept-simulation` or `real-clone`, alongside the C6 confirmation
    - If `real-clone` was chosen, a genuine second-machine (or WSL/container) clone was taken and its `sha256` of `data/raw/hillstrom.csv` is recorded and equals `0e5893329d8b93cefecc571777672028290ab69865718020c78c7284f291aece`
    - If any issue was described instead of approval, it is captured as a gap for `/gsd:plan-phase --gaps` rather than closed silently
    - `.venv/Scripts/python.exe -m pytest -q --strict-markers` reports 42 passed, 0 failed at the moment of approval
  </acceptance_criteria>
  <done>The developer has explicitly confirmed the phase goal and recorded a decision on the second-machine question and on C6; any raised issue is captured as a gap rather than absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author's working tree -> fresh clone | Everything verified so far was verified on the machine that produced it. This plan crosses into what an independent party actually receives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-23 | Tampering | a defect visible only outside the author's working tree (the CRLF normalization class of bug) | mitigate | Full reproduction from a `git -c core.autocrlf=input clone`: fresh venv, fresh install, full suite, artifact regeneration, and digest comparison — all against bytes checked out under Linux/macOS conversion rules. |
| T-01-24 | Repudiation | "it works on my machine" as the sole evidence for the phase goal | mitigate | Literal command output from the clean clone is recorded in the SUMMARY, and the developer independently spot-checks the `git cat-file` digest at the checkpoint. |
| T-01-25 | Tampering | a fix applied inside the throwaway clone and never propagated to the repo | mitigate | The task explicitly forbids committing in or copying from the clone; any failure is fixed in the source repo and re-verified from a fresh clone. |
| T-01-SC2 | Tampering | supply-chain substitution on the clone's fresh `pip install` | mitigate | The clone installs from the same exact `==` pins with `--only-binary=:all:`; a resolver returning anything other than the pinned versions would surface as an install or test failure. All ten packages were slopcheck-clean (10/10 `[OK]`) in RESEARCH.md's Package Legitimacy Audit, so no blocking legitimacy checkpoint is required. |

ASVS L1 categories in scope for this plan: **V6 Cryptography** (digest comparison across checkout boundaries) and **V14 Configuration** (reproducible pinned install on a clean environment). No HIGH-severity threat is left unmitigated.
</threat_model>

<verification>
- Clean clone: 42 tests passed, 0 failed
- Clean clone CSV digest equals the recorded checksum, at 3,964,977 bytes
- Clean clone regenerates all three Parquet artifacts with the verified shapes and control counts
- Tampered CSV in the clone aborts the pipeline with `ChecksumMismatchError` and no fetch attempt
- `01-VALIDATION.md` is fully green with `nyquist_compliant: true`
- Developer has responded to the second-machine decision
</verification>

<success_criteria>
- ROADMAP Phase 1 criterion 1 demonstrated on a clean clone: tampered CSV aborts with a checksum mismatch, and no network-fetch code path exists anywhere in the repo
- Criterion 2 demonstrated: a fresh clone under Linux/macOS line-ending rules produces the identical SHA-256, and ingest loads 64,000 x 12 into DuckDB — with the developer's explicit decision recorded on whether the local simulation satisfies "second machine"
- Criterion 3 demonstrated: the schema rejects the corrupted fixtures reporting all violations at once, and passes on the real file
- Criterion 4 demonstrated: two mutually exclusive frames, each with exactly two `segment` values and a control group of 21,306 rows
- Criterion 5 demonstrated: `pytest` passes on a clean checkout, and the allowlist leakage test is present and green
- DATA-01, DATA-02, DATA-03, DATA-04 all satisfied and evidenced
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-05-SUMMARY.md` when done. Include the literal clean-clone command output for each of the five criteria, and record the developer's decision on the second-machine question (accept-simulation or real-clone) so it is auditable rather than implicit.
</output>

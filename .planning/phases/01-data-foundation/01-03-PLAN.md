---
phase: 01-data-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - dont_email_everyone/ingest.py
  - dont_email_everyone/schemas.py
  - tests/conftest.py
  - tests/test_ingest.py
  - tests/test_schemas.py
autonomous: true
requirements: [DATA-02, DATA-03, DATA-04]
must_haves:
  truths:
    - "The pipeline loads the vendored CSV into DuckDB and produces a 64,000 x 12 pandas DataFrame"
    - "DuckDB is used in-process only; no .duckdb file is written anywhere"
    - "The Pandera schema passes on the real file"
    - "The Pandera schema rejects a wrong dtype, an out-of-range recency, an unexpected segment value, an injected null, an extra column, and reordered columns"
    - "A fixture with three simultaneous violations reports all of them in a single raise, not just the first"
    - "The schema repairs nothing — a recency of 10.5 is rejected, not silently truncated to 10"
  artifacts:
    - path: "dont_email_everyone/ingest.py"
      provides: "checksum gate plus the DuckDB explicitly-typed loader"
      exports: ["RAW_COLUMNS", "load_raw", "verify_checksum", "ChecksumMismatchError"]
    - path: "dont_email_everyone/schemas.py"
      provides: "RawHillstrom DataFrameSchema with strict/ordered/coerce=False"
      exports: ["RawHillstrom", "HISTORY_SEGMENTS", "ZIP_CODES", "CHANNELS", "SEGMENTS"]
      contains: "coerce=False"
    - path: "tests/conftest.py"
      provides: "session-scoped real-data fixture and the corruption factory"
    - path: "tests/test_schemas.py"
      provides: "positive validation plus the six-corruption negative battery"
  key_links:
    - from: "dont_email_everyone/ingest.py"
      to: "duckdb read_csv"
      via: "explicit columns= type map, never read_csv_auto"
      pattern: "RAW_COLUMNS"
    - from: "dont_email_everyone/schemas.py"
      to: "pandera"
      via: "import pandera.pandas as pa"
      pattern: "import pandera\\.pandas as pa"
    - from: "tests/conftest.py"
      to: "dont_email_everyone.ingest.load_raw"
      via: "session-scoped raw_df fixture"
      pattern: "load_raw"
---

## Objective — CONTEXT.md decisions implemented: D-09, D-10

<objective>
Build the two remaining validation gates: DuckDB's explicitly-typed CSV load (Gate 2 — types) and the Pandera `RawHillstrom` schema (Gate 3 — values and structure), and prove the schema is capable of failing by driving six distinct corruptions through it.

Purpose: DATA-02's load half and all of DATA-03, plus ROADMAP criterion 3. RESEARCH.md's central finding is that this schema's value is not that it passes — it is that it is *proven capable of rejecting*, which requires `coerce=False`. With `coerce=True` (what PITFALLS.md Pitfall 16 recommends) a `recency` of `10.5` is silently truncated to `10` and validation reports success. That single flag is the difference between satisfying criterion 3 and defeating it.

Output: `load_raw` in `ingest.py`, `schemas.py`, the shared test fixtures, and the negative battery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-PATTERNS.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Contracts already established by plan 01-02 (do not redefine): -->
  dont_email_everyone/config.py: ROOT, RAW_CSV, CHECKSUM_FILE, PROCESSED,
                                 CONTROL, ARMS, PRE_TREATMENT_FEATURES
  dont_email_everyone/ingest.py: ChecksumMismatchError, sha256_file,
                                 read_expected, verify_checksum

<!-- Contracts this plan adds, consumed by plan 01-04: -->
  dont_email_everyone/ingest.py:
    RAW_COLUMNS: dict[str, str]          # column name -> DuckDB type
    def load_raw(csv_path=config.RAW_CSV) -> pandas.DataFrame   # (64000, 12)
  dont_email_everyone/schemas.py:
    RawHillstrom: pandera.pandas.DataFrameSchema
    HISTORY_SEGMENTS, ZIP_CODES, CHANNELS, SEGMENTS: list[str]

Verified DuckDB type map (01-RESEARCH.md §Code Example 2, executed against the real file):
  recency BIGINT | history_segment VARCHAR | history DOUBLE | mens BIGINT |
  womens BIGINT  | zip_code VARCHAR        | newbie BIGINT  | channel VARCHAR |
  segment VARCHAR| visit BIGINT            | conversion BIGINT | spend DOUBLE

Verified DuckDB -> pandas 3.0.5 dtype mapping (01-RESEARCH.md §Pattern 3):
  BIGINT -> int64 | INTEGER -> int32 | TINYINT -> int8 | DOUBLE -> float64
  VARCHAR -> str   <-- NOT object (pandas 3.0 PDEP-14). `pa.Column(object)` FAILS.

Verified literal category values, read out of the real file (do NOT "correct" them):
  HISTORY_SEGMENTS = ["1) $0 - $100", "2) $100 - $200", "3) $200 - $350",
                      "4) $350 - $500", "5) $500 - $750", "6) $750 - $1,000",
                      "7) $1,000 +"]                # note the comma inside "$1,000"
  ZIP_CODES = ["Rural", "Surburban", "Urban"]       # "Surburban" is the file's literal misspelling
  CHANNELS  = ["Multichannel", "Phone", "Web"]
  SEGMENTS  = ["Mens E-Mail", "No E-Mail", "Womens E-Mail"]

Verified numeric ranges: recency in [1, 12]; history in [29.99, 3345.93];
  spend in [0.0, 499.0]; mens/womens/newbie/visit/conversion in {0, 1}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DuckDB explicitly-typed load and the shared test fixtures</name>
  <files>dont_email_everyone/ingest.py, tests/conftest.py, tests/test_ingest.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Code Example 2 (the verified loader, including the note on the unresolved parameter-binding form), §Pitfall 3 (why `read_csv_auto` is banned even though it happens to work), §Architecture Patterns / Pattern 3 (reader and schema agree by construction; the type-mapping table)
    - .planning/phases/01-data-foundation/01-PATTERNS.md — the `dont_email_everyone/ingest.py` section, especially "Open item the planner must close" on the `columns=` binding form
    - .planning/phases/01-data-foundation/01-CONTEXT.md — D-09, D-10 (DuckDB is in-process only; nothing downstream may require DuckDB)
    - dont_email_everyone/ingest.py (current state from plan 01-02) — extend it, do not rewrite the checksum gate
    - dont_email_everyone/config.py — reuse `RAW_CSV`
  </read_first>
  <behavior>
    - tests/test_ingest.py::test_load_shape — `load_raw()` returns a DataFrame of shape exactly (64000, 12)
    - tests/test_ingest.py::test_load_dtypes — the returned dtypes are `int64` for recency/mens/womens/newbie/visit/conversion, `float64` for history/spend, and `str` for history_segment/zip_code/channel/segment (NOT `object`)
    - tests/test_ingest.py::test_load_column_order — `list(df.columns)` equals the documented 12-column order exactly
    - tests/test_ingest.py::test_load_writes_no_duckdb_file — after `load_raw()`, no `*.duckdb` file exists under the repo root
    - tests/test_ingest.py::test_load_rejects_malformed_value — a tiny CSV in tmp_path with `abc` in the `recency` column raises `duckdb.ConversionException`
  </behavior>
  <action>
Extend `dont_email_everyone/ingest.py` (do not rewrite plan 01-02's checksum gate; append to it). Add `import duckdb` in the third-party import block, between the stdlib block and the first-party `from dont_email_everyone import config`.

Define `RAW_COLUMNS` as a module-level dict mapping each of the twelve column names to its DuckDB type, in the file's column order: `recency` BIGINT, `history_segment` VARCHAR, `history` DOUBLE, `mens` BIGINT, `womens` BIGINT, `zip_code` VARCHAR, `newbie` BIGINT, `channel` VARCHAR, `segment` VARCHAR, `visit` BIGINT, `conversion` BIGINT, `spend` DOUBLE. Use `BIGINT`/`DOUBLE`/`VARCHAR` deliberately rather than narrower types like `TINYINT` for the 0/1 columns: BIGINT/DOUBLE/VARCHAR make DuckDB's `.df()` output dtype-identical to what `pandas.read_csv` produces, which keeps hand-built test fixtures interchangeable with production data against a single schema. `TINYINT` would force the schema to declare `int8` and split those two paths for no benefit at 64k rows.

Define `load_raw(csv_path=config.RAW_CSV)` returning a pandas DataFrame. Open an in-memory connection with `duckdb.connect()` used as a context manager — passing no path means in-memory, so nothing is persisted (CONTEXT.md D-09). Issue exactly one `SELECT * FROM read_csv(...)` statement with `header=true` and an explicit `columns=` map built from `RAW_COLUMNS`, then call `.df()`. Never use `read_csv_auto` (PITFALLS.md Pitfall 17 / RESEARCH.md §Pitfall 3): auto-inference happens to be correct on this exact file today, but inference is data-dependent, and any future filtered read or subsetted fixture can shift a type and break the schema written against the full file. The explicit map makes the reader/schema contract independent of the data's contents.

**Resolve the one open research item here.** RESEARCH.md §Code Example 2 presents a parameterized form (`read_csv(?, header=true, columns=$cols)` with `params=[...]`) that was written but **not executed**; the form that was actually verified working is f-string interpolation of the dict literal: `f"SELECT * FROM read_csv('{path}', header=true, columns={RAW_COLUMNS!r})"`. Try the parameter-bound form first (it is the better habit) and confirm it executes and returns (64000, 12); if DuckDB rejects binding for `columns=`, fall back to the verified f-string form. Either is safe here because both the path and the column map are internal constants that never receive user input — record which form you landed on and why in the module docstring and in the plan SUMMARY, so Phase 2+ does not re-litigate it. If you fall back, use `str(path)` interpolation only for the path and keep the struct literal for `columns`.

Add `load_raw` to the module in a way that does *not* call `verify_checksum` internally — keep the four gates one-reason-per-gate (RESEARCH.md §Shared Patterns "Validation: two independent layers"). Composition of gate 1 + gate 2 + gate 3 + gate 4 happens in the pipeline entrypoint built in plan 01-04, not inside `load_raw`.

Create `tests/conftest.py` with shared fixtures and no `sys.path` manipulation of any kind (`pythonpath = ["."]` in `pyproject.toml` already handles imports):
  - a **session-scoped** `raw_df` fixture that calls `load_raw()` once and returns the real 64,000-row DataFrame. Session scope matters: the corruption tests in Task 2 each need a fresh *copy* of this frame, and reloading 64k rows per test would multiply runtime for no benefit. Fixtures that mutate must call `.copy()`.
  - a `corrupt` factory fixture returning a callable `corrupt(df, kind)` that produces one of six named corruptions from a copy of the input frame, leaving the original untouched: `"bad_dtype"` (cast `history` to `str`), `"out_of_range"` (set `recency` at row 0 to 99), `"unexpected_category"` (set `segment` at row 0 to `"Nobody"`), `"injected_null"` (set `spend` at row 0 to `None`/NaN), `"extra_column"` (add a column named `leaked`), `"reordered"` (swap two column positions).
  - a `multi_corrupt` fixture applying `"out_of_range"`, `"unexpected_category"`, and `"injected_null"` to rows 0, 1 and 2 simultaneously, for the lazy-reporting test in Task 2.

Extend `tests/test_ingest.py` with the five behaviors above. `test_load_writes_no_duckdb_file` should glob `config.ROOT` recursively for `*.duckdb` after calling `load_raw()` and assert the result is empty — enforcing D-09 as a test, not a promise. `test_load_rejects_malformed_value` writes a two-line CSV into `tmp_path` with a full 12-column header and one row whose `recency` is `abc`, then asserts `duckdb.ConversionException` is raised (the verified message names the line number, the column, and the offending value — this is DuckDB acting as a second, independent type guard *before* Pandera ever runs).

Commit as its own commit, e.g. `feat(01-03): load vendored csv through duckdb with explicit column types`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ingest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_ingest.py -q` reports 9 passed, 0 failed (4 from plan 01-02 plus 5 new)
    - `.venv/Scripts/python.exe -c "from dont_email_everyone.ingest import load_raw; d=load_raw(); assert d.shape==(64000,12), d.shape; print(dict(d.dtypes.astype(str)))"` exits 0 and prints `str` (not `object`) for history_segment, zip_code, channel, segment
    - `grep -c 'read_csv_auto' dont_email_everyone/ingest.py` returns 0
    - `grep -c 'RAW_COLUMNS' dont_email_everyone/ingest.py` returns at least 2 (definition plus use in the query)
    - `.venv/Scripts/python.exe -c "from dont_email_everyone import config,ingest; ingest.load_raw(); import pathlib; f=list(config.ROOT.rglob('*.duckdb')); assert not f, f; print('no duckdb file')"` exits 0
    - `grep -c 'verify_checksum' dont_email_everyone/ingest.py` shows `verify_checksum` is defined but is NOT called from inside `load_raw` (gates stay separable)
    - `tests/conftest.py` contains no `sys.path` reference: `grep -c 'sys.path' tests/conftest.py` returns 0
    - The module docstring or a comment in `ingest.py` records which `columns=` binding form was used and why
  </acceptance_criteria>
  <done>`load_raw()` returns exactly (64000, 12) with the verified dtypes via an explicit DuckDB `columns=` map, writes no `.duckdb` file, raises `ConversionException` on malformed input, and the shared fixtures exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The RawHillstrom Pandera schema and its six-corruption negative battery</name>
  <files>dont_email_everyone/schemas.py, tests/test_schemas.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Code Example 3 (the full verified schema), §Pitfall 4 (the `coerce=True` truth table — the `10.5` case), §Pitfall 5 (the pandas 3.0 `str` vs `object` declaration matrix), §Code Example 5 corruption table (all six verified failure-case names, plus the 79-failure-case multi-violation result), §State of the Art (`import pandera.pandas as pa`)
    - .planning/phases/01-data-foundation/01-PATTERNS.md — the `dont_email_everyone/schemas.py` section and its "Four hard constraints the planner must not let drift", plus conflicts C1, C2, C3
    - tests/conftest.py (from Task 1) — the `raw_df`, `corrupt`, and `multi_corrupt` fixtures to consume
    - dont_email_everyone/ingest.py (from Task 1) — the `RAW_COLUMNS` type map the schema must agree with
    - .planning/phases/01-data-foundation/01-VALIDATION.md — the three DATA-03 rows this task satisfies
  </read_first>
  <behavior>
    - tests/test_schemas.py::test_real_file_validates — `RawHillstrom.validate(raw_df, lazy=True)` returns a (64000, 12) frame and does not raise
    - tests/test_schemas.py::test_corruption_rejected[bad_dtype] — raises `SchemaErrors`, failure cases include a `dtype('float64')` check
    - tests/test_schemas.py::test_corruption_rejected[out_of_range] — raises `SchemaErrors`, failure cases include `in_range(1, 12)`
    - tests/test_schemas.py::test_corruption_rejected[unexpected_category] — raises `SchemaErrors`, failure cases include an `isin` check on `segment`
    - tests/test_schemas.py::test_corruption_rejected[injected_null] — raises `SchemaErrors`, failure cases include `not_nullable`
    - tests/test_schemas.py::test_corruption_rejected[extra_column] — raises `SchemaErrors`, failure cases include `column_in_schema`
    - tests/test_schemas.py::test_corruption_rejected[reordered] — raises `SchemaErrors`, failure cases include `column_ordered`
    - tests/test_schemas.py::test_lazy_reports_all — the `multi_corrupt` frame raises once, and the raised `SchemaErrors.failure_cases` covers all three distinct violated checks (not just the first)
    - tests/test_schemas.py::test_schema_does_not_coerce — `RawHillstrom.coerce is False`, and a frame whose `recency` is float `10.5` is REJECTED rather than silently truncated to `10`
    - tests/test_schemas.py::test_cross_column_checks — a frame with `spend > 0` but `conversion == 0` is rejected by the `spend_positive_iff_conversion` check
  </behavior>
  <action>
Create `dont_email_everyone/schemas.py`. Import with `import pandera.pandas as pa` — **not** `import pandera as pa`. The top-level path still functions in 0.32.1 but emits a `FutureWarning` announcing removal; the submodule path has been the correct one since pandera 0.24.0.

Define the four category constants `HISTORY_SEGMENTS`, `ZIP_CODES`, `CHANNELS`, `SEGMENTS` with the exact literal values listed in the `<interfaces>` block. These were read directly out of the real file. Do not "correct" the misspelling `"Surburban"` and do not strip the comma from `"6) $750 - $1,000"` — the schema must match the data, not a tidied-up idea of it. Add a comment on `ZIP_CODES` noting the misspelling is the file's, so a future contributor does not helpfully fix it and break validation.

Define `RawHillstrom` as a `pa.DataFrameSchema` (object style). Note PATTERNS.md conflict C3: `ARCHITECTURE.md` says `DataFrameModel` (class style), but `DataFrameSchema` is the form RESEARCH.md actually executed against the real file. Use `DataFrameSchema` and record the reconciliation in a one-line module comment so Phase 2+ stays consistent.

Columns, with dtype and checks:
  `recency` int64 with `in_range(1, 12)`; `history_segment` `str` with `isin(HISTORY_SEGMENTS)`; `history` float64 with `in_range(29.99, 3345.93)`; `mens`/`womens`/`newbie`/`visit`/`conversion` int64 with `isin([0, 1])`; `zip_code` `str` with `isin(ZIP_CODES)`; `channel` `str` with `isin(CHANNELS)`; `segment` `str` with `isin(SEGMENTS)`; `spend` float64 with `in_range(0.0, 499.0)`. Declare all four string columns as the Python builtin `str`, **never** `object` — this is PATTERNS.md conflict C2: pandas 3.0 makes `str` the default string dtype (PDEP-14) and `pa.Column(object)` fails outright with `WRONG_DATATYPE`. PITFALLS.md Pitfall 17's `object` guidance was correct for pandas 2.x and is stale here.

Add three dataframe-level cross-column checks, each with an explicit `name=` so the failure case is self-describing: `spend_positive_iff_conversion` (`(spend > 0) == (conversion == 1)` holds for every row), `conversion_implies_visit` (`conversion <= visit`), and `mens_or_womens` (`(mens == 1) | (womens == 1)`). These encode the experiment's causal/structural assumptions and are the most valuable part of the schema — a column-wise check can only catch a bad value, but these catch a bad *relationship*, which is how a grouping or join bug announces itself.

Set the schema options: `strict=True` (extra columns rejected), `ordered=True` (reordered columns rejected), `coerce=False`, `unique_column_names=True`, `name="RawHillstrom"`.

`coerce=False` is the single most important flag in this phase and must not drift, which is PATTERNS.md conflict C1. PITFALLS.md Pitfall 16 recommends `coerce=True`; RESEARCH.md §Pitfall 4 verified that with `coerce=True` a `history` column corrupted to strings validates clean, a `recency` of `10.0` coerces to `10` and passes, and — the dangerous one — a `recency` of `10.5` is **silently truncated to `10` and reported as success**. `coerce` turns a validator into a repair tool. This schema's job is the opposite: assert that a known, vendored, checksummed file is exactly what was recorded. Put a comment on the `coerce=False` line saying exactly this, because it is the line a future contributor is most likely to "fix".

Add a module docstring stating the validation call convention: always `RawHillstrom.validate(df, lazy=True)`. `lazy=True` reports *every* violation in one raise with row indices and failure cases; without it, validation stops at the first problem, turning a data fix into N debugging cycles instead of one.

Create `tests/test_schemas.py` implementing the ten behaviors above, consuming the `raw_df`, `corrupt`, and `multi_corrupt` fixtures from `tests/conftest.py`. Parametrize `test_corruption_rejected` over the six corruption kinds with `pytest.mark.parametrize` so each shows up as its own test id, and assert not merely that `SchemaErrors` was raised but that the expected check name appears in `err.value.failure_cases["check"]` — a test that only asserts "it raised" can pass for the wrong reason.

For `test_lazy_reports_all`, assert on the *content* of the failure cases: that all three distinct violated check names are present in one raise. Assert on the number of distinct violated checks (>= 3) rather than pinning the exact failure-case row count, so the test states the property rather than a data-dependent number; record the observed count in the SUMMARY (RESEARCH.md observed 79 rows across the three checks on the real file). This test is ROADMAP criterion 3's "reporting all violations at once".

For `test_schema_does_not_coerce`, assert `RawHillstrom.coerce is False` directly *and* drive the `10.5` case through validation and assert it is rejected. Asserting the flag alone would pass even if a later pandera version changed coercion semantics; driving the value proves the behavior.

Commit as its own commit, e.g. `feat(01-03): add strict non-coercing pandera schema with negative battery`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_schemas.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_schemas.py -q` reports 10 passed, 0 failed (including all six parametrized corruption ids)
    - `grep -c 'import pandera.pandas as pa' dont_email_everyone/schemas.py` returns 1, and `grep -cE '^import pandera as pa' dont_email_everyone/schemas.py` returns 0
    - `grep -c 'coerce=False' dont_email_everyone/schemas.py` returns 1, and `grep -c 'coerce=True' dont_email_everyone/schemas.py` returns 0
    - `grep -cE 'pa\.Column\((object|"object")' dont_email_everyone/schemas.py` returns 0
    - `grep -c 'Surburban' dont_email_everyone/schemas.py` returns 1 (the misspelling is preserved verbatim)
    - `.venv/Scripts/python.exe -c "from dont_email_everyone.schemas import RawHillstrom as S; assert S.coerce is False; assert S.strict is True; assert S.ordered is True; assert len(S.columns)==12; print('schema options ok')"` exits 0
    - `.venv/Scripts/python.exe -c "from dont_email_everyone.ingest import load_raw; from dont_email_everyone.schemas import RawHillstrom; v=RawHillstrom.validate(load_raw(), lazy=True); assert v.shape==(64000,12), v.shape; print('real file validates')"` exits 0
    - `pytest tests/test_schemas.py::test_corruption_rejected -q` reports exactly 6 tests
    - `.venv/Scripts/python.exe -m pytest -q` (whole suite) reports 28 passed, 0 failed (13 from plan 01-02, 15 from this plan)
  </acceptance_criteria>
  <done>`RawHillstrom` validates the real file to (64000, 12) with `coerce=False`, rejects all six corruptions with the expected named checks, reports three simultaneous violations in a single raise, and rejects a `recency` of 10.5 instead of truncating it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSV bytes -> typed table | DuckDB's `read_csv` converts untyped text into typed values. Malformed input crosses here. |
| typed table -> analysis | Pandera is the last gate before data is treated as trustworthy by every downstream phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Tampering | validator that repairs instead of rejecting | mitigate | `coerce=False` on `RawHillstrom`, asserted two ways by `test_schema_does_not_coerce`: the flag itself, and driving a `recency` of `10.5` through validation and requiring rejection. RESEARCH.md §Pitfall 4 verified `coerce=True` silently truncates it to `10` and reports success. |
| T-01-12 | Tampering | undetected schema drift (extra, missing, reordered, or duplicate columns) | mitigate | `strict=True`, `ordered=True`, `unique_column_names=True`, each proven firing by the `extra_column` and `reordered` corruption cases. |
| T-01-13 | Tampering | silent type coercion at the reader layer | mitigate | Explicit `columns=` map passed to DuckDB `read_csv` rather than `read_csv_auto`, making the reader/schema contract independent of data contents; `ConversionException` on malformed input is proven by `test_load_rejects_malformed_value`. Two independent type guards (DuckDB, then Pandera) rather than one. |
| T-01-14 | Tampering | SQL injection into the DuckDB query | mitigate | The query is built from internal constants only (`config.RAW_CSV`, `RAW_COLUMNS`); no user input reaches it. Parameter binding is preferred for the path where DuckDB supports it, with the verified literal form as the documented fallback. |
| T-01-15 | Denial of Service | loading a 4 MB CSV exhausting memory | accept | 64,000 x 12 is trivially small; DuckDB streams the read and the in-memory frame is a few MB. No mitigation warranted. |
| T-01-16 | Tampering | a `.duckdb` file persisted and later committed as an artifact | mitigate | `duckdb.connect()` with no path (in-memory) per D-09, plus `*.duckdb` in `.gitignore` (plan 01-01), plus `test_load_writes_no_duckdb_file` asserting no such file exists under the repo root after a load. |

ASVS L1 categories in scope for this plan: **V5 Input Validation** (two independent validation layers — DuckDB explicit `columns=` for types, Pandera `strict`/`ordered`/`coerce=False`/`unique_column_names` for values and structure, validated with `lazy=True` for complete failure reporting). No HIGH-severity threat is left unmitigated.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` reports 28 passed
- `load_raw()` returns (64000, 12) with `str` dtype (not `object`) for the four string columns
- `RawHillstrom.coerce is False` and the real file validates
- No `*.duckdb` file exists anywhere under the repo root after a load
- No `read_csv_auto` and no `import pandera as pa` anywhere in the package
</verification>

<success_criteria>
- DATA-02 fully satisfied: the pipeline loads the vendored CSV into DuckDB (64,000 x 12), verifying the checksum, with no re-download path
- DATA-03 satisfied: the Pandera schema validates types, ranges, nulls, and categories on ingest
- ROADMAP criterion 2's load half satisfied: ingest loads 64,000 x 12 rows into DuckDB
- ROADMAP criterion 3 satisfied: the schema rejects a deliberately corrupted fixture (wrong dtype, out-of-range `recency`, unexpected `segment`, injected null, extra column, reordered columns) reporting all violations at once, and passes on the real file — proven by a negative test, not by having never failed
- PATTERNS.md conflicts C1 (`coerce=False`), C2 (`str` not `object`), and C3 (`DataFrameSchema` not `DataFrameModel`) are resolved in code with the reconciliation recorded in comments
- RESEARCH.md's open item on the DuckDB `columns=` binding form is closed and documented
- VALIDATION.md rows for `test_load_shape`, `test_real_file_validates`, `test_corruption_rejected`, and `test_lazy_reports_all` are green
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-03-SUMMARY.md` when done. Record which DuckDB `columns=` binding form was used, the observed dtype map, and the observed failure-case count for the multi-violation test.
</output>

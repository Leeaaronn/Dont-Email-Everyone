---
phase: 01-data-foundation
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - dont_email_everyone/frames.py
  - dont_email_everyone/ingest.py
  - tests/test_frames.py
  - tests/test_artifacts.py
  - data/processed/analysis_table.parquet
  - data/processed/mens_vs_control.parquet
  - data/processed/womens_vs_control.parquet
  - README.md
autonomous: true
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "Two mutually exclusive analysis frames exist, each containing exactly two segment values"
    - "Each frame's control group has exactly 21,306 rows, not 42,693"
    - "The treatment column is named `treatment`, never `T`"
    - "One command reproduces every committed artifact from the vendored CSV with no network access"
    - "Phase 2+ can read the committed artifacts with pandas alone, without DuckDB or Pandera installed"
    - "A reader of the README can set up the environment and reproduce the data foundation"
  artifacts:
    - path: "dont_email_everyone/frames.py"
      provides: "positive-membership arm-vs-control frame constructor"
      exports: ["build_frame", "build_all_frames"]
      contains: "isin"
    - path: "data/processed/analysis_table.parquet"
      provides: "the validated 64,000 x 12 table"
    - path: "data/processed/mens_vs_control.parquet"
      provides: "42,613 x 13 mens-vs-control frame"
    - path: "data/processed/womens_vs_control.parquet"
      provides: "42,693 x 13 womens-vs-control frame"
    - path: "tests/test_frames.py"
      provides: "pooled-control regression guard"
    - path: "tests/test_artifacts.py"
      provides: "proof artifacts are readable without DuckDB or Pandera"
  key_links:
    - from: "dont_email_everyone/frames.py"
      to: "dont_email_everyone/config.py"
      via: "CONTROL and ARMS constants"
      pattern: "config\\.(CONTROL|ARMS)"
    - from: "dont_email_everyone/ingest.py"
      to: "data/processed/*.parquet"
      via: "build_all() pipeline entrypoint writing Parquet"
      pattern: "to_parquet"
    - from: "tests/test_artifacts.py"
      to: "data/processed/*.parquet"
      via: "pandas.read_parquet with no duckdb import"
      pattern: "read_parquet"
---

## Objective — CONTEXT.md decisions implemented: D-05, D-07, D-09, D-10

<objective>
Build the two mutually exclusive arm-vs-control analysis frames by positive membership, wire the four gates into a single reproducible pipeline entrypoint, commit the Parquet artifacts Phase 2+ consumes, and record this phase's setup and reconciliation decisions in the README.

Purpose: ROADMAP criterion 4 and the completion of DATA-03/DATA-04. This is where PITFALLS.md Pitfall 1 — the pooled-control bug, described by the roadmap as "the single structural decision that prevents the ~25-30% bias that sinks most Hillstrom writeups" — is made structurally impossible rather than merely avoided. Building frames by negation (`segment != "Mens E-Mail"`) pools the Womens arm into control; building by positive membership (`segment.isin([arm, CONTROL])`) cannot.

Output: `frames.py`, the `build_all()` entrypoint, three committed Parquet artifacts, two test modules, and a README setup section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@.planning/phases/01-data-foundation/01-PATTERNS.md
@.planning/phases/01-data-foundation/01-VALIDATION.md
@.planning/phases/01-data-foundation/01-03-SUMMARY.md

<interfaces>
<!-- Contracts established by plans 01-02 and 01-03 (do not redefine): -->
  dont_email_everyone/config.py: ROOT, RAW_CSV, CHECKSUM_FILE, PROCESSED,
                                 CONTROL="No E-Mail",
                                 ARMS={"mens":"Mens E-Mail","womens":"Womens E-Mail"},
                                 PRE_TREATMENT_FEATURES (7 hard-coded entries)
  dont_email_everyone/ingest.py: ChecksumMismatchError, sha256_file, read_expected,
                                 verify_checksum, RAW_COLUMNS, load_raw
  dont_email_everyone/schemas.py: RawHillstrom (strict/ordered/coerce=False),
                                 HISTORY_SEGMENTS, ZIP_CODES, CHANNELS, SEGMENTS

<!-- Contracts this plan adds, consumed by Phases 2-7: -->
  dont_email_everyone/frames.py:
    def build_frame(df, arm_label: str) -> pandas.DataFrame   # +1 column: treatment
    def build_all_frames(df) -> dict[str, pandas.DataFrame]   # keys: "mens", "womens"
  dont_email_everyone/ingest.py:
    def build_all() -> None    # gate1 -> gate2 -> gate3 -> gate4 -> Parquet
                               # invoked via `python -m dont_email_everyone.ingest`

Verified ground-truth counts (01-RESEARCH.md §Code Example 4, executed on the real file):

  full-file segment counts: Womens E-Mail 21,387 | Mens E-Mail 21,307 | No E-Mail 21,306
                            (sums to 64,000)

  | Frame  | Rows   | treatment=1 | treatment=0 (control) |
  |--------|--------|-------------|-----------------------|
  | mens   | 42,613 | 21,307      | 21,306                |
  | womens | 42,693 | 21,387      | 21,306                |

  TRAP: the pooled-control value is 42,693 = 64,000 - 21,307 (the "not Mens" pool),
  which COINCIDENTALLY equals the womens frame's legitimate row count. A test that
  asserts on total frame size can therefore pass while pooled. Assert on the CONTROL
  COUNT (21,306) and on segment.nunique() == 2.

  The treatment column is named `treatment`, never `T` — `df.T` is DataFrame transpose
  and returns a (13, 42613) DataFrame instead of a Series, failing SILENTLY inside
  groupbys (01-RESEARCH.md §Pitfall 6, hit accidentally during research).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Arm-vs-control frames built by positive membership</name>
  <files>dont_email_everyone/frames.py, tests/test_frames.py</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Code Example 4 (the verified constructor and the row-count table), §Pitfall 6 (the `T` / transpose collision), §Anti-Patterns to Avoid ("Building the control group by negation")
    - .planning/phases/01-data-foundation/01-PATTERNS.md — the `dont_email_everyone/frames.py` section, including the "Trap to encode in the test" note about 42,693
    - .planning/research/PITFALLS.md — Pitfall 1 (pooled control), the single most consequential error available in this project
    - dont_email_everyone/config.py — reuse `CONTROL` and `ARMS`; do not redefine them locally
    - tests/conftest.py — the session-scoped `raw_df` fixture to build frames from
  </read_first>
  <behavior>
    - tests/test_frames.py::test_frames_are_mutually_exclusive — for each of the mens and womens frames, `frame["segment"].nunique() == 2` and the two values are exactly `{arm_label, "No E-Mail"}`
    - tests/test_frames.py::test_control_group_is_not_pooled[mens] — the mens frame has exactly 21,306 rows with `treatment == 0` (NOT 42,693)
    - tests/test_frames.py::test_control_group_is_not_pooled[womens] — the womens frame has exactly 21,306 rows with `treatment == 0`
    - tests/test_frames.py::test_treated_counts — mens frame has 21,307 rows with `treatment == 1`; womens frame has 21,387
    - tests/test_frames.py::test_treatment_column_is_not_named_T — `"T"` is not in `frame.columns`, `"treatment"` is, and `frame.treatment` returns a Series (not a transposed DataFrame)
    - tests/test_frames.py::test_treatment_matches_segment — `treatment == 1` exactly where `segment == arm_label`, dtype int64
    - tests/test_frames.py::test_frames_do_not_mutate_input — building both frames leaves the source DataFrame's shape and columns unchanged
    - tests/test_frames.py::test_arms_are_disjoint_in_treated_rows — the treated rows of the two frames share no segment value, i.e. no row is treated in both frames
  </behavior>
  <action>
Create `dont_email_everyone/frames.py`. Import order: stdlib (none needed), blank, third-party (none needed directly), blank, `from dont_email_everyone import config`. Source `CONTROL` and `ARMS` from `config` rather than redefining the string literals locally — one definition site means a typo cannot desynchronize the frame builder from the schema.

Define `build_frame(df, arm_label)` that selects rows with `df["segment"].isin([arm_label, config.CONTROL])`, takes a `.copy()` so the caller's frame is never mutated, and adds a column named `treatment` computed as `(frame["segment"] == arm_label).astype("int64")`.

Two rules here carry through every later phase and must be stated in the module docstring, because both fail silently rather than loudly:

  (a) **Positive membership only.** Never `df[df["segment"] != arm_label]`. On this dataset, negation pools the *other* email arm into the control group: the "not Mens" pool is 42,693 rows containing 21,387 Womens-emailed customers who were treated. Every ATE, uplift score, and revenue figure in Phases 2-5 would then be computed against a contaminated counterfactual. Add an inline comment on the `isin` line saying so.

  (b) **The treatment column is named `treatment`, never `T`.** `DataFrame.T` is the transpose property, so `frame.T` returns a (13, 42613) DataFrame rather than the treatment Series, and it fails silently inside aggregations and groupbys rather than raising. This was hit accidentally while writing the research verification script.

Define `build_all_frames(df)` returning a dict keyed by the `config.ARMS` keys (`"mens"`, `"womens"`) mapping to the built frames, so callers iterate a mapping instead of hardcoding two calls.

Create `tests/test_frames.py` implementing the eight behaviors above, consuming the `raw_df` fixture. Parametrize the control-count test over both arms.

`test_control_group_is_not_pooled` is the load-bearing test of this plan and its assertion target must be chosen carefully: assert on the **control count of 21,306** and on `segment.nunique() == 2`, never on total frame size. The pooled value 42,693 coincidentally equals the womens frame's *legitimate* row count, so a size-based assertion can pass while the frame is pooled. Write the failure message to name 42,693 explicitly as the pooled-control signature, so a future regression diagnoses itself.

`test_treatment_column_is_not_named_T` should assert `isinstance(frame.treatment, pd.Series)` in addition to the column-name checks — proving attribute access returns the Series rather than something transposed.

Commit as its own commit, e.g. `feat(01-04): build arm-vs-control frames by positive membership`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frames.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_frames.py -q` reports 9 passed, 0 failed (8 behaviors, one of them parametrized over 2 arms)
    - `.venv/Scripts/python.exe -c "from dont_email_everyone.ingest import load_raw; from dont_email_everyone.frames import build_all_frames; f=build_all_frames(load_raw()); assert f['mens'].shape==(42613,13), f['mens'].shape; assert f['womens'].shape==(42693,13), f['womens'].shape; assert (f['mens'].treatment==0).sum()==21306; assert (f['womens'].treatment==0).sum()==21306; assert (f['mens'].treatment==1).sum()==21307; assert (f['womens'].treatment==1).sum()==21387; print('frame counts ok')"` exits 0
    - `grep -cE 'segment.*!=' dont_email_everyone/frames.py` returns 0 — no negation-based selection anywhere
    - `grep -c 'isin' dont_email_everyone/frames.py` returns at least 1
    - `grep -cE '\"T\"|\[.T.\]\s*=' dont_email_everyone/frames.py` returns 0 — no column is ever named `T`
    - `.venv/Scripts/python.exe -c "from dont_email_everyone import config,frames,ingest; d=ingest.load_raw(); before=(d.shape,list(d.columns)); frames.build_all_frames(d); assert (d.shape,list(d.columns))==before; print('no mutation')"` exits 0
    - `grep -c 'config.CONTROL' dont_email_everyone/frames.py` returns at least 1 — constants are sourced, not re-literalled
  </acceptance_criteria>
  <done>Both frames contain exactly two segment values with a control group of exactly 21,306 rows; the treatment column is named `treatment` and is int64; no negation-based selection exists in the module; the source frame is never mutated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the four gates into one reproducible entrypoint and commit the Parquet artifacts</name>
  <files>dont_email_everyone/ingest.py, tests/test_artifacts.py, data/processed/analysis_table.parquet, data/processed/mens_vs_control.parquet, data/processed/womens_vs_control.parquet</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §System Architecture Diagram (the four-gate ordering and the artifact boundary), §Don't Hand-Roll (Parquet round-trip verified dtype-stable including the pandas 3.0 `str` dtype), §Pitfall 8 (pyarrow required)
    - .planning/phases/01-data-foundation/01-CONTEXT.md — D-09 (Parquet is the committed artifact format; no `.duckdb` file is persisted), D-10 (nothing downstream may require DuckDB), and the Claude's-Discretion note on artifact naming
    - dont_email_everyone/ingest.py (current state) — extend it; `verify_checksum` and `load_raw` already exist
    - dont_email_everyone/frames.py (from Task 1) — `build_all_frames`
    - dont_email_everyone/schemas.py — `RawHillstrom`, validated with `lazy=True`
    - .gitignore — confirm `data/processed/` is NOT ignored (the Parquet files are deliberately committed)
    - .planning/ROADMAP.md — Phase 2 criterion 1 (three pairwise arm comparisons, needs the full table) and Phase 6 criterion 4 (the app must read committed artifacts with no DuckDB/Pandera), which together justify the artifact split below
  </read_first>
  <action>
Add a `build_all()` function to `dont_email_everyone/ingest.py` that composes the four gates in the order RESEARCH.md prescribes — bytes, then types, then values, then experimental structure — so that each gate can fail for exactly one reason, which is what makes a failure diagnosable:

  Gate 1: `verify_checksum(config.RAW_CSV, config.CHECKSUM_FILE)` — raises `ChecksumMismatchError` or `FileNotFoundError`
  Gate 2: `load_raw(config.RAW_CSV)` — raises `duckdb.ConversionException`
  Gate 3: `RawHillstrom.validate(df, lazy=True)` — raises `SchemaErrors` listing every violation
  Gate 4: `build_all_frames(validated)` plus assertions that each frame has `segment.nunique() == 2` and a control count of 21,306 — raises `AssertionError` naming the arm and the observed count

Do not collapse these into one try/except and do not add any fallback, repair, or re-fetch branch. Print a one-line progress note per gate with the observed shape so a run is self-documenting in the SUMMARY.

Write three Parquet artifacts under `config.PROCESSED` (creating the directory with `mkdir(parents=True, exist_ok=True)`), all with `index=False`:
  - `analysis_table.parquet` — the validated 64,000 x 12 table
  - `mens_vs_control.parquet` — the 42,613 x 13 mens frame
  - `womens_vs_control.parquet` — the 42,693 x 13 womens frame

This resolves CONTEXT.md's Claude's-Discretion item on artifact naming. Rationale to record in the SUMMARY: Phase 2 criterion 1 requires balance across *all three* pairwise arm comparisons including mens-vs-womens, which needs the full table; Phases 3-5 consume the two arm frames; and Phase 6's app must load only what it needs, so three narrow files beat one wide table with a membership column. Use `data/processed/` and not `artifacts/` — that closes RESEARCH.md Open Question 4 / PATTERNS.md conflict C5, where CONTEXT.md D-09 (a locked user decision) outranks ARCHITECTURE.md's earlier naming.

Add an `if __name__ == "__main__": build_all()` guard so the pipeline runs as `python -m dont_email_everyone.ingest`. Note in the module docstring that Phase 7's ROADMAP criterion 5 expects a `python -m dont_email_everyone.pipeline all` entrypoint; `pipeline.py` is a later-phase wrapper that will call `build_all()`, so keep `build_all()` importable and free of side effects at import time.

Run `python -m dont_email_everyone.ingest`, then commit the three Parquet files together with the code, e.g. `feat(01-04): add pipeline entrypoint and commit processed parquet artifacts`.

Create `tests/test_artifacts.py` proving the artifact boundary holds — this is what makes CONTEXT.md D-10 real rather than aspirational:
  - `test_artifacts_exist` — all three Parquet files exist under `config.PROCESSED` and are tracked by git (`git ls-files` on the directory lists all three)
  - `test_artifact_shapes` — reading each with `pandas.read_parquet` gives (64000, 12), (42613, 13), and (42693, 13)
  - `test_artifact_dtypes_survive_round_trip` — the four string columns read back as `str` dtype (not `object`), and `treatment` reads back as int64; the Parquet round-trip was verified dtype-stable on this stack including the new pandas 3.0 `str` dtype
  - `test_artifacts_readable_without_duckdb_or_pandera` — spawn a subprocess with `sys.executable -c` that imports only `pandas`, reads all three Parquet files, and asserts `"duckdb" not in sys.modules and "pandera" not in sys.modules` after the reads. This proves Phase 6's Streamlit Cloud app can drop DuckDB and Pandera from its serve-time `requirements.txt` (ROADMAP Phase 6 criterion 4). Use a subprocess rather than an in-process check, because the test session itself has already imported both.
  - `test_committed_control_counts` — reading `mens_vs_control.parquet` and `womens_vs_control.parquet` from disk (not rebuilding them) each yields exactly 21,306 rows with `treatment == 0`, so a stale or pooled artifact cannot sit committed in the repo undetected

Note in the test module docstring that Parquet writes are not guaranteed byte-identical across runs (pyarrow embeds metadata), so artifact freshness is asserted on *content*, never on file bytes or a checksum.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_artifacts.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m dont_email_everyone.ingest` exits 0 and prints a progress line per gate
    - `pytest tests/test_artifacts.py -q` reports 5 passed, 0 failed
    - `git ls-files data/processed/` lists exactly `analysis_table.parquet`, `mens_vs_control.parquet`, `womens_vs_control.parquet`
    - `.venv/Scripts/python.exe -c "import pandas as pd; from dont_email_everyone import config; a=pd.read_parquet(config.PROCESSED/'analysis_table.parquet'); m=pd.read_parquet(config.PROCESSED/'mens_vs_control.parquet'); w=pd.read_parquet(config.PROCESSED/'womens_vs_control.parquet'); assert (a.shape,m.shape,w.shape)==((64000,12),(42613,13),(42693,13)); assert (m.treatment==0).sum()==21306 and (w.treatment==0).sum()==21306; print('artifacts ok')"` exits 0
    - `.venv/Scripts/python.exe -c "import sys,pandas as pd; from pathlib import Path; [pd.read_parquet(p) for p in Path('data/processed').glob('*.parquet')]; assert 'duckdb' not in sys.modules and 'pandera' not in sys.modules; print('no duckdb/pandera needed')"` exits 0
    - `.venv/Scripts/python.exe -c "from dont_email_everyone import config; f=list(config.ROOT.rglob('*.duckdb')); assert not f, f; print('no duckdb file')"` exits 0
    - `grep -c 'if __name__' dont_email_everyone/ingest.py` returns 1
    - `grep -v '^#' dont_email_everyone/ingest.py | grep -ciE 'fallback|download|urlretrieve'` returns 0 — no fallback or re-fetch branch exists
  </acceptance_criteria>
  <done>`python -m dont_email_everyone.ingest` reproduces all three Parquet artifacts from the vendored CSV through all four gates; the artifacts are committed, have the verified shapes and control counts, and are readable with pandas alone.</done>
</task>

<task type="auto">
  <name>Task 3: Record setup, reproduction, and the six reconciliation decisions in the README</name>
  <files>README.md</files>
  <read_first>
    - README.md — current contents, so existing material is preserved rather than overwritten
    - .planning/phases/01-data-foundation/01-PATTERNS.md — §"Documented Conflicts the Planner Must Reconcile" (the C1-C6 table)
    - .planning/phases/01-data-foundation/01-RESEARCH.md — §Open Questions 1-4, §Environment Availability (the install-time vs pipeline-time network distinction), §Standard Stack "Installation"
    - .planning/ROADMAP.md — Phase 7 (DOC-01), so this task stays scoped to setup/reproduction and does not pre-empt the non-technical README
    - .planning/phases/01-data-foundation/01-CONTEXT.md — D-05, D-07, D-09
  </read_first>
  <action>
Add two focused sections to `README.md` without rewriting or removing existing content. Phase 7 (DOC-01) owns the non-technical, business-facing README — do **not** write the business framing, the headline dollar figure, or the limitations section here. Keep this task to setup and reproduction, and add a one-line note that the reader-facing overview arrives in Phase 7.

Section one, "Setup and reproduction": state Python 3.11 as the required interpreter; give the venv creation and install commands (`py -3.11 -m venv .venv`, activate, `python -m pip install --only-binary=:all: -r requirements-dev.txt`); give the pipeline command `python -m dont_email_everyone.ingest`; give the test command `python -m pytest -q`; and list what the pipeline produces (`data/processed/analysis_table.parquet`, `mens_vs_control.parquet`, `womens_vs_control.parquet`).

In that section, state the network distinction explicitly, because a reviewer will otherwise read it as a contradiction: `pip install` requires network access exactly once at environment setup; the **pipeline** makes no network calls, has no fetch code path, and is prevented from acquiring one by `tests/test_no_network.py`. Also state that the raw CSV is vendored and checksum-verified on every run rather than downloaded, and that `data/raw/*.csv` is marked `-text` in `.gitattributes` so the recorded SHA-256 holds on Linux and macOS clones as well as Windows.

Section two, "Data foundation decisions": a short table recording the six reconciliations from PATTERNS.md, labeled C1 through C6, so they are deliberate, discoverable, and not re-litigated in Phases 2-7. Record each with its resolution:
  - C1 — Pandera `coerce=False`, not `coerce=True` as `.planning/research/PITFALLS.md` Pitfall 16 suggested; `coerce=True` was verified to silently truncate a `recency` of `10.5` to `10` and report success, defeating this phase's own success criterion
  - C2 — string columns declared `str`, not `object`; pandas 3.0 (PDEP-14) makes `str` the default string dtype and `pa.Column(object)` fails outright
  - C3 — Pandera `DataFrameSchema` (object style), not `DataFrameModel` as ARCHITECTURE.md suggested
  - C4 — flat `tests/` for this phase, not ARCHITECTURE.md's tiered `tests/unit|statistical|integration`; Phase 2 introduces `tests/statistical/` when it has its first seeded-DGP test
  - C5 — `data/processed/` for committed artifacts, not `artifacts/`; CONTEXT.md D-09 is a locked user decision and outranks the earlier research doc
  - C6 — `pyarrow` is admitted as an I/O engine even though CLAUDE.md's allowlist does not name it; the allowlist governs modeling/analysis libraries ("demonstrates the technique from first principles, not library calls"), and `pandas.to_parquet` raises `ImportError` without pyarrow while D-09 mandates Parquet

Add one line noting that `streamlit` is deliberately absent from both requirements files until Phase 6, because `dont_email_everyone/` must never import it (D-07).

Commit as its own commit, e.g. `docs(01-04): document setup, reproduction, and data-foundation decisions`.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import pathlib; t=pathlib.Path('README.md').read_text(encoding='utf-8'); need=['py -3.11 -m venv .venv','python -m dont_email_everyone.ingest','python -m pytest','data/processed/','coerce=False','pyarrow','.gitattributes','C1','C2','C3','C4','C5','C6']; missing=[n for n in need if n not in t]; assert not missing, missing; print('readme ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains the literal strings `py -3.11 -m venv .venv`, `python -m dont_email_everyone.ingest`, and `python -m pytest`
    - `README.md` names all three artifact filenames: `analysis_table.parquet`, `mens_vs_control.parquet`, `womens_vs_control.parquet`
    - `README.md` contains all six reconciliation labels `C1` through `C6`, each with a stated resolution
    - `README.md` states that `pip install` is the only network access and that the pipeline itself makes none
    - `README.md` mentions `.gitattributes` and the cross-platform SHA-256 guarantee
    - `git diff --stat HEAD~1 README.md` shows lines added and none of the pre-existing content removed
    - The verify command exits 0
  </acceptance_criteria>
  <done>A reader can set up Python 3.11, install the pinned stack, reproduce all three artifacts, and run the test suite from the README alone; the six reconciliation decisions are recorded in-repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| validated table -> analysis frames | The experimental structure (who is treated, who is control) is established here. A wrong split silently biases every downstream causal claim. |
| pipeline -> committed artifacts | Parquet files committed here are consumed by Phases 2-7 and by the deployed Streamlit app, without re-validation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-17 | Tampering | control group contaminated by pooling the other treatment arm | mitigate | `build_frame` selects by `segment.isin([arm_label, config.CONTROL])`; negation is absent from the module and asserted absent by a source grep. `test_control_group_is_not_pooled` asserts the control count is exactly 21,306, deliberately not total frame size — because the pooled value 42,693 coincidentally equals the womens frame's legitimate row count and would let a size-based assertion pass while pooled. |
| T-01-18 | Tampering | treatment indicator silently resolving to `DataFrame.T` (transpose) | mitigate | The column is named `treatment`; `test_treatment_column_is_not_named_T` asserts `"T"` is absent from `columns` and that `frame.treatment` returns a Series. A source grep confirms no column is ever assigned the name `T`. |
| T-01-19 | Tampering | a stale or hand-edited Parquet artifact committed and consumed as authoritative | mitigate | `test_committed_control_counts` and `test_artifact_shapes` read the artifacts *from disk* rather than rebuilding them, so a stale or pooled committed file fails the suite. Full regeneration is one command (`python -m dont_email_everyone.ingest`). |
| T-01-20 | Elevation of Privilege | arbitrary code execution via a deserialized artifact | mitigate | Parquet is the only artifact format (D-09). No pickle, no joblib, no `.duckdb`. Parquet carries data and schema, not executable payloads. |
| T-01-21 | Denial of Service | the deployed app requiring heavy engines at serve time | mitigate | `test_artifacts_readable_without_duckdb_or_pandera` spawns a clean subprocess proving pandas alone suffices, satisfying D-10 and enabling ROADMAP Phase 6 criterion 4's slim serve-time requirements. |
| T-01-22 | Information Disclosure | PII in the committed artifacts | accept | The 2008 Hillstrom dataset is a public, anonymized teaching dataset with no identifiers — only recency, history, category flags, zip bucket, channel, arm, and outcomes. Nothing to redact. |

ASVS L1 categories in scope for this plan: **V5 Input Validation** (Gate 4 structural assertions on the frames, run inside `build_all()` and not only in tests) and **V14 Configuration** (artifact boundary — Parquet only, no `.duckdb`, no pickle). No HIGH-severity threat is left unmitigated.
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` reports 42 passed (28 after plan 01-03, plus 9 frame tests and 5 artifact tests)
- `python -m dont_email_everyone.ingest` regenerates all three artifacts and exits 0
- Both frames report `segment.nunique() == 2` and a control count of exactly 21,306
- No `*.duckdb` file exists anywhere under the repo root
- All three Parquet artifacts are tracked by git and readable with pandas alone
</verification>

<success_criteria>
- ROADMAP criterion 4 satisfied: two mutually exclusive analysis frames exist (mens-vs-control, womens-vs-control), each asserted by test to contain exactly two `segment` values with a control group of 21,306 rows, not 42,693
- DATA-03 completed end to end: schema validation runs inside the pipeline entrypoint, not only in tests
- DATA-04 extended: frame and artifact behavior is covered by pytest
- CONTEXT.md D-09 and D-10 satisfied and enforced by test: Parquet is the only committed artifact format, no `.duckdb` file is written, and downstream consumers need neither DuckDB nor Pandera
- The Claude's-Discretion item on artifact naming is resolved with recorded rationale
- PATTERNS.md conflicts C4, C5, and C6 are recorded in the README alongside C1-C3
- VALIDATION.md row for `test_frames_are_mutually_exclusive` is green
</success_criteria>

<output>
Create `.planning/phases/01-data-foundation/01-04-SUMMARY.md` when done. Record the observed frame shapes and control counts, the artifact naming rationale, and the `python -m dont_email_everyone.ingest` gate-by-gate output.
</output>

---
phase: 03-uplift-evaluation-metric
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dont_email_everyone/evaluation.py
  - tests/test_evaluation.py
autonomous: true
requirements: [UPLIFT-02]

must_haves:
  truths:
    - "ROADMAP C1: `evaluation.py` computes Qini curve points from `(score, treatment, outcome)` arrays using NumPy only — no causalml, no scikit-uplift, no model dependency, no file I/O."
    - "ROADMAP C2: `Q(0) == 0` exactly, and the curve endpoint equals the independently computed ATE."
    - "ROADMAP C2 / D-03 tier 1: with all-distinct scores, shuffling the input rows and recomputing returns a bit-identical curve array."
    - "ROADMAP C3: the Qini normalization convention is stated in the module docstring and pinned by a source-reading test, so the definition cannot silently drift."
    - "D-01: ties are broken by a seeded shuffle before a stable descending sort, not by caller row order — the sign-flip bug (+0.001890 vs −0.002051) is structurally impossible."
    - "D-02: every seeded function carries `seed: int = 20260902` as a keyword default; no `config.SEED` constant is introduced anywhere."
    - "The endpoint reproduces all six committed `ate.json` effects — a cross-check of two independent implementations, so a bug in either surfaces."
  artifacts:
    - path: "dont_email_everyone/evaluation.py"
      provides: "_ranked_arrays, qini_curve, qini_coefficient, input guards, convention docstring"
      contains: "def qini_curve("
      min_lines: 150
    - path: "tests/test_evaluation.py"
      provides: "module-purity boundary tests and the deterministic curve invariants"
      contains: "def test_endpoint_matches_committed_ate("
      min_lines: 150
  key_links:
    - from: "dont_email_everyone/evaluation.py"
      to: "numpy.trapezoid"
      via: "qini_coefficient area computation"
      pattern: "np\\.trapezoid"
    - from: "tests/test_evaluation.py"
      to: "data/processed/ate.json"
      via: "config.PROCESSED-anchored read in the cross-implementation endpoint test"
      pattern: "config\\.PROCESSED"
---

<objective>
Build the arithmetic core of `dont_email_everyone/evaluation.py` — the seeded ranking helper,
the Qini curve, and the Qini coefficient — and prove it against Phase 2's already-verified ATE.

Purpose: this closes the Qini normalization convention that `.planning/STATE.md` has carried as an
open blocker since roadmap creation, and it does so *before* any model exists so a disappointing
Phase 4 curve can be trusted rather than blamed on the metric. The cross-implementation endpoint
test built here is, per ARCHITECTURE.md, the highest-value single test in the whole suite: it
compares this phase's `cumsum`-based Qini endpoint against Phase 2's statsmodels OLS ATE, so a bug
in *either* implementation surfaces.

Output: `dont_email_everyone/evaluation.py` (new) and `tests/test_evaluation.py` (new).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uplift-evaluation-metric/03-CONTEXT.md
@.planning/phases/03-uplift-evaluation-metric/03-RESEARCH.md
@.planning/phases/03-uplift-evaluation-metric/03-PATTERNS.md
@.planning/phases/03-uplift-evaluation-metric/03-VALIDATION.md

<scope_note>
CONTEXT.md's `<domain>` block says the phase "runs entirely on seeded synthetic fixtures", while its
Claude's-Discretion section says the endpoint test "compares directly against
`data/processed/ate.parquet`". RESEARCH.md Open Question 2 resolves this and **this plan builds
both**: a synthetic endpoint-identity test AND a cross-check against the committed `ate.json`.
This is deliberate, not scope creep. The `<domain>` sentence is about *no model, no real-data
scoring, no train/holdout split* — all three remain true here. An arbitrary array is passed through
a pure function whose endpoint is provably score-independent; nothing is fitted and nothing is
persisted. The real-frame version is the only one that tests two independent implementations
against each other, which is what makes it the highest-value test in the suite.
</scope_note>

<discretion_resolutions>
CONTEXT.md left these open. Resolved here with RESEARCH.md §Q8's reasoning:

- **`qini_coefficient` is a separate module-level function**, `qini_coefficient(fraction, qini)`,
  not a field on the curve's return value (§Q8.1). Reasons: `qini_curve` stays a plain 2-tuple of
  arrays, which is the type every band function needs and would otherwise unpack 500 times per
  band; the bands recompute the coefficient on *resampled* curves, so a baked-in field would be
  redundant rather than free; and the area definition becomes independently testable against a
  hand-written polyline. It also matches how `ate.py` is factored (`ate_table` / `apply_holm` /
  `bootstrap_spend_ate` — one concern per function).
- **Tests stay flat as `tests/test_evaluation.py`** (§Q8.3). The repo is 15 files of
  `tests/test_<module>.py`; introducing ARCHITECTURE.md's `unit/ statistical/ integration/` tiering
  now would leave the repo half-migrated. Use section banners inside the file instead.
- **Structure inside `evaluation.py`** follows §Q8.2: six public functions plus one private
  `_ranked_arrays` helper; keyword-only after the three array arguments.
</discretion_resolutions>

<interfaces>
Public surface being created in this plan (the remaining functions land in plans 03-02 and 03-05):

    _ranked_arrays(score, treatment, outcome, seed) -> (t, y)   # private; the ONLY sort in the module
    qini_curve(score, treatment, outcome, *, seed=20260902)     -> (fraction, qini)  # both length n+1
    qini_coefficient(fraction, qini)                            -> float

Landing later, do not build here:
    uplift_at_k(...)          -> float          # plan 03-02
    tie_diagnostics(score)    -> dict           # plan 03-02
    bootstrap_indices(...)    -> ndarray (R, n) # plan 03-05
    qini_bootstrap_band(...)  -> (grid, lo, hi) # plan 03-05
    qini_random_band(...)     -> (grid, lo, hi) # plan 03-05

Committed data this plan's tests read (already present, do not regenerate):
    data/processed/ate.json          -> {"effects": [ {arm, outcome, unit, effect, se, ...} x 6 ]}
    data/processed/mens_vs_control.parquet    (42,613 rows; N_t = 21,307, N_c = 21,306)
    data/processed/womens_vs_control.parquet
Existing session fixtures in tests/conftest.py: `mens_frame`, `womens_frame`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create evaluation.py with the convention docstring, _ranked_arrays, qini_curve and qini_coefficient</name>
  <files>dont_email_everyone/evaluation.py</files>
  <read_first>
    - dont_email_everyone/ate.py (lines 1-14 module docstring shape; 83-115 `_guard_arm_vs_control`; 118-125 `_fit` centralization; 317 and 356-367 the seeded-function signature and provenance-in-return idiom)
    - dont_email_everyone/coverage.py (lines 12-18 array-taking purity docstring — the closer wording for this module; 42-66 the DOCUMENTED DIVERGENCE block to copy in shape; 97-110 pinned-constant-with-a-comment; 175-184 seeded-once-outside-the-loop)
    - dont_email_everyone/config.py (ROOT-anchored path constants; PRE_TREATMENT_FEATURES immutability precedent)
    - .planning/phases/03-uplift-evaluation-metric/03-RESEARCH.md §Q1.3, §Q1.4, §Q1.5, §Q2, §Q8.2, §"Code Examples" → Curve, coefficient, and uplift-at-k
    - .planning/phases/03-uplift-evaluation-metric/03-PATTERNS.md §"dont_email_everyone/evaluation.py"
    - tests/test_no_network.py (lines 12-27 — the rglob sweep and its FORBIDDEN token regex; it fires on docstrings and comments too)
  </read_first>
  <action>
Create `dont_email_everyone/evaluation.py`. `import numpy as np` is the only import — no pandas, no
matplotlib, no scipy, no file I/O.

**Module docstring.** Open with the purity declaration in `coverage.py:12-18`'s wording adapted to a
module that takes arrays rather than frames: reads no files, writes no files, prints nothing; the
caller supplies the data so nothing here can re-derive a frame from disk and bypass Phase 1's
SHA-256 and Pandera gates. Then use `ate.py:11-14`'s lettered-decision-block structure with the
"stated so a future agent does not simplify them" framing. Blocks required in this plan:

  (a) **Normalization convention (Radcliffe's Q, adjusted, per treated head).** State it as
      RESEARCH §Q1.5 specifies, verbatim or near-verbatim. The load-bearing sentences:
      the curve value at targeting fraction phi is the cumulative incremental outcome delivered by
      targeting the top phi of the combined population, divided by the total number of treated
      customers N_t; written out, Q(phi) = [Y_t(phi) - Y_c(phi) * n_t(phi) / n_c(phi)] / N_t, with
      Q(0) = 0. The treated/control ratio correction uses the *cumulative* counts inside the
      selection; the denominator is always the *total treated arm size*. Under this convention Q(1)
      is the average treatment effect exactly, so the curve is in the outcome's own units per
      treated customer — percentage points of visit rate, or dollars of spend. The Qini coefficient
      is the area between this curve and the random-targeting chord from (0,0) to (1, Q(1)), in
      those same units. It is deliberately NOT divided by a "perfect model" curve: that denominator
      requires an ordering by the individual treatment effect, which is never observable, and the
      published definitions disagree on how to fake it. This curve is algebraically identical to
      scikit-uplift's qini curve divided by N_t, and to pylift's adjusted qini (aqini).
      The following substrings MUST appear literally in the docstring because plan task 2 greps for
      them: `Radcliffe`, `per treated customer`, `cumulative`, `total treated arm size`,
      `perfect model`, `aqini`.
  (b) **D-01, the tie rule.** Seeded shuffle, then `np.argsort(-score, kind="stable")`. State the
      measured reason: on the real mens frame with a Radcliffe-shaped 3-rule 0-3 indicator score
      (largest tie group 39.0% of rows), a plain stable argsort with no pre-shuffle gives Qini
      coefficient +0.001890 in natural row order and -0.002051 when the frame is sorted by
      `treatment` — a sign flip from row order alone. The seeded-shuffle version gives +0.001699 on
      the same adversarial order, next to the 200-shuffle mean of +0.002020.
  (c) **D-03, the two-tier row-order guarantee — state it precisely, do not overclaim.**
      Distinct scores: exact, bit-identical equality under an input row shuffle. Tie-heavy scores:
      the coefficient genuinely moves inside tie groups; measured SD across 200 input shuffles is
      3.27e-04 against a random-score noise floor of SD 9.58e-04 on the same data — the tie-breaking
      rule moves the number by about one third of the metric's own noise floor, so it cannot
      manufacture a signal. No sentence containing the word "invariant" may appear without this
      qualifier.
  (d) **A DOCUMENTED DIVERGENCE FROM PITFALLS.md, RECORDED SO IT IS NOT "FIXED".** Copy
      `coverage.py:42-66`'s three moves: name the divergent source and both numbers, quantify
      whether the gap is noise, and name in backticks the assertion that *would* fail on correct
      code. Content: PITFALLS.md reports the top-20% random-score incremental-visit count as mean
      336 / SD 42 (a "~13% noise floor"). Measured here under the adjusted form on
      `mens_vs_control` (n=42,613, visit, 300 seeded random scores): mean 326.6 / SD 27.3 (8.4%).
      The measured mean sits on the closed-form expectation of 326 = 0.20 x 21,307 x 0.07659;
      PITFALLS.md's 336 does not, and PITFALLS.md's own Sources section states its Qini figures use
      an ad-hoc normalization whose absolute values are not canonical. Use 27.3 / 8.4% when deriving
      tolerances. Also pre-diagnose the two assertions that fail on correct code:
      `assert q[-1] == effect` (the endpoint is ~2.6e-14 from `ate.json`, not bit-identical — four
      cumsum accumulations versus an OLS solve) and `assert q_negated == -q_oracle` (measured
      +0.6061 versus -0.6051, residual 9.3e-04 — the seeded shuffle and the cumulative ratio
      correction are both order-dependent, so antisymmetry is false).
  (e) **What this number may not be used for.** The two arms share a control group, so their Qini
      coefficients are correlated and cannot be numerically compared — Radcliffe states this
      explicitly for this dataset. Also record the deliberate trade against scikit-uplift's
      distinct-value thresholding: it evaluates the curve only at score-change boundaries, which is
      exactly tie-invariant, but with a 39% largest tie fraction it cannot answer "what is the
      uplift at the top 20%?", which the Phase 6 slider requires. D-01's seeded shuffle traces an
      unbiased path *through* each tie group instead. Say it was a decision, not an oversight.

  **Docstring token hazards — the docstring is subject to two token checks and is NOT exempt.**
  `tests/test_no_network.py` greps every package `.py` file including prose for the word-boundary
  tokens `requests urllib httpx aiohttp urlretrieve socket ftplib http.client` — so never write the
  word "requests" (including as a plural noun). Task 2's purity test additionally forbids the
  substrings `to_parquet read_parquet open( savefig print( plt. matplotlib st. accuracy_score
  roc_auc classification_report .score(` anywhere outside a `#` comment line — so write
  scikit-uplift's normalized scalar as `qini_auc_score` with no trailing parenthesis, and never
  mention the plotting library by name.

**Constants.** Any fixed sequence is a tuple, never a list, following `coverage.py:104-106`'s
`CELL_SIZES` and `config.py:38`'s `PRE_TREATMENT_FEATURES`, each with a comment saying why it is
pinned. This plan needs at most the default seed literal; do not introduce `config.SEED` (D-02).

**Guards — a shared private validator, `if`/`raise`, never `assert`.** `assert` is compiled out
under `python -O`/`PYTHONOPTIMIZE`, which is the reason `ate._guard_arm_vs_control` records for its
own style; the same reasoning applies with more force to a gate standing between a NaN and a
published dollar figure. Every message names the observed values so the failure is diagnosable from
the traceback alone (`ate.py:83-115` is the template). Required checks before any sort:
  - the three arrays have equal length — name all three lengths;
  - `treatment` contains only the values 0 and 1 — name the offending distinct values found;
  - both arms are non-empty — name both counts;
  - `score` contains no NaN — name the NaN count and the first offending position. This one is not
    optional: `np.argsort` places NaN **last** regardless of sign, so a NaN score is silently ranked
    as the worst prospect. A Phase 4 T-learner emitting NaN on an unseen category would sink those
    customers to the bottom of the targeting list with no warning.
Convert `score` with `np.asarray(score, dtype=float)` **before** negating: negating an integer array
is not always safe (`-np.iinfo(np.int64).min` overflows back to itself), and the float conversion is
what makes the NaN check meaningful.

**`_ranked_arrays(score, treatment, outcome, seed)`.** The single place the seeded shuffle and the
stable descending sort live; both `qini_curve` and (in plan 03-02) `uplift_at_k` call it. Write its
docstring to `ate._fit`'s template — "one place where the sort is chosen, so `qini_curve` and
`uplift_at_k` cannot drift onto different rankings and silently break the
`uplift_at_k(k) == Q(k) * N_t / n_t(k)` identity (module docstring, decision (b))". Body: run the
guards, build `perm = np.random.default_rng(seed).permutation(n)`, then
`order = np.argsort(-score[perm], kind="stable")`, and return the permuted-and-ordered `treatment`
and `outcome` as float64 arrays. Do not copy scikit-uplift's `argsort(uplift, kind="mergesort")[::-1]`
— reversing a stable ascending sort reverses tie order, which is a different convention.

**`qini_curve(score, treatment, outcome, *, seed=20260902)`.** Keyword-only after the three arrays
so nobody positionally passes a seed where `k` belongs. Returns `(fraction, qini)`, both length
`n + 1`. Compute cumulative `n_t`, `n_c`, `y_t`, `y_c` with `np.cumsum` on the ranked arrays; form
the correction with `np.divide(y_c * n_t, n_c, out=np.zeros(n), where=n_c > 0)` so the head of the
curve produces a correct 0 instead of a RuntimeWarning storm; divide `(y_t - correction)` by
`n_t[-1]`, which is N_t. Then `fraction = np.arange(0, n + 1) / n` and prepend a literal `0.0` to
the gain array so `qini[0] == 0.0` exactly. Return the **full-length** grid, never a coarsened or
binned one: at n=42,613 that is two 341 KB float64 arrays computing in 5.3 ms, and binning would
make `uplift_at_k` at an arbitrary k inexpressible. Carry an inline comment distinguishing the
adjusted form from the genuinely broken `y_t(phi)/n_t(phi) - y_c(phi)/n_c(phi)` within-subset
response-rate form whose top-1% values explode.

**`qini_coefficient(fraction, qini)`.** Area between the curve and the **computed** random chord
`fraction * qini[-1]`, via `np.trapezoid`. **`np.trapz` does not exist** — it was removed in NumPy
2.x and is absent from this project's pinned NumPy 2.4.6. The system Python 3.9 on this machine
still has it and only warns, so reaching for it fails silently outside the venv; every command in
this plan uses `.venv/Scripts/python.exe`. Return a plain `float(...)`. Trapezoid is exact for a
piecewise-linear polyline by construction, so do not reach for Simpson's rule or a spline.
Note in a comment that `n_c == 0` at the head of the curve is handled by the `where=` clause above,
not by a per-element branch.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import json, pathlib, numpy as np, pandas as pd; from dont_email_everyone import config, evaluation; f = pd.read_parquet(config.PROCESSED / 'mens_vs_control.parquet'); s = np.random.default_rng(1).normal(size=len(f)); fr, q = evaluation.qini_curve(s, f['treatment'].to_numpy(), f['visit'].to_numpy()); e = [r for r in json.loads((config.PROCESSED / 'ate.json').read_text(encoding='utf-8'))['effects'] if r['arm'] == 'mens' and r['outcome'] == 'visit'][0]['effect']; assert fr.shape == q.shape == (len(f) + 1,); assert q[0] == 0.0; assert abs(q[-1] - e) < 1e-9, (q[-1], e); print('endpoint', q[-1], 'ate.json', e, 'coef', evaluation.qini_coefficient(fr, q))"</automated>
  </verify>
  <acceptance_criteria>
    - `dont_email_everyone/evaluation.py` exists and its only third-party import is `numpy`:
      `.venv/Scripts/python.exe -c "import ast,pathlib;src=pathlib.Path('dont_email_everyone/evaluation.py').read_text(encoding='utf-8');mods={(n.module or '').split('.')[0] for n in ast.walk(ast.parse(src)) if isinstance(n,ast.ImportFrom)}|{a.name.split('.')[0] for n in ast.walk(ast.parse(src)) if isinstance(n,ast.Import) for a in n.names};print(sorted(mods));assert mods<= {'numpy'},mods"` exits 0.
    - Source contains `def _ranked_arrays(`, `def qini_curve(`, `def qini_coefficient(`.
    - Source contains `np.trapezoid` and contains **no** occurrence of `np.trapz` or `trapz(`.
    - Source contains `seed: int = 20260902` and contains no occurrence of `config.SEED`.
    - Source contains `kind="stable"` and `default_rng(seed).permutation`.
    - Source contains `where=` on the `np.divide` call (the `n_c == 0` head-of-curve guard).
    - The module docstring contains all six greppable phrases: `Radcliffe`, `per treated customer`,
      `cumulative`, `total treated arm size`, `perfect model`, `aqini`. Check with
      `.venv/Scripts/python.exe -c "from dont_email_everyone import evaluation as e;d=e.__doc__;[print(p, p in d) for p in ('Radcliffe','per treated customer','cumulative','total treated arm size','perfect model','aqini')];assert all(p in d for p in ('Radcliffe','per treated customer','cumulative','total treated arm size','perfect model','aqini'))"`.
    - The module docstring names both divergent PITFALLS.md figures: it contains the substrings
      `27.3` and `42`, and contains `8.4%`.
    - No `assert ` statement appears in `evaluation.py` outside a `#` comment line (all guards are
      `if`/`raise`): `.venv/Scripts/python.exe -c "import pathlib;b=[l for l in pathlib.Path('dont_email_everyone/evaluation.py').read_text(encoding='utf-8').splitlines() if not l.lstrip().startswith('#')];assert not [l for l in b if l.lstrip().startswith('assert ')]"` exits 0.
    - `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` exits 0 (the rglob sweep now
      covers the new module).
    - The Task 1 `<automated>` command exits 0 and prints an endpoint within 1e-9 of `ate.json`'s
      mens/visit effect.
    - Calling `evaluation.qini_curve` with a score array containing `np.nan` raises `ValueError`
      whose message contains `nan`; calling it with mismatched array lengths raises `ValueError`.
  </acceptance_criteria>
  <done>`evaluation.py` computes a full-length Qini curve and its coefficient from NumPy alone, with the normalization convention, D-01's tie rule, D-03's two-tier guarantee and the measured PITFALLS.md divergence recorded in the module docstring, and every input guard written as `if`/`raise`.</done>
</task>

<task type="auto">
  <name>Task 2: Create tests/test_evaluation.py with the module-boundary tests and the deterministic curve invariants</name>
  <files>tests/test_evaluation.py</files>
  <read_first>
    - dont_email_everyone/evaluation.py (as written in Task 1)
    - tests/test_plots.py (lines 71-74 and 341-344 section banners; 346-361 `_plots_source` + comment-stripping + split-string forbidden-token loop; 378-385 the chdir/empty-dir writes-nothing test)
    - tests/test_pipeline.py (lines 349-380 the two-helper `_pipeline_source` / `_pipeline_body` split and the "no CWD-relative path literal" check)
    - tests/test_ate.py (lines 169-173 seeded-therefore-deterministic framing; 233-240 purity test; 424-439 the exact-at-same-seed / banded-across-seeds two-tier structure)
    - tests/test_coverage.py (lines 229-234 the failure message quoting 0.769827 and pre-diagnosing the cause; 395-410 the array-input purity variant)
    - tests/conftest.py (the `mens_frame` / `womens_frame` session fixtures)
    - .planning/phases/03-uplift-evaluation-metric/03-VALIDATION.md (the per-task map — these node IDs are the contract; do not rename them)
  </read_first>
  <action>
Create `tests/test_evaluation.py`. Use `# ---- <section> ----`-style banners in `test_plots.py`'s
shape, one per public function plus a `Module boundary` section. Read every path through
`config.ROOT` / `config.PROCESSED`; no CWD-relative literal appears anywhere. Every assertion
carries an f-string message naming the observed value followed by a sentence saying what a wrong
value *means* — the house style at `tests/test_coverage.py:229-234`, not a bare `assert failed`.

Write exactly these node IDs — 03-VALIDATION.md's per-task map is the naming contract:

1. `test_evaluation_module_is_pure` — build it as `_evaluation_source()` + `_evaluation_body()`
   (the two-helper split from `test_pipeline.py:349-380`), where `_evaluation_body` drops every line
   whose lstrip starts with `#`. Loop over the forbidden tuple with a per-token message
   (`test_plots.py:346-361`'s loop). Tokens: `to_parquet`, `read_parquet`, `open(`, `savefig`,
   `print(`, `plt.`, `matplotlib`, `st.`, `accuracy_score`, `roc_auc`, `classification_report`,
   `.score(`. Any token that would otherwise appear literally in this test file must be built by
   string concatenation the way `test_plots.py` writes `"plt.sh" + "ow"`, so the test file does not
   trip its own check when a future grep is widened. The accuracy/AUC tokens are here so
   PITFALLS.md Pitfall 9's repo-grep is enforced at the module boundary rather than remembered.
   Note in the test's docstring that this list is *wider* than anything else currently enforced in
   the repo — that widening is this phase's own contribution.
2. `test_evaluation_module_writes_nothing` — `monkeypatch.chdir(tmp_path)`, construct inputs from a
   local `np.random.default_rng`, call **every** public function the module currently exposes
   (`qini_curve`, `qini_coefficient`), then `assert list(tmp_path.iterdir()) == []` with
   `test_coverage.py:395-410`'s message wording. Leave a comment stating that plans 03-02 and 03-05
   must extend the call list when they add public functions.
3. `test_curve_starts_at_origin` — `fraction[0] == 0.0` and `qini[0] == 0.0` exactly (not approx),
   and `fraction[-1] == 1.0`, and both arrays have length `n + 1`.
4. `test_endpoint_equals_difference_in_means` — on a synthetic frame from the existing
   `synthetic_frame` fixture, assert `qini[-1] == pytest.approx(y[t==1].mean() - y[t==0].mean(), rel=1e-12)`.
   This is the *realized sample* difference in means, NOT the injected DGP `effect` — measured, the
   two differ by 0.074 at n=8,000 with an injected effect of 1.0, which is pure baseline sampling
   noise. Put that number in the test's comment so nobody later "tightens" this into a comparison
   against the injected parameter.
5. `test_endpoint_matches_committed_ate` — **the highest-value test in the suite; over-engineer it.**
   Parametrize over all six `(arm, outcome)` pairs read from `config.PROCESSED / "ate.json"`'s
   `effects` list (`arm` in mens/womens, `outcome` in visit/conversion/spend), loading the matching
   `mens_frame` / `womens_frame` fixture. For each pair, run `qini_curve` with an arbitrary seeded
   random score and assert `qini[-1] == pytest.approx(row["effect"], rel=1e-12)`. Comment that the
   measured agreement is 2.6e-14 on mens visit (0.07658956365153388) and mens spend
   (0.7698271558945627), so `rel=1e-12` is two orders of magnitude of headroom rather than a guess,
   and that `assert qini[-1] == row["effect"]` **will fail on correct code** because the curve
   reaches the ATE through four `cumsum` accumulations while statsmodels reaches it through an OLS
   solve. Over-engineering, all of which is cheap: also assert the endpoint is unchanged (to the
   same tolerance) across three different score seeds — the endpoint is provably score-independent,
   so a score-dependent endpoint means the ranking has leaked into the denominator; and assert the
   sign and rough magnitude match `row["ci_low"] < qini[-1] < row["ci_high"]`. Leave this test
   **unmarked** (two curve calls, ~11 ms each) so it runs on every commit.
6. `test_distinct_scores_are_exactly_row_order_invariant` — D-03 tier 1. With an all-distinct score
   array, shuffle the input rows with an independent RNG, recompute, and assert `np.array_equal` on
   both returned arrays — **bit-identical**, not `np.allclose`. Measured max diff is exactly 0.0 at
   n=42,613. Message must explain why this holds: with distinct scores the descending sort produces
   the same permutation of `(t, y)` regardless of the seeded pre-shuffle, so the `cumsum`
   accumulation order is identical — which is the assertion that proves the sort leaks no row order.
   Run this at synthetic scale unmarked; if a real-frame version is added, mark it
   `@pytest.mark.slow`.
7. `test_curve_is_not_forced_monotone` — assert `np.diff(qini)` contains at least one strictly
   negative entry on a random-score curve. A curve that only ever increases is the symptom of a
   cumulative-gain curve mislabeled as a Qini curve.
8. `test_docstring_pins_the_normalization_convention` — read `evaluation.__doc__` and assert each of
   the six phrases `Radcliffe`, `per treated customer`, `cumulative`, `total treated arm size`,
   `perfect model`, `aqini` is present, with a per-phrase message saying ROADMAP criterion 3
   requires the convention to be un-driftable. Also assert the docstring contains both `27.3` and
   `8.4%` so the documented PITFALLS.md divergence cannot be quietly deleted.
9. A `qini_coefficient` unit case on a hand-written polyline with a known area — e.g. a 3-point
   curve whose area against its own chord is computable by hand — so the area definition is testable
   independently of the curve. Name it `test_qini_coefficient_area_against_a_hand_written_polyline`.
   Also assert `qini_coefficient` of the chord itself is 0.0.
10. Guard cases: a NaN in `score` raises `ValueError` matching `nan`; mismatched array lengths raise
    `ValueError`; a `treatment` array containing a 2 raises `ValueError`; an empty control arm raises
    `ValueError`. Use `pytest.raises(ValueError, match=...)` following `test_coverage.py:389`.

No test in this file may pin a value derived from Radcliffe's published `Q` percentages (5.44%,
3.02%, ...). RESEARCH.md assumption **A1** flags the scaling behind them as inferred, not confirmed;
reproducing them is an explicit non-goal for this phase.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_evaluation.py -q -m "not slow"</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_evaluation.py -q -m "not slow"` exits 0.
    - Each of these node IDs exists and passes individually:
      `.venv/Scripts/python.exe -m pytest "tests/test_evaluation.py::test_evaluation_module_is_pure" "tests/test_evaluation.py::test_evaluation_module_writes_nothing" "tests/test_evaluation.py::test_curve_starts_at_origin" "tests/test_evaluation.py::test_endpoint_equals_difference_in_means" "tests/test_evaluation.py::test_distinct_scores_are_exactly_row_order_invariant" "tests/test_evaluation.py::test_curve_is_not_forced_monotone" "tests/test_evaluation.py::test_docstring_pins_the_normalization_convention" -q` exits 0.
    - `.venv/Scripts/python.exe -m pytest "tests/test_evaluation.py::test_endpoint_matches_committed_ate" -q` exits 0 and collects **6** parametrized cases (one per `ate.json` effect row).
    - `test_endpoint_matches_committed_ate` is unmarked: `.venv/Scripts/python.exe -m pytest tests/test_evaluation.py -q -m "not slow" --collect-only` lists it.
    - The test file contains `pytest.approx` with `rel=1e-12` and contains `np.array_equal`.
    - The test file contains no CWD-relative path literal:
      `grep -n '"data/\|"reports/\|"dont_email_everyone/' tests/test_evaluation.py` returns nothing
      outside a `config.ROOT` / `config.PROCESSED` expression.
    - The test file contains no `5.44` and no `Radcliffe's published` claim (assumption A1 non-goal):
      `grep -c '5\.44' tests/test_evaluation.py` returns 0.
    - Full suite is green and has grown from the 188-test baseline:
      `.venv/Scripts/python.exe -m pytest -q` exits 0 with more than 188 passed.
  </acceptance_criteria>
  <done>The deterministic Qini invariants and the module-purity boundary are enforced by tests using the exact node IDs 03-VALIDATION.md specifies, and the six-way cross-check against Phase 2's committed `ate.json` passes unmarked on every commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller arrays → `evaluation.py` | The only boundary. In-process NumPy arrays supplied by a test today, and by Phase 4/5/6 code later. No network, no filesystem, no user input, no deserialization, no authentication, no secrets exist anywhere in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `qini_curve` input `score` | mitigate | NaN scores are ranked **last** by `np.argsort` with no warning, silently sinking those customers to the bottom of the targeting list. Explicit `if np.isnan(...).any(): raise ValueError` naming the count and first position (Task 1). Pinned by the guard case in Task 2. |
| T-03-02 | Tampering | all input guards | mitigate | Guards are `if`/`raise`, never `assert` — `assert` is compiled out under `python -O`/`PYTHONOPTIMIZE`, which would disable every gate at once. Enforced by the "no bare `assert` in the module body" acceptance criterion in Task 1. |
| T-03-03 | Information Disclosure | `evaluation.py` filesystem surface | mitigate | The module performs no I/O. Enforced two ways: `test_evaluation_module_is_pure` (source-reading forbidden-token list) and `test_evaluation_module_writes_nothing` (chdir to tmp, call every public function, assert the directory stayed empty). |
| T-03-04 | Repudiation (by analogy — the project's real reputational risk) | the reported Qini number | mitigate | PITFALLS.md's own security table names "presenting a noise-driven segment as a finding" as the live risk here. Mitigations in this plan: D-03's two-tier invariance statement instead of a blanket "invariant" claim; the module docstring carrying the caveat that the two arms' Qini coefficients cannot be numerically compared; the documented PITFALLS.md divergence block naming both numbers so a future agent does not "fix" a non-bug. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | **No package is installed by this plan.** RESEARCH.md's Package Legitimacy Audit table is empty by construction: every library used is already pinned at an exact `==` version in the committed `requirements.txt` / `requirements-dev.txt`. If any task here proposes adding a package, it is out of compliance with CLAUDE.md's allowlist and must be rejected rather than gated. |
| T-03-05 | Denial of Service | memory | accept | The full-length curve allocates two float64 arrays of 341 KB at n=42,613, computed in 5.3 ms. No unbounded allocation exists in this plan; the `bootstrap_indices` memory bound is plan 03-05's concern. |

No V2 (authentication), V3 (session), V4 (access control) or V6 (cryptography) control applies —
this plan introduces no identity, session, protected resource or cryptographic operation. ASVS L1
categories that do apply are V5 (input validation, above), V7 (error handling — every raised message
names the offending values so failures are diagnosable from the traceback alone) and V12 (files and
resources — no file I/O).
</threat_model>

<verification>
Run in the project venv only. The machine has a second interpreter (system Python 3.9 / NumPy 2.0.2)
that does **not** match `requirements.txt` and still carries the removed-in-2.x `np.trapz`, so code
that is broken inside the project can appear to work outside it.

1. `.venv/Scripts/python.exe -c "import numpy; print(numpy.__version__, hasattr(numpy,'trapezoid'), hasattr(numpy,'trapz'))"` → `2.4.6 True False`
2. `.venv/Scripts/python.exe -m pytest tests/test_evaluation.py -q -m "not slow"` → exit 0, under 5 s
3. `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` → exit 0
4. `.venv/Scripts/python.exe -m pytest -q` → exit 0, passed count > 188 (the pre-phase baseline)
5. `git status --short dont_email_everyone/pipeline.py tests/test_pipeline.py tests/test_reports.py data/processed reports/figures` → empty (this plan touches none of them)
</verification>

<success_criteria>
- `dont_email_everyone/evaluation.py` exists, imports only NumPy, and exposes `qini_curve` and
  `qini_coefficient` with `seed: int = 20260902` keyword defaults.
- `qini_curve` returns `(fraction, qini)` of length `n + 1` with `qini[0] == 0.0` exactly and
  `qini[-1]` equal to all six committed `ate.json` effects within `rel=1e-12`.
- The normalization convention, the D-01 tie rule, the D-03 two-tier guarantee and the measured
  PITFALLS.md divergence are all in the module docstring and pinned by
  `test_docstring_pins_the_normalization_convention`.
- `tests/test_evaluation.py` passes with the node IDs 03-VALIDATION.md specifies, and the full suite
  is green above the 188-test baseline.
- `pipeline.py`, `analyze()`, `tests/test_reports.py`'s `FIGURE_NAMES` and
  `tests/test_artifacts.py`'s `ARTIFACT_NAMES` are untouched (D-09).
</success_criteria>

<output>
Create `.planning/phases/03-uplift-evaluation-metric/03-01-SUMMARY.md` when done.
</output>


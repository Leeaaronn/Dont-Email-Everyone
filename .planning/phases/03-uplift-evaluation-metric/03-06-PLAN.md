---
phase: 03-uplift-evaluation-metric
plan: 06
type: execute
wave: 5
depends_on: ["03-03", "03-05"]
files_modified:
  - reports/metric.md
  - tests/test_reports.py
autonomous: false
requirements: [UPLIFT-02]

must_haves:
  truths:
    - "D-08: the phase leaves behind code, tests AND a short `reports/metric.md` write-up stating the Qini normalization convention, the uplift-at-k convention, the tie rule and both band definitions, with the synthetic-oracle results as evidence the implementation is correct."
    - "D-08 does not relax ROADMAP criterion 3 — the convention still lives in `evaluation.py`'s module docstring and is still pinned by a test; `reports/metric.md` is additional, not a substitute."
    - "The conventions are stated BEFORE any result in the document, matching `reports/validity.md`'s 'a decision rule stated after the result it judges is not a decision rule' precedent."
    - "Every number in the write-up names the unmarked test that produces it, so the document is reproducible by running the suite rather than by trusting the author."
    - "D-09: no synthetic demo figure is committed; `FIGURE_NAMES` in tests/test_reports.py is not extended and `reports/figures/` gains nothing."
    - "No claim in the document reproduces Radcliffe's published Q percentages — RESEARCH.md assumption A1 flags that scaling as inferred, not confirmed, and the write-up says so."
  artifacts:
    - path: "reports/metric.md"
      provides: "the conventions, the tie rule, both band definitions and the synthetic-oracle evidence"
      contains: "## "
      min_lines: 90
    - path: "tests/test_reports.py"
      provides: "presence and git-tracking assertions for metric.md, matching the existing validity.md pattern"
      contains: "metric.md"
  key_links:
    - from: "reports/metric.md"
      to: "tests/test_evaluation.py"
      via: "per-section source lines naming the test that reproduces each number"
      pattern: "tests/test_evaluation.py::"
---

<objective>
Author `reports/metric.md` and extend `tests/test_reports.py` to cover it, closing D-08 and the
phase.

Purpose: a reviewer skimming this repo will not read a module docstring, and the conventions are
exactly what a knowledgeable reviewer checks. Phase 2 established the `reports/` convention
(02-CONTEXT.md D-06) and Phase 7 links rather than re-derives, so this is the second file in
`reports/` and the pattern is now established rather than being invented. It also closes the entry
`.planning/STATE.md` has carried under Blockers since roadmap creation: "Qini normalization
convention is unsettled — several published definitions differ. Pick one explicitly, document it in
the module docstring, and encode it in a test."

Output: `reports/metric.md` (new), a `metric.md` case in `tests/test_reports.py`, and a human
verification checkpoint on the prose.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-uplift-evaluation-metric/03-CONTEXT.md
@.planning/phases/03-uplift-evaluation-metric/03-RESEARCH.md
@.planning/phases/03-uplift-evaluation-metric/03-PATTERNS.md
@.planning/phases/03-uplift-evaluation-metric/03-VALIDATION.md
@.planning/phases/03-uplift-evaluation-metric/03-03-SUMMARY.md
@.planning/phases/03-uplift-evaluation-metric/03-05-SUMMARY.md

<discretion_resolutions>
- **`reports/metric.md` gets presence, git-tracking and non-triviality assertions only** (RESEARCH.md
  Open Question 3). Extend `tests/test_reports.py`'s existing `validity.md` pattern and stop there —
  do not assert on prose. That file goes further for `validity.md` (section ordering, forbidden
  overclaim phrases, headline-number tracing at lines 85-148), and the machinery is available, but
  this write-up's numbers are produced by *tests* rather than by committed artifacts, so the
  artifact-tracing test has no analogue here. Prose accuracy is a manual verification, already
  recorded in 03-VALIDATION.md's Manual-Only table and discharged by this plan's checkpoint task.
- **This phase has no committed artifact to cite per section** (D-09). `validity.md`'s convention of
  an italic `*Source: data/processed/<file>.parquet*` line under each section becomes, here, an
  italic source line naming the unmarked pytest node ID that reproduces the number. RESEARCH.md Open
  Question 3 asks for exactly this: "the write-up should say which test produces each".
</discretion_resolutions>

<interfaces>
Everything this document describes now exists and is tested:

    evaluation.qini_curve / qini_coefficient / uplift_at_k / tie_diagnostics
    evaluation.bootstrap_indices / qini_bootstrap_band / qini_random_band
    plots.qini_plot

Existing in `tests/test_reports.py`, to extend:
    MIN_FIGURE_BYTES = 5_000                       # module constant, around line 31
    FIGURE_NAMES = [...]                           # line 31 — DO NOT ADD TO THIS (D-09)
    _tracked_names(directory)                      # lines 36-52 — the git ls-files helper, unchanged
    test_validity_report_exists()                  # lines 75-82 — the presence + tracking template
</interfaces>

<do_not_touch>
- `FIGURE_NAMES` in `tests/test_reports.py:31` — adding a Qini figure name fails `test_figures_exist`
  because no such PNG is committed (D-09).
- `tests/test_artifacts.py`'s `ARTIFACT_NAMES` — a presence allowlist, not a glob. This phase commits
  nothing under `data/processed/`.
- `dont_email_everyone/pipeline.py` and its **three** pinning tests, not one:
  `tests/test_pipeline.py:114-125` (`test_analyze_writes_exactly_the_expected_artifact_set`),
  `:383-385` (`body.count("savefig(") == body.count("plt.close(") == 2`) and `:388-390`
  (`to_parquet(` count is 3). D-09 adds neither a figure nor a Parquet; `analyze()` is not modified
  by this phase.
- The existing `validity.md` assertions in `tests/test_reports.py:85-148` — leave them exactly as
  they are.
</do_not_touch>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author reports/metric.md</name>
  <files>reports/metric.md</files>
  <read_first>
    - reports/validity.md (all 206 lines — the heading order, the opening paragraph shape, the provenance paragraph, the italic per-section source line at line 22, the criteria-before-results paragraph at line 16 and its justification sentence, the pinned-axis paragraph at line 37 as the model for explaining a design choice's honesty, and the closing "Inputs and artifacts" section)
    - dont_email_everyone/evaluation.py (the module docstring — the write-up must agree with it, not paraphrase it into something subtly different)
    - tests/test_evaluation.py (the node IDs — each number in the write-up cites the test that produces it)
    - .planning/phases/03-uplift-evaluation-metric/03-RESEARCH.md §Q1.3, §Q1.4, §Q1.6, §Q2, §Q3, §Q5, §Q6, §Assumptions Log A1 and A4
  </read_first>
  <action>
Create `reports/metric.md`, mirroring `reports/validity.md`'s structure: a one-line H1 naming the
phase, an opening paragraph giving the question and what the document covers **in that order**, a
provenance paragraph, then numbered `##` sections each opening with an italic source line naming the
pytest node ID that reproduces its numbers.

**Conventions come before results, and the document says why.** `validity.md:16` states "a decision
rule stated after the result it judges is not a decision rule"; the Phase 3 analogue is that the
normalization convention and the uplift-at-k convention are what a knowledgeable reviewer checks, so
they sit above every number. Sections:

  1. **The conventions, stated before any result.**
     - Normalization: Radcliffe's Q, adjusted, per treated head. Write the formula out as
       `Q(phi) = [Y_t(phi) - Y_c(phi) * n_t(phi) / n_c(phi)] / N_t` with `Q(0) = 0`. State the two
       axes the published definitions diverge on — whether the ratio correction uses cumulative
       counts or total arm sizes, and whether the scalar is divided by a "perfect model" curve — and
       this project's answer on both: cumulative counts for the ratio correction, total treated arm
       size as the denominator, **no** perfect-curve normalization. Give the three reasons for the
       last: the perfect curve requires an ordering by the individual treatment effect, which is
       never observable; scikit-uplift manufactures one out of the realized outcomes, making the
       denominator a random variable estimated from the same data as the numerator; and the
       denominator has at least four published variants, one of which (Radcliffe's own q0) reaches
       118%. Then the positive claim: this curve is algebraically identical to scikit-uplift's qini
       curve divided by N_t and to pylift's adjusted qini (aqini) — two independent reference
       implementations agree with it up to a stated scaling, with neither as a dependency.
     - Uplift-at-k: `'overall'`, with the reason, not just the choice — it is the quantity the
       deployment decision produces (rank the list once, mail the top k, observe the mailed-versus-not
       comparison inside that slice), while `'by_group'` describes an experiment nobody runs because
       in deployment there is no separate control ranking to take a top-k of. State the `int(n * k)`
       truncation rule.
     - Units, and why they are stated separately: the curve is in outcome units **per treated
       customer in the full population**; uplift-at-k is in outcome units **per targeted customer**.
       Give the exact bridge `uplift_at_k(k) == Q(k) * N_t / n_t(k)` and say plainly that `Q(k) / k`
       is the near-miss — 0.09429 against the correct 0.09384 at k=0.20 — because the realized
       treated count inside the top-k is not `k * N_t`.
     - The tie rule (D-01) and the two-tier row-order guarantee (D-03), stated as a guarantee with
       its limits rather than as a blanket claim.
     - Both band definitions (D-06) and the question each one answers.
  2. **Evidence that the implementation is correct** — the synthetic-oracle results, each under its
     italic source line. Cover: the endpoint reproducing all six committed `ate.json` effects (quote
     the mens visit 0.076590 and mens spend 0.769827 figures and the 2.6e-14 agreement, and say the
     comparison is against Phase 2's independently computed OLS number, which is what makes it a
     check on two implementations rather than one); `Q(0) = 0` exactly; the random score landing
     inside the measured null band; the oracle score clearing it by roughly 7x; the negated score
     being non-positive; the `uplift_at_k` identity holding to 1.4e-17; and bit-identical row-order
     invariance under distinct scores.
  3. **What the tie rule costs, measured** — at a 39.0% largest tie fraction the coefficient's SD
     across 200 input shuffles is 3.27e-04 against a random-score noise floor of SD 9.58e-04 on the
     same data, about one third. Conclusion in one sentence: the tie-breaking rule moves the number
     by less than the metric's own noise floor, so it cannot manufacture a signal. Contrast with what
     happens without the seeded shuffle: +0.001890 in natural row order versus −0.002051 sorted by
     treatment, a sign flip from row order alone.
  4. **A documented divergence from the project's own pitfalls research** — same voice
     `reports/validity.md` uses for the 38.05% / 37.4% coverage gap, and for the same reason.
     PITFALLS.md reports the top-20% random-score incremental-visit count as mean 336 / SD 42 (a 13%
     noise floor); measured here under the adjusted form it is mean 326.6 / SD 27.3 (8.4%), and the
     measured mean sits on the closed-form expectation of 326 while PITFALLS.md's does not. State
     that this repo's tolerances derive from 27.3 / 8.4%, and that PITFALLS.md's own Sources section
     records that its Qini figures use an ad-hoc normalization whose absolute values are not
     canonical. Add the measured variance argument the published sources do not make: the adjusted
     form's Qini-coefficient null SD is 9.63e-04 against the unadjusted form's 1.06e-03, about a 9%
     reduction — so the balance correction is a measured step here, not a mechanical one, even though
     its effect on the *bias* is negligible on this near-perfectly-balanced data.
  5. **Bands, and the sentence they exist to make sayable** — both definitions, the replicate counts
     (200 null, 500 bootstrap) with the measured 1.1 s and 2.47 s at n=42,613, and a demonstration on
     synthetic data that the machinery produces a claim of the shape *"beats random targeting in the
     top ~20%, indistinguishable beyond that"*, citing the test showing the oracle curve escaping the
     null band in the top 20%. Record assumption A4: R=500/R=200 sufficiency is sourced from the
     project's own features and pitfalls research rather than independently convergence-tested, and
     raising either costs seconds.
  6. **What this number may not be used for** — the two arms share a control group, so their Qini
     coefficients are correlated and cannot be numerically compared; Radcliffe states this explicitly
     for this dataset. Then the A1 statement: he quotes his Q as a percentage (5.44% for his Mens
     visit training model) and the most likely scaling is the area between the curve and the chord as
     a proportion of the area *under* the chord — a 3-rule proxy score on this repo's real mens frame
     gives 5.3% against his published 5.44% — but that scaling is **not stated in the extractable
     text of his paper**, so the correspondence is inferred rather than confirmed and **no test in
     this repo pins reproducing it**. Say that plainly. This is the same disposition
     `reports/validity.md` took toward the coverage gap: cite the external number as separately
     generated rather than claiming to have reproduced it.
  7. **Inputs and reproduction** — mirroring `validity.md`'s closing section. State that this phase
     commits no artifact and no figure by design (D-09): a synthetic Qini curve sitting beside the
     real `love_plot.png` and `ate_forest.png` could be mistaken for a result, and the first committed
     uplift figure is Phase 4's, drawn on real holdout scores. Give the reproduction command as
     `.venv/Scripts/python.exe -m pytest tests/test_evaluation.py -q` and note that the slow-marked
     cases carry the real-Hillstrom-scale checks.

Target 90-200 lines — `validity.md` is 206 and this document covers less ground. The write-up must
not contradict `evaluation.py`'s module docstring; where both state a convention, they state the
same thing.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import pathlib; t=pathlib.Path('reports/metric.md').read_text(encoding='utf-8'); need=['Radcliffe','per treated customer','per targeted customer','overall','aqini','perfect model','0.076590','0.769827','2.6e-14','1.4e-17','27.3','8.4','336','3.27e-04','9.58e-04','0.001890','tests/test_evaluation.py::']; missing=[s for s in need if s not in t]; print('missing:',missing); assert not missing; assert '5.44' in t and 'inferred' in t; assert len(t.splitlines())>=90, len(t.splitlines()); print('lines',len(t.splitlines()))"</automated>
  </verify>
  <acceptance_criteria>
    - `reports/metric.md` exists and is at least 90 lines.
    - The Task 1 `<automated>` command exits 0 — every required substring is present, including the
      conventions (`Radcliffe`, `aqini`, `perfect model`, `overall`, both unit phrases), the
      cross-check figures (`0.076590`, `0.769827`, `2.6e-14`), the identity tolerance (`1.4e-17`),
      the divergence numbers (`27.3`, `8.4`, `336`), the tie-wobble numbers (`3.27e-04`, `9.58e-04`,
      `0.001890`) and at least one `tests/test_evaluation.py::` source citation.
    - The conventions section precedes the first results section:
      `.venv/Scripts/python.exe -c "import pathlib,re; t=pathlib.Path('reports/metric.md').read_text(encoding='utf-8'); h=[m.group(0) for m in re.finditer(r'^## .*$', t, re.M)]; print(h); assert 'convention' in h[0].lower()"` exits 0.
    - The A1 non-goal is stated, not merely omitted: the file contains `5.44` **and** `inferred`.
    - Every `##` section after the first is followed within three lines by an italic source line
      containing either `tests/test_evaluation.py::` or `tests/test_plots.py::`.
    - No figure or data artifact was created: `git status --short reports/figures data/processed` is
      empty.
    - The document does not claim unqualified row-order invariance:
      `grep -n 'invariant' reports/metric.md` shows every hit accompanied by `distinct`, `tie` or
      `exact` on the same or an adjacent line.
  </acceptance_criteria>
  <done>`reports/metric.md` states both conventions, the tie rule, the two-tier invariance guarantee and both band definitions above every result, carries the synthetic-oracle evidence with a pytest node ID beside each number, records the measured PITFALLS.md divergence, and marks reproducing Radcliffe's published percentages as an explicit non-goal.</done>
</task>

<task type="auto">
  <name>Task 2: Extend tests/test_reports.py to cover metric.md</name>
  <files>tests/test_reports.py</files>
  <read_first>
    - tests/test_reports.py (lines 28-33 the module constants `MIN_FIGURE_BYTES` and `FIGURE_NAMES`; lines 36-52 `_tracked_names` with its `cwd=config.ROOT` / `check=True` docstring; lines 75-82 `test_validity_report_exists` — the presence + git-tracking template to generalize; lines 85-148 the validity-specific assertions, which must be left untouched)
    - reports/metric.md (as authored in Task 1)
    - .planning/phases/03-uplift-evaluation-metric/03-RESEARCH.md §Open Question 3 (extend the presence/tracking pattern and stop there)
  </read_first>
  <action>
Add a module constant `REPORT_NAMES` beside the existing `MIN_FIGURE_BYTES` / `FIGURE_NAMES`
constants, holding `("validity.md", "metric.md")` — a tuple, not a list, following
`config.PRE_TREATMENT_FEATURES` and `coverage.CELL_SIZES` (`tests/test_ate.py:243-246` proves that
immutability convention with `pytest.raises(TypeError)`). Give it a comment in
`coverage.py:97-110`'s style saying it is a presence allowlist rather than a glob — 02-06 recorded
that an untracked or deleted write-up would otherwise still pass the suite — and that adding a name
here is how a new report becomes covered.

Add `test_metric_report_exists`, parametrized over `REPORT_NAMES` or written as a sibling of
`test_validity_report_exists` (either is fine; do not rewrite the existing test's body). It asserts
three things and stops there:
  - the file exists under `config.REPORTS`;
  - its name appears in `_tracked_names(config.REPORTS)`, so it is committed rather than merely
    present in a working tree;
  - it is non-trivial — a byte floor in the spirit of `MIN_FIGURE_BYTES`, e.g. at least 2,000 bytes,
    with a message saying a stub write-up passes a presence check and fails a reader.

**Stop there. Do not assert on `metric.md`'s prose.** The existing section-ordering, forbidden-phrase
and headline-number-tracing tests at lines 85-148 apply to `validity.md` only, because their premise
is that every number traces to a committed artifact — this write-up's numbers trace to *tests*
instead. Prose accuracy is discharged by Task 3's checkpoint.

**Do not add anything to `FIGURE_NAMES`.** D-09 commits no figure, and `test_figures_exist` would
fail on a name with no PNG behind it.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_reports.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_reports.py` contains `REPORT_NAMES` and `metric.md`.
    - `.venv/Scripts/python.exe -m pytest tests/test_reports.py -q` exits 0, and every pre-existing
      `validity.md` case still passes:
      `.venv/Scripts/python.exe -m pytest "tests/test_reports.py::test_validity_report_exists" -q` exits 0.
    - `FIGURE_NAMES` is unchanged:
      `git diff -- tests/test_reports.py | grep -c '^-.*FIGURE_NAMES'` returns 0.
    - The new report test fails when the file is absent — prove the gate is live:
      `git stash push -- reports/metric.md && .venv/Scripts/python.exe -m pytest tests/test_reports.py -q; git stash pop`
      shows a failure between the two git commands and a clean restore after.
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 (full suite).
    - `git status --short dont_email_everyone/pipeline.py tests/test_pipeline.py tests/test_artifacts.py reports/figures data/processed` is empty.
  </acceptance_criteria>
  <done>`reports/metric.md` is covered by presence, git-tracking and non-triviality assertions using the same `_tracked_names` helper `validity.md` uses, `FIGURE_NAMES` and the validity-specific assertions are untouched, and the gate is demonstrated to fire when the file is missing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of reports/metric.md prose</name>
  <files>reports/metric.md</files>
  <read_first>
    - reports/metric.md (as authored in Task 1)
    - reports/validity.md (the Phase 2 precedent this document is judged against)
    - .planning/phases/03-uplift-evaluation-metric/03-VALIDATION.md (the Manual-Only Verifications table — this task discharges its single row)
  </read_first>
  <action>
Pause and hand the write-up to the user for review. Do not edit `reports/metric.md` during this task
except to apply corrections the user names. Present: the full-suite result including `-m slow`, the
`git status --short` output proving nothing landed under `reports/figures/` or `data/processed/`, and
a pointer to `reports/metric.md`. Then walk the user through the `<how-to-verify>` steps below and
wait. If the user names corrections, apply exactly those, re-run
`.venv/Scripts/python.exe -m pytest tests/test_reports.py -q` and the Task 1 substring check, and
present again. Do not close the phase on anything other than an explicit approval.
  </action>
  <what-built>
`reports/metric.md` — the phase's write-up — plus its presence, git-tracking and non-triviality tests
in `tests/test_reports.py`. Everything mechanically checkable about the document is already asserted
(required substrings, section ordering, source citations, byte floor). What no test can check is
whether the prose is accurate and readable to someone who did not write it, which is why
03-VALIDATION.md records this as the phase's single manual-only verification.

Also complete by this point: `evaluation.py` with all six public functions, `plots.qini_plot`, the
extended `synthetic_frame` fixture, and `tests/test_evaluation.py`'s full invariant suite.
  </what-built>
  <how-to-verify>
1. Run the full suite in the project venv, including slow cases:
   `.venv/Scripts/python.exe -m pytest -q` and then
   `.venv/Scripts/python.exe -m pytest -q -m slow`
   Both should exit 0. The baseline before this phase was 188 passed.
2. Open `reports/metric.md` and read it end to end. Confirm it states, in this order and
   before any result:
   - the Qini normalization convention (Radcliffe's Q, adjusted, per treated head — and that it is
     deliberately NOT divided by a perfect-model curve);
   - the uplift-at-k convention (`'overall'`, with the reason, and the `int(n * k)` truncation rule);
   - the tie rule (seeded shuffle before a stable descending sort);
   - both band definitions and the question each answers.
3. Confirm the synthetic-oracle results read as evidence rather than as assertion — each number has
   an italic source line naming the pytest node that produces it. Spot-check one by running that
   node ID directly and confirming the number in the document matches what the test asserts.
4. Confirm the two honesty passages are present and read as honest rather than defensive:
   - the documented divergence from PITFALLS.md (336 / 42 versus the measured 326.6 / 27.3);
   - the statement that reproducing Radcliffe's published 5.44% is inferred, not confirmed, and is
     not pinned by any test.
5. Confirm nothing was committed that should not have been:
   `git status --short` and `git diff --stat` — expect only `reports/metric.md` and
   `tests/test_reports.py` from this plan, and nothing under `reports/figures/` or `data/processed/`.
6. Judgment call, the only one that matters here: would a technical reviewer skimming this repo on
   GitHub come away able to check the metric's definition without reading the source? If not, say
   what is missing.
  </how-to-verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 and `.venv/Scripts/python.exe -m pytest -q -m slow` exits 0.
    - `git status --short reports/figures data/processed` is empty.
    - The user types "approved", or names specific prose corrections which are then made and
      re-verified.
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest -q && .venv/Scripts/python.exe -m pytest -q -m slow && git status --short reports/figures data/processed</automated>
    <human-check>The user has read `reports/metric.md` end to end and replied "approved", or named corrections which have since been applied and re-verified.</human-check>
  </verify>
  <done>The full suite is green including slow cases, nothing was committed under `reports/figures/` or `data/processed/`, and the user has explicitly approved `reports/metric.md`'s prose.</done>
  <resume-signal>Type "approved" to close the phase, or describe what the write-up gets wrong or leaves out.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → human reader | The only boundary this plan creates. `reports/metric.md` is static Markdown; nothing executes it, nothing parses it at runtime, and it carries no credential, path or input. |
| `tests/test_reports.py` → `git ls-files` subprocess | An existing, unchanged helper run with `cwd=config.ROOT` and `check=True`, so a silent git failure raises instead of leaving assertions vacuous. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-24 | Repudiation (by analogy — the project's real reputational risk) | `reports/metric.md`'s claims | mitigate | PITFALLS.md's own security table names "presenting a noise-driven segment as a finding" as the live risk in this project. Mitigations here: the conventions are stated above every result so no rule is stated after the result it judges; every number cites the test that reproduces it; the PITFALLS.md divergence names both figures rather than quietly adopting one; and reproducing Radcliffe's 5.44% is explicitly declared inferred and unpinned. |
| T-03-25 | Tampering | the write-up drifting out of sync with the code | mitigate | The document must not contradict `evaluation.py`'s module docstring, which is itself pinned by `test_docstring_pins_the_normalization_convention`. The write-up's required substrings are asserted by the Task 1 automated check, so a silent rewrite of the convention in one place fails a check in the other. |
| T-03-26 | Information Disclosure | committed content | accept | The document contains no secret, no credential, no path outside the repo and no personal data. The underlying dataset is a public 2008 release. |
| T-03-27 | Tampering | regression in Phase 2's report tests | mitigate | `FIGURE_NAMES` and the `validity.md`-specific assertions at `tests/test_reports.py:85-148` are declared do-not-touch, with a `git diff` grep as an acceptance criterion and the full suite as the gate. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No package is installed. RESEARCH.md's Package Legitimacy Audit table is empty by construction; every library is already pinned at an exact `==` version in the committed requirements files. |

V2/V3/V4/V6 do not apply — no identity, session, protected resource or cryptography is introduced.
Applicable ASVS L1 categories: V7 (error handling — `_tracked_names` already uses `check=True` so a
git failure raises rather than silently emptying the tracked set) and V12 (files and resources — the
only writes are two repository text files; the tests write nothing outside `tmp_path`).
</threat_model>

<verification>
Project venv only — the system Python 3.9 on this machine does not match `requirements.txt`.

1. `.venv/Scripts/python.exe -m pytest tests/test_reports.py -q` → exit 0
2. `.venv/Scripts/python.exe -m pytest -q` → exit 0, passed count well above the 188 pre-phase baseline
3. `.venv/Scripts/python.exe -m pytest -q -m slow` → exit 0
4. `git status --short reports/figures data/processed dont_email_everyone/pipeline.py tests/test_pipeline.py tests/test_artifacts.py` → empty
5. `git ls-files reports/` → lists `reports/metric.md` and `reports/validity.md` after commit
</verification>

<success_criteria>
- `reports/metric.md` exists, is git-tracked, is at least 90 lines, and states both conventions, the
  tie rule, the two-tier invariance guarantee and both band definitions **before** any result.
- Every result in the document names the pytest node ID that reproduces it.
- The PITFALLS.md divergence and the Radcliffe A1 non-goal are both stated plainly.
- `tests/test_reports.py` covers `metric.md` with presence, git-tracking and non-triviality
  assertions and nothing more; `FIGURE_NAMES` and the `validity.md` assertions are unchanged.
- The full suite is green including `-m slow`, and nothing was committed under `reports/figures/` or
  `data/processed/` (D-09).
- The human checkpoint is approved.
</success_criteria>

<output>
Create `.planning/phases/03-uplift-evaluation-metric/03-06-SUMMARY.md` when done.
</output>

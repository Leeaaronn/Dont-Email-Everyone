---
phase: 04-uplift-modeling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dont_email_everyone/features.py
  - dont_email_everyone/frames.py
  - tests/test_features.py
  - tests/test_frames.py
autonomous: true
requirements: [UPLIFT-01]

must_haves:
  truths:
    - "ROADMAP C2 (half): a design matrix is built from `config.PRE_TREATMENT_FEATURES` alone, with the encoder fit ONCE on the combined 64,000-row frame, so the two arms' models cannot land in different feature spaces (PITFALLS Pitfall 5)."
    - "ROADMAP C1 (half): a seeded, segment-stratified 50/50 train/holdout assignment exists as a pure function returning a `str` Series — the column plan 04-03 materializes."
    - "D-24: `features.py` is flat beside `ate.py`/`balance.py`/`evaluation.py`, is pure (reads nothing, writes nothing, prints nothing), and never constructs features by dropping columns."
    - "D-06: the split is 50/50 within each of the three `segment` values, drawn from `np.random.default_rng`, never `sklearn.model_selection.train_test_split`."
    - "D-08: `assign_split` takes `seed: int = 20260902` as a keyword default; no project-wide `config.SEED` is introduced."
    - "A post-treatment column reaching the feature list raises `ValueError` naming the offender — and `split` is in that forbidden set even though `balance.POST_TREATMENT_COLUMNS` does not list it."
    - "PC-4: no accuracy-family token (`accuracy_score`, `roc_auc`, `classification_report`, `.score(`) appears anywhere in `features.py`, docstrings included."
  artifacts:
    - path: "dont_email_everyone/features.py"
      provides: "design_matrix() — the all-K one-hot design matrix and its fitted encoder"
      contains: "def design_matrix("
    - path: "dont_email_everyone/frames.py"
      provides: "assign_split() — the seeded segment-stratified 50/50 train/holdout labeller"
      contains: "def assign_split("
    - path: "tests/test_features.py"
      provides: "design-matrix shape/column/order tests, the leak guard, the purity sweep and the writes-nothing test"
      contains: "def test_features_module_is_pure("
    - path: "tests/test_frames.py"
      provides: "assign_split determinism, the six per-segment counts, and the positionality docstring test"
      contains: "assign_split"
  key_links:
    - from: "dont_email_everyone/features.py::design_matrix"
      to: "config.PRE_TREATMENT_FEATURES"
      via: "list(config.PRE_TREATMENT_FEATURES) indexing, never df.columns.drop"
      pattern: "config\\.PRE_TREATMENT_FEATURES"
    - from: "dont_email_everyone/frames.py::assign_split"
      to: "numpy's documented Generator stream"
      via: "np.random.default_rng(seed).permutation per segment"
      pattern: "default_rng\\(seed\\)"
---

<objective>
Create `dont_email_everyone/features.py` (the design matrix, D-24) and add
`frames.assign_split` (the seeded 50/50 stratified split, D-06/D-08), with their tests.

Purpose: these are the two pure functions every later plan in the phase stands on. `design_matrix`
is what makes ROADMAP criterion 2's `m0.feature_names_in_ == m1.feature_names_in_` assertion
non-vacuous — one encoder, fit once on all 64,000 rows, sliced per arm. `assign_split` is the
function plan 04-03 wires into `ingest.build_all` as gate 4; it must exist and be tested before that
plan touches Phase 1.

Output: one new pure module, one new function in an existing pure module, one new test file, one
extended test file. **No committed artifact changes in this plan** — `data/processed/` is untouched
and the full suite must be green when this plan ends.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-uplift-modeling/04-CONTEXT.md
@.planning/phases/04-uplift-modeling/04-RESEARCH.md
@.planning/phases/04-uplift-modeling/04-PATTERNS.md
@.planning/phases/04-uplift-modeling/04-VALIDATION.md

<discretion_resolutions>
- **`assign_split` lives in `frames.py`, not `features.py`** (04-RESEARCH §Q3b). `ingest.py` already
  imports `frames.build_all_frames`; routing the split through `features.py` would make Phase 1's
  ingestion depend on a Phase 4 modeling module, a dependency inversion nothing else in this repo
  has. The split is a property of the experimental frame — the same category as `frames.py`'s
  existing `treatment` column — while `features.py` is about the design matrix.
- **All-K one-hot (`drop=None`), not K-1** (04-RESEARCH §Q2). This is the **third** encoding
  convention in the repo and it is deliberate. `balance.py` decision (b) already documents the other
  two. The collinearity argument that forces K-1 in `balance.omnibus_lr_test` does not transfer:
  `statsmodels.MNLogit` is an unpenalized MLE, whereas every learner in D-10 is L2-penalized or a
  tree. Trees additionally *need* all K — with `drop="first"`, `zip_code == "Rural"` is only
  representable as a two-split conjunction. All-K also yields exactly the 11 expanded covariate
  names `balance.parquet` already carries, so a reader can lay the two reports side by side.
- **`features.py` gets its OWN forbidden-column tuple**, not an edit to
  `balance.POST_TREATMENT_COLUMNS` (04-PATTERNS §features.py). That constant predates D-07 and does
  not list `split`; editing it would silently change what the Phase 2 balance table guards against.
  The local tuple names the same columns plus `split`, with a comment cross-referencing the other.
- **`np.random.default_rng`, never `sklearn.model_selection.train_test_split`** (04-RESEARCH §Q3c).
  NumPy's Generator stream is a documented stability guarantee (NEP 19); sklearn's RNG consumption
  pattern is an undocumented implementation detail. For a column materialized once and regenerated
  by `pipeline ingest`, stream stability across a future sklearn bump is the entire contract.
</discretion_resolutions>

<interfaces>
Existing, consumed here — do not modify:

    config.PRE_TREATMENT_FEATURES = ("recency", "history", "mens", "womens", "zip_code", "newbie", "channel")
    config.ARMS    = MappingProxyType({"mens": "Mens E-Mail", "womens": "Womens E-Mail"})
    config.CONTROL = "No E-Mail"
    frames.build_frame(df, arm_label) -> DataFrame          # frames.py:26-42
    frames.build_all_frames(df) -> dict                     # frames.py:43-47
    balance.POST_TREATMENT_COLUMNS                          # balance.py:76 — READ, never edit

Dtypes on the committed analysis table (measured, 04-RESEARCH §Q2):
    recency int64 (1-12) · history float64 (29.99-3345.93) · mens/womens/newbie int64 0/1
    zip_code str {Rural, Surburban, Urban} · channel str {Multichannel, Phone, Web}

Added here:

    features.CATEGORICAL = ("zip_code", "channel")
    features.design_matrix(df) -> (DataFrame, ColumnTransformer)
    frames.assign_split(df, seed: int = 20260902) -> Series[str]   # "train" / "holdout"

Verified target values (04-RESEARCH §Q2, §Q3c — all MEASURED in this repo):
    design_matrix(analysis).shape == (64000, 11)
    column order exactly: zip_code_Rural, zip_code_Surburban, zip_code_Urban,
                          channel_Multichannel, channel_Phone, channel_Web,
                          recency, history, mens, womens, newbie
    encoder.feature_names_in_ holds the 7 RAW names
    assign_split at seed 20260902: Mens E-Mail 10653/10654, No E-Mail 10653/10653,
                                   Womens E-Mail 10693/10694  (train/holdout)
</interfaces>

<test_fixtures>
`tests/conftest.py` (session-scoped, already present — write no new ones):
`raw_df`, `analysis_df`, `mens_frame`, `womens_frame`, `synthetic_frame(n, effect, imbalance, seed, hetero)`.
`analysis_df` reads the **committed** `analysis_table.parquet`, which at this plan's start still has
12 columns and no `split`. Nothing in this plan may assume the split column is committed — plan
04-03 does that.
</test_fixtures>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dont_email_everyone/features.py — the all-K design matrix and its leak guard</name>
  <files>dont_email_everyone/features.py</files>
  <read_first>
    - dont_email_everyone/balance.py (lines 1-40 the module docstring's lettered decisions, especially decision (b) on the two coexisting one-hot conventions; 58-60 the import block ordering; 76-122 `POST_TREATMENT_COLUMNS` and `_guard_no_post_treatment` — copy the structure and message tone exactly; 151-175 the allowlist-driven expansion)
    - dont_email_everyone/evaluation.py (lines 1-16 the purity paragraph and the numbered-decisions docstring format to copy verbatim in shape)
    - dont_email_everyone/config.py (lines 25-47 `ARMS`, `CONTROL`, and the `PRE_TREATMENT_FEATURES` comment that names this phase by name)
    - .planning/phases/04-uplift-modeling/04-RESEARCH.md §Q2 and §Code Examples 2
    - .planning/phases/04-uplift-modeling/04-PATTERNS.md §"dont_email_everyone/features.py" and §"Shared Patterns" items 1, 3, 4, 5
  </read_first>
  <action>
Create `dont_email_everyone/features.py` as a **pure** module: reads no files, writes no files,
prints nothing, imports no matplotlib and no streamlit.

**Module docstring.** Open with a one-paragraph statement of what the module is (the design matrix
for the Phase 4 T-learners, built from `config.PRE_TREATMENT_FEATURES`), then copy
`evaluation.py:4-10`'s purity paragraph in its own wording, then the lettered-decision block in
`evaluation.py`/`coverage.py`'s `(a)`/`(b)`/`(c)` format. The letters are:

  (a) ENCODING CONVENTION — all K levels (`drop=None`). State that this is the **third** convention
      in the repo, name `balance.py` decision (b) as the place the other two are documented, and give
      the four reasons in descending strength: the collinearity argument does not transfer because
      every D-10 learner is L2-penalized or a tree while `statsmodels.MNLogit` is an unpenalized MLE
      whose rank-deficient design has no unique solution; a `RandomForest` splits on individual
      columns, so `drop="first"` makes one level representable only as a two-split conjunction and
      D-12 forbids giving the forest depth budget back; all-K yields exactly the 11 expanded
      covariate names already in `balance.parquet` so the two reports are cross-readable; and
      dropping a level makes the coefficient vector's meaning depend on which category sorted first
      inside `OneHotEncoder.categories_`.
  (b) FIT ONCE, SLICE EVERYWHERE — the encoder is fit on the **combined** frame (all 64,000 rows,
      all three segments) and every per-arm and per-split view is obtained by `.loc`/mask slicing the
      single transformed frame. Two `pd.get_dummies` calls, one per arm, is PITFALLS.md Pitfall 5's
      named failure mode; slicing one transformed frame makes the misalignment structurally
      impossible rather than merely avoided. Note that this is also what makes a cross-arm score
      correlation meaningful at all.
  (c) ALLOWLIST, NEVER DROP — the feature list comes from `config.PRE_TREATMENT_FEATURES`; the module
      never calls `df.columns.drop(...)` or takes a set difference. Quote the stakes `config.py`'s own
      comment states: dropping is exactly how `visit`/`conversion`/`spend` leak in.
  (d) `handle_unknown="error"` — a level present in one arm and absent from the fit frame must raise
      rather than encode as an all-zeros row, because an all-zeros row is a silently wrong prediction.

**PC-4 warning:** any prose in this module explaining why classification-accuracy-family metrics are
the wrong yardstick must be spelled so it does not contain the literal tokens the purity sweep bans.
The sweep in Task 3 strips comment *lines* only — **docstrings are not stripped**. Follow the
02-03 / 03-01 precedent: rephrase the sentence, never drop the warning.

**Imports.** Follow `balance.py:58-60`'s ordering: standard library, then third party
(`numpy as np`, and from `sklearn.compose` / `sklearn.preprocessing` the `ColumnTransformer` and
`OneHotEncoder`), then `from dont_email_everyone import config`.

**Module constants**, each with a comment in `coverage.CELL_SIZES`'s "editing this breaks nothing
loudly" voice, and each a **tuple**, never a list (the `config.PRE_TREATMENT_FEATURES` /
`coverage.CELL_SIZES` immutability convention):

  - `CATEGORICAL = ("zip_code", "channel")` — the two `str` columns inside
    `config.PRE_TREATMENT_FEATURES`; everything else passes through numerically.
  - `FORBIDDEN_FEATURE_COLUMNS` — a tuple naming `visit`, `conversion`, `spend`, `segment`,
    `treatment` and **`split`**. The comment must state that this is deliberately a
    `features.py`-local tuple and **not** `balance.POST_TREATMENT_COLUMNS`: that constant predates
    CONTEXT.md D-07 and does not list `split`, and editing it would change what the Phase 2 balance
    table guards without re-verifying the balance table. Cross-reference `balance.py:76` by name so a
    reader finds the other list.

**`_guard_no_post_treatment(columns)`** — copy `balance._guard_no_post_treatment`'s structure and
message tone exactly. A plain `if`/`raise` with `ValueError`, never `assert`, carrying the repo's
standard sentence that asserts are compiled out under `python -O`/`PYTHONOPTIMIZE`. The message names
the leaked columns, states that only `config.PRE_TREATMENT_FEATURES` may enter a design matrix, and
says what goes wrong: an outcome column here makes the fitted model predict the outcome from itself
and turns the uplift score into a restatement of the treatment effect (PITFALLS.md Pitfall 6,
ROADMAP Phase 1 criterion 5).

**`design_matrix(df)`** returning the pair `(X, encoder)`:

  - Take `features = list(config.PRE_TREATMENT_FEATURES)` and call `_guard_no_post_treatment` on it
    before anything else, so the guard fires even if the constant is ever edited wrongly.
  - Raise `ValueError` naming the missing names if any of the seven is absent from `df.columns`.
  - Build a `ColumnTransformer` with a single `"cat"` transformer:
    `OneHotEncoder(drop=None, sparse_output=False, dtype=np.float64, handle_unknown="error")` over
    `list(CATEGORICAL)`, `remainder="passthrough"`, `verbose_feature_names_out=False` (so the output
    names are `zip_code_Rural`, not `cat__zip_code_Rural`), and `.set_output(transform="pandas")`.
  - Fit and transform `df[features]` in one `fit_transform` call, then `.astype("float64")` on the
    result. State in the docstring why the cast is kept: `remainder="passthrough"` preserves int64
    for `recency`/`mens`/`womens`/`newbie`, so the raw output has mixed dtypes, and a per-arm slice
    that happens to be dtype-homogeneous can take a different code path inside `StandardScaler` than
    one that is not — D-12's "identical across both arms" must be true of the inputs as well as the
    hyperparameters.
  - Return `(X, encoder)`. The fitted encoder is returned, not discarded, because it carries
    `feature_names_in_` (the 7 raw names) and `get_feature_names_out()` (the 11 expanded names) and
    a caller that re-fits to recover them has already lost the property (b) exists to guarantee.
  - The docstring states the returned shape on the committed table — `(64000, 11)` — and the exact
    11-name column order, and states that `df` is never mutated.

**Do not** add a `split`-aware or arm-aware parameter. Slicing is the caller's job and is a one-line
`.loc` on the returned frame; a `arm=` parameter here would re-introduce the per-arm fit this module
exists to make impossible.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -c "import pandas as pd; from dont_email_everyone import config, features; a=pd.read_parquet(config.PROCESSED/'analysis_table.parquet'); X,enc=features.design_matrix(a); print(X.shape); print(list(X.columns)); print(list(enc.feature_names_in_)); assert X.shape==(64000,11); assert list(X.columns)==['zip_code_Rural','zip_code_Surburban','zip_code_Urban','channel_Multichannel','channel_Phone','channel_Web','recency','history','mens','womens','newbie']; assert list(enc.feature_names_in_)==list(config.PRE_TREATMENT_FEATURES); assert str(X.dtypes.unique().tolist())=='[dtype(\"float64\")]'; assert 'split' in features.FORBIDDEN_FEATURE_COLUMNS"</automated>
  </verify>
  <acceptance_criteria>
    - `dont_email_everyone/features.py` exists and contains `def design_matrix(`, `def _guard_no_post_treatment(`, `CATEGORICAL = (` and `FORBIDDEN_FEATURE_COLUMNS = (`.
    - The Task 1 `<automated>` command exits 0 — shape `(64000, 11)`, the exact 11-name column order above, `encoder.feature_names_in_` equal to the 7 raw names, and every output column `float64`.
    - `"split"` is a member of `features.FORBIDDEN_FEATURE_COLUMNS` (asserted by the same command).
    - `dont_email_everyone/balance.py` is unmodified: `git status --short dont_email_everyone/balance.py` is empty.
    - No `assert` statement is used as a runtime gate: `grep -n '^\s*assert ' dont_email_everyone/features.py` returns nothing.
    - The module never derives features by dropping: `grep -c 'columns.drop\|difference(' dont_email_everyone/features.py` returns 0, and `grep -c 'config.PRE_TREATMENT_FEATURES' dont_email_everyone/features.py` returns at least 1.
    - No accuracy-family or I/O token anywhere in the file, docstrings included: `grep -Ec 'accuracy_score|roc_auc|classification_report|\.score\(|to_parquet|read_parquet|savefig|print\(|matplotlib|streamlit' dont_email_everyone/features.py` returns 0.
    - `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` exits 0 (the `rglob` sweep picks the new module up automatically).
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 — no existing test regressed.
  </acceptance_criteria>
  <done>`features.design_matrix` returns an 11-column all-K design matrix and its fitted encoder from `config.PRE_TREATMENT_FEATURES` alone, guarded against post-treatment leakage including `split`, in a module the purity sweep will pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add frames.assign_split and its tests in tests/test_frames.py</name>
  <files>dont_email_everyone/frames.py, tests/test_frames.py</files>
  <read_first>
    - dont_email_everyone/frames.py (lines 1-24 the module docstring including the `treatment`-never-`T` rationale; 26-42 `build_frame` — the docstring/no-mutation/dtype-statement shape to match; 43-47 `build_all_frames`)
    - dont_email_everyone/coverage.py (lines 145-182 the `seed: int = 20260902` signature and the `np.random.default_rng(seed)` stream comment)
    - dont_email_everyone/evaluation.py (lines 414-484 `_ranked_arrays` / `qini_curve` — the precisely-worded row-order docstring that 03-04 wrote rather than overclaiming invariance)
    - tests/test_frames.py (all of it — 11 existing tests, the `frames` fixture, and the assertion-message voice)
    - tests/conftest.py (lines 17-68 `raw_df` / `analysis_df` / `mens_frame` / `womens_frame`)
    - .planning/phases/04-uplift-modeling/04-RESEARCH.md §Q3b, §Q3c and §Code Examples 1
  </read_first>
  <action>
**Add `assign_split(df, seed: int = 20260902)` to `dont_email_everyone/frames.py`.** Place it after
`build_all_frames` and before `build_arm_vs_arm_frame`. Add `import numpy as np` and `import pandas
as pd` to the module's import block if they are not already present, keeping `balance.py:58-60`'s
ordering.

Behavior:
  - Return a `pd.Series` of dtype `str` (pandas 3.0's `str` dtype, the same dtype
    `tests/test_artifacts.py::STRING_COLUMNS` already asserts for `segment`/`zip_code`/`channel`),
    holding exactly the two values `"train"` and `"holdout"`, indexed identically to `df`.
  - One `np.random.default_rng(seed)` stream for the whole call. Iterate segments in **sorted**
    order so the stream's consumption order is deterministic regardless of the frame's row order or
    of pandas' `unique()` ordering. For each segment: take the integer positions of its rows,
    `rng.permutation` them, assign the first `size // 2` to `"train"` and the rest to `"holdout"`.
    Integer division means an odd segment gets one extra holdout row, which is why the measured
    counts are 10653/10654 for Mens rather than an exact half.
  - Raise `ValueError` (plain `if`/`raise`, never `assert`) if `"segment"` is absent from `df`, and
    if any segment has fewer than 2 rows so that a split is meaningless. Messages name the observed
    value, matching `evaluation._guard_treatment`'s tone.
  - Never mutate `df` — do not assign the column onto the caller's frame. Returning a Series rather
    than a modified copy is what lets `ingest.build_all` place the assignment between gate 3 and
    frame construction with one `.assign(...)` call.

Docstring requirements, all load-bearing:
  - State the returned dtype (`str`) and the two admissible values.
  - State the stratification: 50/50 **within each `segment` value**, all three arms, so every arm's
    treated and control halves stay proportional (D-06). Name that this is Radcliffe's own split on
    this dataset, which is why PITFALLS.md's train/holdout ratio table transfers without
    re-derivation.
  - **The positionality note — do not overclaim invariance.** State plainly that the assignment is
    positional: reordering `df`'s rows before calling this function produces a *different*
    assignment. State why that is acceptable rather than a defect — row order here is pinned by the
    SHA-256 gate on the vendored CSV plus DuckDB's order-preserving `SELECT *` — and mirror the
    precision 03-04 used for `qini_curve`'s row-order guarantee rather than claiming an invariance
    the function does not have.
  - State why `np.random.default_rng` and not `sklearn.model_selection.train_test_split`: NumPy's
    Generator stream is a documented stability guarantee, sklearn's RNG consumption pattern is an
    undocumented implementation detail, and this column is committed to git and regenerated by
    `pipeline ingest`, so stream stability across a future sklearn bump is the whole contract.
  - One sentence on the shared seed literal: the split, `qini_curve`'s tie-breaking shuffle and the
    permutation null all default to `20260902` but consume independent `Generator` instances, so
    there is no correlation between them (04-RESEARCH open question 4).

**Extend `tests/test_frames.py`** with an `assign_split` section, using the file's existing banner
and message style. Write these node IDs:

  1. `test_assign_split_returns_two_string_values` — dtype is `str`, `set(unique()) == {"train", "holdout"}`, length and index match `analysis_df`.
  2. `test_assign_split_counts_are_pinned` — the six measured counts as literals in a
     `groupby("segment")` cross-tab: Mens E-Mail 10653 train / 10654 holdout, No E-Mail 10653 /
     10653, Womens E-Mail 10693 / 10694. The comment above them states that these literals belong
     **here** and not inside `ingest.build_all` (04-RESEARCH Pitfall 8): a pinned count in production
     code turns a legitimate future re-seed into an undiagnosable crash, whereas a failure here is
     readable.
  3. `test_assign_split_is_deterministic_for_a_seed` — two calls at the same seed are element-wise
     equal; a call at a different seed differs on at least one row.
  4. `test_assign_split_does_not_mutate_input` — snapshot `analysis_df.shape` and
     `list(analysis_df.columns)` before and after.
  5. `test_assign_split_is_positional_and_the_docstring_says_so` — call on `analysis_df` and on a
     row-reversed copy, confirm the label assigned to a fixed identity row differs, then assert the
     docstring contains the word `positional`. This is 03-04's
     `test_curve_docstring_does_not_overclaim_invariance` pattern: the property is asserted **and**
     the prose is asserted to admit it.
  6. `test_assign_split_rejects_a_frame_without_segment` — `pytest.raises(ValueError)` whose message
     names `segment`.

Use `analysis_df` (session-scoped, already reading the committed Parquet). Do **not** assume the
committed artifact carries a `split` column — it does not yet, and plan 04-03 is what adds it.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frames.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_frames.py -k assign_split -q` exits 0 and collects at least 6 tests.
    - The six pinned counts reproduce: `.venv/Scripts/python.exe -c "import pandas as pd; from dont_email_everyone import config, frames; a=pd.read_parquet(config.PROCESSED/'analysis_table.parquet'); s=frames.assign_split(a); import collections; ct=a.assign(split=s).groupby(['segment','split']).size().to_dict(); print(ct); assert ct[('Mens E-Mail','train')]==10653 and ct[('Mens E-Mail','holdout')]==10654 and ct[('No E-Mail','train')]==10653 and ct[('No E-Mail','holdout')]==10653 and ct[('Womens E-Mail','train')]==10693 and ct[('Womens E-Mail','holdout')]==10694"` exits 0.
    - `frames.assign_split` returns pandas' `str` dtype, not `object`: the same command's Series satisfies `str(s.dtype) == "str"`.
    - The docstring admits positionality: `.venv/Scripts/python.exe -c "from dont_email_everyone import frames; assert 'positional' in frames.assign_split.__doc__"` exits 0.
    - `frames.py` uses the documented NumPy stream and not sklearn: `grep -c 'default_rng' dont_email_everyone/frames.py` returns at least 1 and `grep -c 'train_test_split\|sklearn' dont_email_everyone/frames.py` returns 0.
    - No runtime `assert` gate in the new code: `grep -n '^\s*assert ' dont_email_everyone/frames.py` returns nothing.
    - `frames.py` stays pure: `grep -Ec 'to_parquet|read_parquet|savefig|print\(|matplotlib|streamlit' dont_email_everyone/frames.py` returns 0.
    - `data/processed/` is untouched: `git status --short data/processed` is empty.
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 (full suite; the 11 pre-existing `test_frames.py` cases still pass).
  </acceptance_criteria>
  <done>`frames.assign_split` produces the seeded, segment-stratified 50/50 `str` labelling with the six measured counts pinned in the test file, a docstring that admits positionality rather than overclaiming invariance, and no mutation of the caller's frame.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/test_features.py — design matrix, leak guard, purity sweep, writes-nothing</name>
  <files>tests/test_features.py</files>
  <read_first>
    - tests/test_evaluation.py (lines 100-155 `_evaluation_source` / `_evaluation_body` / `test_evaluation_module_is_pure` — the token-concatenation purity sweep to copy; 157-195 the writes-nothing test and the comment block recording its maintenance contract)
    - tests/test_balance.py (the `_guard_no_post_treatment` negative-test cluster and the one-hot expansion assertions, for the assertion-message voice)
    - dont_email_everyone/features.py (as created in Task 1)
    - tests/conftest.py (lines 29-68 `analysis_df`, `mens_frame`, `womens_frame`)
    - .planning/phases/04-uplift-modeling/04-PATTERNS.md §"tests/test_features.py and tests/test_models.py" and §"Shared Patterns" item 1
    - .planning/phases/04-uplift-modeling/04-VALIDATION.md (the node-ID keywords `design_matrix`, `no_post_treatment`, `combined_frame`, `is_pure` are the contract this file must satisfy)
  </read_first>
  <action>
Create `tests/test_features.py`. Flat file, one test file per module, matching the repo's banner and
failure-message style.

**The purity sweep — copy `test_evaluation.py:104-155` structurally.** A `_features_source()` reading
`config.ROOT / "dont_email_everyone" / "features.py"`, a `_features_body()` joining the lines that do
not `lstrip().startswith("#")`, and `test_features_module_is_pure` looping a forbidden tuple. Assemble
**every** forbidden token by string concatenation so this test file does not trip its own check —
that is why `test_evaluation.py` writes them split. The tuple carries the same members the evaluation
sweep does: the two Parquet verbs, the builtin file opener, the figure-saving verb, the builtin
printer, the pyplot prefix, the matplotlib package name, the Streamlit alias prefix, and the four
classification-metric tokens (`accuracy_` + `score`, `roc_` + `auc`, `classification_` + `report`,
`.sco` + `re(`). The failure message states that `features.py` is a pure array-in/frame-out core and
that an accuracy-family token here would put the wrong headline metric one import away from the
uplift results (PITFALLS.md Pitfall 9, ROADMAP Phase 7 criterion 4).

Add a comment above the tuple recording the two traps, because both will bite a later agent:
`_features_body()` strips comment **lines only** — docstrings are not stripped — and the metric token
carries a leading dot, so a variable or column named `m0_score` is fine while `model.score(X, y)` is
what fails.

**`test_features_module_writes_nothing`** — copy `test_evaluation.py:175-195`. `monkeypatch.chdir`
into `tmp_path`, call **every** public callable in `features.py` (currently just `design_matrix`, on
a small hand-built frame carrying the seven feature columns), then assert
`list(tmp_path.iterdir()) == []`. Reproduce the maintenance-contract comment: a call list that
quietly stops growing turns this guarantee into a guarantee about history.

Then the design-matrix cluster. Write these node IDs:

  1. `test_design_matrix_shape_and_column_order` — on `analysis_df`: shape `(64000, 11)` and the
     exact column list in order (`zip_code_Rural`, `zip_code_Surburban`, `zip_code_Urban`,
     `channel_Multichannel`, `channel_Phone`, `channel_Web`, `recency`, `history`, `mens`, `womens`,
     `newbie`). Assert the list equals a literal, not a `set` — order is part of the contract because
     `feature_names_in_` is an ordered array.
  2. `test_design_matrix_keeps_all_k_levels` — `zip_code` contributes 3 columns and `channel`
     contributes 3, so no reference level is dropped. The comment states this is the deliberate third
     convention and names `balance.py` decision (b).
  3. `test_design_matrix_names_match_the_balance_artifact` — read `balance.parquet`, take
     `balance["covariate"].nunique()`, assert it is 11 and that the set of its distinct covariate
     names equals the set of design-matrix column names. This is the cross-readability claim in
     `features.py` decision (a), made checkable rather than asserted.
  4. `test_design_matrix_is_all_float64` — `X.dtypes.unique()` holds exactly `float64`.
  5. `test_design_matrix_encoder_carries_the_raw_feature_names` — `encoder.feature_names_in_` equals
     `config.PRE_TREATMENT_FEATURES` as a list, length 7. Add the non-vacuity assertion first: the
     attribute exists and is non-empty, so the equality is not `None == None`.
  6. `test_design_matrix_has_no_post_treatment_column` — none of `visit`, `conversion`, `spend`,
     `segment`, `treatment`, `split` appears in `X.columns`, and none appears as a prefix of any
     expanded column. Failure message quotes PITFALLS.md Pitfall 6.
  7. `test_design_matrix_rejects_a_post_treatment_feature` — `pytest.raises(ValueError)` from
     `_guard_no_post_treatment` when handed a list containing `visit`, and the message names `visit`.
     A second case for `split`, proving the `features.py`-local tuple covers the column
     `balance.POST_TREATMENT_COLUMNS` does not.
  8. `test_design_matrix_rejects_a_missing_feature` — dropping `channel` from the input frame raises
     `ValueError` naming `channel`.
  9. `test_design_matrix_combined_frame_slices_align_across_arms` — the property PITFALLS Pitfall 5
     exists for. Fit once on `analysis_df`, slice by `mens_frame.index` and by `womens_frame.index`,
     and assert both slices have identical column order and identical dtypes to each other and to
     `X`. Then assert the two slices' index sets **overlap** — the ~21,306 shared control rows are
     literally the same rows of one transformed frame, not two independently encoded copies.
 10. `test_design_matrix_does_not_mutate_input` — `analysis_df.shape` and `list(analysis_df.columns)`
     unchanged across the call.
 11. `test_design_matrix_unknown_category_raises` — build a two-row frame whose `zip_code` carries a
     level absent from the fitted encoder, call `encoder.transform` on it, and assert it raises
     rather than emitting an all-zeros row. The message in the test states why: an all-zeros
     encoding is a silently wrong prediction, not a missing value.

No test in this file may read or write anything under `data/processed/` other than through the
existing session fixtures, and no test may write a file.
  </action>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_features.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_features.py -q` exits 0 and collects at least 13 tests.
    - These node IDs all exist and pass: `.venv/Scripts/python.exe -m pytest "tests/test_features.py::test_features_module_is_pure" "tests/test_features.py::test_features_module_writes_nothing" "tests/test_features.py::test_design_matrix_shape_and_column_order" "tests/test_features.py::test_design_matrix_encoder_carries_the_raw_feature_names" "tests/test_features.py::test_design_matrix_has_no_post_treatment_column" "tests/test_features.py::test_design_matrix_combined_frame_slices_align_across_arms" -q` exits 0.
    - The VALIDATION.md keyword selectors resolve: `.venv/Scripts/python.exe -m pytest tests/test_features.py -k design_matrix -q`, `-k no_post_treatment -q`, `-k combined_frame -q` and `-k is_pure -q` each exit 0 and each collect at least one test.
    - The purity sweep is non-vacuous — it fails on a deliberately broken module. Verify by temporary edit and revert, or equivalently assert the sweep's forbidden tuple has at least 12 members: `grep -A 20 'forbidden = (' tests/test_features.py | grep -c '" *+ *"'` returns at least 12.
    - The purity sweep does not trip itself: `.venv/Scripts/python.exe -m pytest tests/test_features.py::test_features_module_is_pure -q` exits 0 with `features.py` unmodified.
    - `.venv/Scripts/python.exe -m pytest -q` exits 0 (full suite green; baseline was 286 passed).
    - `git status --short data/processed reports` is empty — this plan commits no artifact and no figure.
  </acceptance_criteria>
  <done>`tests/test_features.py` pins the 11-column all-K design matrix and its order, proves the encoder is fit once and slices align across the two arms over the shared control rows, proves the leak guard fires on `visit` and on `split`, and enforces the PC-4 purity boundary on the new module from the moment it exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed `analysis_table.parquet` → `features.design_matrix` | The only data boundary. The frame has already passed Phase 1's SHA-256 and Pandera gates; this module must not re-derive it from disk and thereby bypass them. |
| `config.PRE_TREATMENT_FEATURES` → the fitted feature space | The allowlist boundary. Everything downstream of it is what the models are allowed to see. |
| `frames.assign_split` → the committed `split` column (plan 04-03) | A value produced here is persisted to git and consumed by every later phase. Its stream must be stable across library upgrades. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `features.design_matrix` feature list | mitigate | A post-treatment column entering the design matrix is the phase's single highest-impact silent-wrong-number bug: the model would predict the outcome from itself and the uplift score would restate the treatment effect. Mitigated by deriving the list from `config.PRE_TREATMENT_FEATURES` only (never `columns.drop`), a `features.py`-local `FORBIDDEN_FEATURE_COLUMNS` tuple that additionally names `split`, and a plain `if`/`raise` guard tested positively and negatively (`test_design_matrix_rejects_a_post_treatment_feature`). ASVS V5. |
| T-04-02 | Tampering | `OneHotEncoder` unknown-level handling | mitigate | `handle_unknown="error"`. The default `"ignore"` would encode an unseen level as an all-zeros row — a confidently wrong prediction rather than a raised error. Pinned by `test_design_matrix_unknown_category_raises`. ASVS V5. |
| T-04-03 | Tampering | split stream stability | mitigate | The split is committed to git and regenerated by `pipeline ingest`. `np.random.default_rng` is used because NumPy's stream is a documented stability guarantee (NEP 19) while sklearn's RNG consumption is not; a future sklearn bump would silently re-draw the split and invalidate every committed holdout metric. Pinned by the six literal counts in `test_assign_split_counts_are_pinned` and enforced by an acceptance criterion greping for `sklearn` in `frames.py`. |
| T-04-04 | Repudiation | overclaimed invariance in `assign_split`'s docstring | mitigate | The assignment is positional, not row-order invariant. Rather than asserting an invariance the function lacks, the docstring states the positionality and its justification (row order is pinned by the SHA-256 gate plus DuckDB's order-preserving `SELECT *`), and `test_assign_split_is_positional_and_the_docstring_says_so` asserts both the behaviour and the prose. 03-04 precedent. |
| T-04-05 | Information Disclosure | `features.py` filesystem and network surface | mitigate | The module reads no files, writes no files and prints nothing. Enforced by `test_features_module_is_pure` (source token sweep, docstrings included) and `test_features_module_writes_nothing` (chdir into an empty tmp dir, assert it stays empty). `tests/test_no_network.py` picks the new module up automatically via `rglob`. ASVS V12. |
| T-04-06 | Repudiation | an accuracy-family token entering through a docstring | mitigate | The purity sweep strips comment lines only, so docstring prose is in scope, and ROADMAP Phase 7 criterion 4 greps the whole repo for exactly these tokens. Any warning about classification metrics in this module is spelled non-greppably (02-03 and 03-01 precedent) rather than dropped. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | **Zero packages are installed by this plan or by this phase.** `scikit-learn==1.9.0`, `numpy==2.4.6` and `pandas==3.0.5` are already pinned at exact `==` versions in the committed `requirements.txt`. 04-RESEARCH's Package Legitimacy Audit table is empty by construction, and PC-2's allowlist makes any addition a scope violation rather than a risk to be assessed. |

Applicable ASVS L1 categories: **V5** (input validation — the feature allowlist, the missing-column
check, `handle_unknown="error"`, the segment guard), **V12** (file handling — both modules are pure
and touch no path), **V14** (configuration — no dependency added). V2/V3/V4 do not apply: no users,
no sessions, no protected resource. V6 does not apply: this plan touches no cryptographic path.
</threat_model>

<verification>
Project venv only. Run from the repo root.

1. `.venv/Scripts/python.exe -m pytest tests/test_features.py tests/test_frames.py -q` → exit 0
2. `.venv/Scripts/python.exe -m pytest -q` → exit 0 (full suite; baseline 286 passed)
3. `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` → exit 0 (the `rglob` sweep now covers `features.py`)
4. `git status --short data/processed reports dont_email_everyone/balance.py dont_email_everyone/ingest.py dont_email_everyone/evaluation.py` → empty (no artifact regenerated, no Phase 1/2/3 module touched)
</verification>

<success_criteria>
- `features.design_matrix(df)` returns an `(64000, 11)` all-K design matrix in the pinned column order plus the fitted `ColumnTransformer`, built from `config.PRE_TREATMENT_FEATURES` alone.
- The encoder is fit once on the combined frame, and per-arm slices are proven to share column order, dtypes and the ~21,306 shared control rows.
- `_guard_no_post_treatment` raises on `visit` and on `split`, from a `features.py`-local tuple, with `balance.POST_TREATMENT_COLUMNS` left byte-identical.
- `frames.assign_split(df, seed=20260902)` returns a `str` Series reproducing the six pinned per-segment counts, is deterministic per seed, mutates nothing, and documents its positionality instead of overclaiming invariance.
- `features.py` passes a purity sweep covering I/O, rendering, Streamlit and the four accuracy-family tokens, with docstrings in scope.
- The full suite is green and no committed artifact or figure changed.
</success_criteria>

<output>
Create `.planning/phases/04-uplift-modeling/04-01-SUMMARY.md` when done.
</output>

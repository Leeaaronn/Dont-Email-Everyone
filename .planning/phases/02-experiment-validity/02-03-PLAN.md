---
phase: 02-experiment-validity
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - dont_email_everyone/ate.py
  - tests/test_ate.py
autonomous: true
requirements: [VALID-02]

must_haves:
  truths:
    - "The ATE table covers 2 arms x 3 outcomes with control base rate, absolute effect, 95% CI from HC-robust SEs, and both raw and Holm-adjusted p-values"
    - "All six effects reproduce Radcliffe's published figures, which is the check that catches a grouping bug"
    - "A covariate-adjusted estimate sits beside every unadjusted headline, and their agreement is itself the randomization evidence"
    - "A seeded bootstrap cross-check on the spend ATE agrees with the analytic interval"
    - "The spend effect is never rendered in percentage points"
  artifacts:
    - path: "dont_email_everyone/ate.py"
      provides: "Unadjusted and covariate-adjusted ATEs, Holm correction, bootstrap and winsorization robustness"
      min_lines: 150
      exports: ["ate_table", "apply_holm", "bootstrap_spend_ate", "winsorization_robustness"]
    - path: "tests/test_ate.py"
      provides: "Unit and statistical proof of VALID-02"
      min_lines: 120
  key_links:
    - from: "dont_email_everyone/ate.py"
      to: "statsmodels HC3 covariance"
      via: "smf.ols fit with cov_type"
      pattern: "cov_type=\"HC3\""
    - from: "dont_email_everyone/ate.py"
      to: "config.ARMS"
      via: "six rows generated by iterating the constant, never hand-listed"
      pattern: "config\\.ARMS"
---

<objective>
Build `ate.py`: the pure estimation module answering VALID-02. It produces the six headline unadjusted ATEs with HC3-robust 95% CIs and raw p-values (D-01, D-03), the covariate-adjusted counterparts shown side by side (D-02), the Holm-Bonferroni adjustment across exactly the six pre-registered tests, the seeded bootstrap cross-check on the spend ATE, and two labeled winsorization robustness rows.

Purpose: Reproducing Radcliffe's published figures to four decimals is the single check that catches a grouping bug. If the control group had been pooled, these numbers would be roughly 25-30% wrong with no error raised anywhere.
Output: `dont_email_everyone/ate.py` (pure — returns tidy DataFrames and dicts, writes nothing), `tests/test_ate.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-experiment-validity/02-CONTEXT.md
@.planning/phases/02-experiment-validity/02-RESEARCH.md
@.planning/phases/02-experiment-validity/02-PATTERNS.md

@dont_email_everyone/config.py
@dont_email_everyone/frames.py
@tests/test_frames.py
@tests/conftest.py

<interfaces>
<!-- Contracts available to this plan. No codebase exploration needed. -->

From dont_email_everyone/config.py:
- CONTROL: str = "No E-Mail"
- ARMS: MappingProxyType = {"mens": "Mens E-Mail", "womens": "Womens E-Mail"}
- PRE_TREATMENT_FEATURES: tuple = ("recency", "history", "mens", "womens", "zip_code", "newbie", "channel")

From tests/conftest.py (added by Plan 02-01):
- mens_frame — session fixture, pd.read_parquet(config.PROCESSED / "mens_vs_control.parquet"), shape (42613, 13)
- womens_frame — session fixture, shape (42693, 13)
- synthetic_frame(n=4000, effect=0.0, imbalance=None, seed=20260902) — factory fixture; true spend ATE equals `effect` by construction

Arm-vs-control frame schema: the 12 analysis_table columns plus `treatment` (int64, 1 = arm, 0 = control).
`zip_code` / `channel` / `segment` are pandas 3.0 `str` dtype; `history` / `spend` are float64.
</interfaces>
</context>

<environment_critical>
**Every command MUST use `.venv/Scripts/python.exe`.** Bare `python`/`pytest` resolve to system Python 3.9.13 / pandas 2.3.3 / statsmodels 0.14.6.

**`tests/test_no_network.py` greps every package `.py` file — comments and docstrings included — for `requests|urllib|httpx|aiohttp|urlretrieve|socket|ftplib|http.client` and the literal `streamlit`. Do not write those words in `ate.py`.**
</environment_critical>

<verified_targets>
Produced by executing `smf.ols(f"{y} ~ treatment", data=frame).fit(cov_type="HC3")` against the committed Parquet frames in this repo's `.venv` on 2026-09-02.

| Arm | Outcome | Coefficient | Published target | 95% CI | Raw p | Holm p |
|-----|---------|-------------|------------------|--------|-------|--------|
| mens | visit | 0.076590 | +7.66 pp | [0.06995, 0.08323] | 2.774e-113 | 1.664e-112 |
| mens | conversion | 0.006805 | +0.68 pp | [0.00500, 0.00861] | 1.473e-13 | 5.893e-13 |
| mens | spend | 0.769827 | +$0.77 | [0.48514, 1.05451] | 1.158e-07 | 3.474e-07 |
| womens | visit | 0.045233 | +4.52 pp | [0.03889, 0.05157] | 1.941e-44 | 9.704e-44 |
| womens | conversion | 0.003111 | +0.31 pp | [0.00150, 0.00472] | 1.559e-04 | 3.117e-04 |
| womens | spend | 0.424412 | +$0.42 | [0.16896, 0.67987] | 1.129e-03 | 1.129e-03 |

**Shared control base rates, identical for both arms** (this is the pooled-control canary): visit `0.10617`, conversion `0.00573`, spend `0.65279`.

**Covariate-adjusted (mens arm):** visit 0.076059 [0.06952, 0.08260]; conversion 0.006773 [0.00497, 0.00858]; spend 0.766873 [0.48250, 1.05124]. Adjustment moves every point estimate by well under 1% — exactly what a valid randomization predicts, and the strongest single sentence available for the "randomization held" argument.

**SE family relationship on mens spend, verified to 13 significant figures:**
Welch 0.14524656024868676 / HC2 0.14524656024869173 (identical) / HC1 0.14524656028088520 / HC3 0.14524996883999752 (slightly larger, conservative by design) / HC0 0.14524315173737562. When `cov_type` is set, statsmodels sets `use_t = False`, so CIs use the normal critical value 1.96.

**Bootstrap on mens spend**, `scipy.stats.bootstrap`, R=4000, `method="percentile"`, `rng=default_rng(20260902)`: `[0.4845, 1.0558]`. At seed 7: `[0.4873, 1.0550]`. Seed-to-seed wobble is ~$0.003, so the acceptance criterion is agreement within $0.02, never exact equality.

**Winsorization (expect a large move — this is real, not a bug):**
| Arm | 99.9th pct | n trimmed | n at $499 | Raw ATE | Winsorized ATE | Winsorized CI |
|---|---|---|---|---|---|---|
| mens | $233.30 | 43 | 8 | 0.7698 | **0.6493** | [0.4289, 0.8697] |
| womens | $209.91 | 43 | 6 | 0.4244 | **0.3620** | [0.1668, 0.5572] |

There are only 267 non-zero treated spenders, so the 99.9th percentile of the mostly-zero spend column is only $233 and a "99.9th percentile winsorization" actually trims 43 of the 267 real purchases — a far more aggressive intervention than it sounds. Sign, significance, and qualitative conclusion all survive.
</verified_targets>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unadjusted headline ATE table with HC3-robust intervals</name>
  <read_first>
    - dont_email_everyone/frames.py (all 46 lines — module docstring voice and the `build_all_frames` dict-comprehension-over-config.ARMS pattern to mirror)
    - dont_email_everyone/ingest.py lines 95-114 and 141-193 (module-constant style with a comment explaining the type choice; the `if`/`raise ValueError` guard pattern)
    - dont_email_everyone/config.py lines 19-32 (MappingProxyType immutability convention for constants)
    - tests/test_frames.py lines 1-42 (test docstring naming the load-bearing test; parametrize + explanatory assertion messages)
    - .planning/phases/02-experiment-validity/02-RESEARCH.md Architecture Patterns, Pattern 3 (OLS + HC3, use_t=False, HC2-equals-Welch), Common Pitfalls 9 (spend is dollars not pp), Code Examples section 3
  </read_first>
  <files>dont_email_everyone/ate.py, tests/test_ate.py</files>
  <behavior>
    - `ate_table(frames)` where `frames` is `{"mens": df, "womens": df}` returns a tidy DataFrame with exactly 6 rows
    - Columns include: `arm`, `outcome`, `unit`, `control_base_rate`, `effect`, `se`, `ci_low`, `ci_high`, `p_raw`, `n_treated`, `n_control`
    - Each of the six `effect` values matches the `<verified_targets>` table to within 1e-4
    - Each `ci_low` / `ci_high` matches the verified interval to within 1e-4
    - `control_base_rate` is 0.10617 / 0.00573 / 0.65279 for visit / conversion / spend, identical across both arms, to within 1e-4
    - `n_control` is 21306 for both arms
    - The `unit` column is `"pp"` for visit and conversion and `"$"` for spend; no row has `unit == "pp"` with `outcome == "spend"`
    - On `synthetic_frame(n=20000, effect=1.5)` the estimated spend effect is within 0.4 of 1.5 and the 95% CI contains 1.5
    - The HC3 CI on mens spend agrees with `scipy.stats.ttest_ind(equal_var=False).confidence_interval()` to 4 decimal places
    - `ate_table` does not mutate its input frames
  </behavior>
  <action>
    Write `tests/test_ate.py` first, run it, confirm failure, then implement `dont_email_everyone/ate.py`.

    Module docstring in the `frames.py` voice must state and justify:
    (a) The estimator is OLS with HC3-robust standard errors for all three outcomes including the two binary ones — a linear probability model, not logistic regression plus marginal effects (CONTEXT.md D-03). One estimator class for all three outcomes, and the OLS coefficient *is* the absolute-percentage-point effect the roadmap asks for, with no transformation step.
    (b) The headline number is the unadjusted difference in means (D-01), because Radcliffe's published figures are themselves unadjusted and reproducing them is the grouping-bug canary.
    (c) The honest HC/Welch relationship, stated precisely: HC2's standard error is algebraically identical to the Welch standard error in a two-group regression (verified to 13 significant figures); HC3 is the small-sample-conservative member of the same White family and agrees with Welch to five decimal places at n ~= 42,600 with near-equal arms. Do NOT write "HC3 is Welch" — it is adjacent to Welch, not equal to it, and a reviewer who knows the White family will notice. Also note that setting `cov_type` sets `use_t = False`, so intervals use the normal critical value 1.96, which is why HC3 and `ttest_ind` intervals differ in the sixth decimal rather than being bit-identical.

    Add a module constant `OUTCOMES` mapping outcome name to unit: visit -> "pp", conversion -> "pp", spend -> "$". Use `types.MappingProxyType`, matching `config.ARMS`, with a comment naming the reason: a shared formatter that multiplies every coefficient by 100 and appends "pp" would render the spend ATE as "+76.98pp". Visit and conversion are proportions; spend is dollars. Cite PITFALLS.md Pitfall 9.

    Implement a private `_ate_row(frame, arm_key, outcome)` returning a dict: fit `smf.ols(f"{outcome} ~ treatment", data=frame).fit(cov_type="HC3")`, read the `treatment` coefficient as `effect`, `res.bse["treatment"]` as `se`, `res.conf_int().loc["treatment"]` as the interval, `res.pvalues["treatment"]` as `p_raw`. Compute `control_base_rate` as the mean outcome among `treatment == 0` rows. Record `n_treated` and `n_control` as ints. Set `unit` from the `OUTCOMES` constant.

    Implement `ate_table(frames)` generating the six rows by iterating `config.ARMS` keys in order and then `OUTCOMES` in order — generated from the constants, never hand-listed, mirroring `build_all_frames`'s dict comprehension over `config.ARMS`. Add an `if`/`raise ValueError` guard that each supplied frame has exactly two distinct `segment` values and a `treatment` column, naming the observed values in the message (the pooled-control signature). Plain `if`/`raise`, never `assert`.

    No file I/O, no printing, no mutation of the input frames.

    `tests/test_ate.py`'s module docstring must name `test_reproduces_published_figures` as the load-bearing test and state what a naive alternative would miss: asserting the CI is non-empty, or that the effect is positive and significant, would all pass on a pooled-control table whose effects are roughly 25-30% wrong. Parametrize that test over the six (arm, outcome) pairs with an assertion message citing both the published target and the verified coefficient, and stating plainly that a mismatch here means a grouping bug, not a tolerance problem.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_ate.py -q` passes
    - `.venv/Scripts/python.exe -c "import pandas as pd; from dont_email_everyone import config, ate; f={k: pd.read_parquet(config.PROCESSED/f'{k}_vs_control.parquet') for k in config.ARMS}; t=ate.ate_table(f); print(len(t)); print(t[['arm','outcome','unit','effect','ci_low','ci_high']].round(6).to_string(index=False))"` prints 6 rows whose effects are 0.076590 / 0.006805 / 0.769827 / 0.045233 / 0.003111 / 0.424412
    - The same command shows `unit` = `$` on both spend rows and `pp` on the other four
    - `dont_email_everyone/ate.py` contains `cov_type="HC3"` and contains no `Logit`
    - `dont_email_everyone/ate.py` contains `MappingProxyType` applied to `OUTCOMES`
    - `grep -v '^#' dont_email_everyone/ate.py | grep -c "HC3 is Welch"` returns 0
    - `dont_email_everyone/ate.py` contains `raise ValueError` and no line matching `^\s*assert `
    - `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ate.py tests/test_no_network.py -q</automated>
  </verify>
  <done>`ate_table` returns the six verified ATEs with HC3 intervals, shared control base rates, and correct units, proven against the published figures by a parametrized test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Covariate-adjusted ATE and the Holm correction across exactly six tests</name>
  <read_first>
    - dont_email_everyone/ate.py (as written in Task 1 — extend it; reuse `OUTCOMES` and the guard style)
    - tests/test_ate.py (as written in Task 1)
    - dont_email_everyone/config.py lines 24-40 (PRE_TREATMENT_FEATURES is the covariate source; never a columns.drop derivation)
    - .planning/phases/02-experiment-validity/02-RESEARCH.md Code Examples sections 4 and 5, and the Don't Hand-Roll table row on Holm-Bonferroni
  </read_first>
  <files>dont_email_everyone/ate.py, tests/test_ate.py</files>
  <behavior>
    - `ate_table(frames, adjusted=True)` (or a sibling function producing the same six rows) adds `effect_adj`, `ci_low_adj`, `ci_high_adj` columns
    - Mens arm adjusted effects: visit 0.076059, conversion 0.006773, spend 0.766873 — each within 1e-4
    - For all six rows, `abs(effect_adj - effect) / abs(effect) < 0.05` — the randomization corollary
    - `apply_holm(table)` on a 6-row table returns it with `p_holm` and `reject_holm` columns added
    - The six `p_holm` values match `<verified_targets>` within a relative tolerance of 1e-3
    - Every `p_holm >= p_raw` and `reject_holm` is True for all six at alpha = 0.05
    - `apply_holm` on a table with 5 or 7 rows raises `ValueError` whose message contains the observed row count
    - The adjusted fit emits no FutureWarning or DeprecationWarning
  </behavior>
  <action>
    Write the tests first, then implement.

    Add an `adjustment_terms(frame)` helper building the right-hand side from `config.PRE_TREATMENT_FEATURES`: wrap each column whose dtype is `str` in `C(...)` and pass numerics through unchanged, joined with `" + "`. The formula API is the correct choice here specifically because `C(...)` handles pandas 3.0 `str` column expansion cleanly; the array API would require manual dummy construction.

    Extend the row builder so each of the six rows also carries the covariate-adjusted estimate: fit `smf.ols(f"{outcome} ~ treatment + {adjustment_terms(frame)}", data=frame).fit(cov_type="HC3")` and record `effect_adj`, `ci_low_adj`, `ci_high_adj` from the `treatment` coefficient. This is CONTEXT.md D-02 — the regression-adjusted ATE that REQUIREMENTS.md v2 flagged and left unscheduled, resolved into this phase as a secondary robustness column beside the unadjusted headline, never replacing it.

    The docstring on the adjusted path must state the interpretive point, because it is the strongest evidence in the phase: adjustment moves every point estimate by well under 1% and barely tightens the intervals, which is exactly what a valid randomization predicts. That agreement is a reportable finding, not a null result.

    Implement `apply_holm(table, alpha=0.05)` using `statsmodels.stats.multitest.multipletests(pvals, alpha=alpha, method="holm")`, which returns a 4-tuple `(reject, pvals_corrected, alphacSidak, alphacBonf)`. Do not hand-roll the step-down loop — the monotonicity enforcement (each adjusted p must be at least the previous) is the step people forget and it silently produces non-monotone adjusted p-values. `scipy.stats.false_discovery_control` is NOT a substitute; it implements Benjamini-Hochberg/Yekutieli only and cannot do Holm.

    Guard with an explicit `if len(table) != 6: raise ValueError(...)`. The message must name why in this repo's voice: the six tests are pre-registered, and appending a single exploratory row silently changes every adjusted p-value in the table. Cite PITFALLS.md Pitfall 11.

    Add a comment (which Plan 02-06 will carry into the report) noting that the six outcomes are strictly nested — spend > 0 implies conversion = 1 implies visit = 1 — so they are heavily positively correlated and Holm is conservative here rather than exact. Saying so pre-empts the criticism.

    Add tests: `test_adjusted_agrees_with_unadjusted` (relative difference under 5% on all six rows, with an assertion message explaining that a large divergence would indicate the randomization did not hold, not that the adjustment is wrong), `test_holm_correction` (monotone, all six reject, values match the verified Holm column), `test_holm_rejects_wrong_test_count` (5 rows and 7 rows both raise ValueError with the count in the message), and `test_adjusted_fit_is_warning_clean` running the adjusted fit under `warnings.catch_warnings` with `error` filters for FutureWarning and DeprecationWarning.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_ate.py -q` passes
    - `.venv/Scripts/python.exe -W error::FutureWarning -W error::DeprecationWarning -c "import pandas as pd; from dont_email_everyone import config, ate; f={k: pd.read_parquet(config.PROCESSED/f'{k}_vs_control.parquet') for k in config.ARMS}; t=ate.apply_holm(ate.ate_table(f, adjusted=True)); print(t[['arm','outcome','effect','effect_adj','p_holm','reject_holm']].to_string(index=False))"` exits 0, prints 6 rows, all `reject_holm` True, and mens adjusted effects 0.076059 / 0.006773 / 0.766873
    - `dont_email_everyone/ate.py` contains `method="holm"` and contains no `false_discovery_control`
    - `dont_email_everyone/ate.py` contains the literal `!= 6` guard and `raise ValueError`
    - `grep -v '^#' dont_email_everyone/ate.py | grep -c "columns.drop"` returns 0
    - `.venv/Scripts/python.exe -m pytest -q -m "not slow"` — full quick suite green
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ate.py -q</automated>
  </verify>
  <done>Every ATE row carries an adjusted estimate within 5% of the unadjusted one, Holm-adjusted p-values are computed across exactly the six pre-registered tests, and a wrong test count raises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Seeded bootstrap cross-check and labeled winsorization robustness</name>
  <read_first>
    - dont_email_everyone/ate.py (as written in Tasks 1-2)
    - tests/test_ate.py (as written in Tasks 1-2)
    - .planning/phases/02-experiment-validity/02-RESEARCH.md Verified Results section (bootstrap cross-check table and winsorization robustness table), Code Examples section 7, and the Don't Hand-Roll rows on bootstrap CI and the `rng=` vs `random_state=` convention
  </read_first>
  <files>dont_email_everyone/ate.py, tests/test_ate.py</files>
  <behavior>
    - `bootstrap_spend_ate(frame, n_resamples=4000, seed=20260902)` returns a dict with `ci_low`, `ci_high`, `n_resamples`, `seed`, `method`
    - On the mens frame the interval is within $0.02 of the analytic HC3 interval [0.48514, 1.05451]
    - Two calls with the same seed return byte-identical endpoints; a different seed shifts endpoints by less than $0.02
    - `method` is the string `"percentile"`
    - `winsorization_robustness(frames)` returns a tidy DataFrame with exactly 4 rows: 2 arms x 2 labeled variants
    - The two variants are distinguishable by a `variant` column with values `"topcode_499"` and `"pct_99_9"`
    - Mens `pct_99_9` effect is within 0.01 of 0.6493 with CI approximately [0.4289, 0.8697]; womens within 0.01 of 0.3620
    - The `topcode_499` variant leaves the effect much closer to the raw value than `pct_99_9` does
    - Every returned row carries `n_trimmed` so a reader can see how aggressive each variant was
  </behavior>
  <action>
    Write the tests first, then implement.

    `bootstrap_spend_ate(frame, n_resamples=4000, seed=20260902)`: extract the treated and control spend vectors with `.to_numpy()`, then call `scipy.stats.bootstrap((t, c), statistic, n_resamples=n_resamples, method="percentile", vectorized=True, confidence_level=0.95, rng=np.random.default_rng(seed))` where the statistic is a vectorized mean difference over the supplied `axis`. Use `rng=`, not the legacy `random_state=` — `rng` is the modern SPEC-7 name and `random_state` is on a deprecation path. Use `method="percentile"`, not `"BCa"` — BCa is 12x slower and yields a shifted interval that will not match the cross-check.

    Return the seed and the method in the result dict so `reports/validity.md` can quote them alongside the interval. The docstring must state that the seed-to-seed wobble is about $0.003, so the honest claim is agreement within $0.02, never exact equality — and that the agreement to roughly one cent at full arm size confirms the contrarian point that at this sample size the central limit theorem has kicked in and the Welch interval is fine. The report must NOT say "the t-test is wrong because spend is zero-inflated."

    `winsorization_robustness(frames)`: for each arm produce two clearly labeled rows.
    - `variant="topcode_499"`: clip spend at 499.0, the source data's own top-code. This is the near-no-op that directly addresses the censoring point in PITFALLS.md Pitfall 13.
    - `variant="pct_99_9"`: clip spend at the 99.9th percentile of that frame's spend column. This is a stress test, not the headline.
    For each, refit `smf.ols("spend ~ treatment").fit(cov_type="HC3")` on the clipped frame and record `effect`, `ci_low`, `ci_high`, plus `threshold` (the clip value) and `n_trimmed` (count of rows whose spend exceeded the threshold).

    The function docstring must set expectations explicitly so a future reader does not treat the result as a red flag: expect the mens `pct_99_9` effect to drop about 16%, from 0.7698 to 0.6493. The reason is that with only 267 non-zero treated spenders, the 99.9th percentile of the mostly-zero spend column is only $233.30, so this "99.9th percentile winsorization" actually trims 43 of the 267 real purchases — a far more aggressive intervention than the label suggests. Sign, significance, and the qualitative conclusion all survive. State that reporting only the `pct_99_9` row would invite a reader to think the headline is fragile when the actual censoring artifact is much smaller, which is why both variants are returned.

    **Do not winsorize anywhere in the headline path.** `ate_table` returns raw-spend results; winsorization is a separately labeled secondary check only.

    Add tests: `test_bootstrap_agrees_with_analytic` (endpoints within $0.02 of the HC3 interval), `test_bootstrap_is_seed_reproducible` (same seed twice gives identical endpoints; a second seed stays within $0.02), `test_winsorization_returns_two_labeled_variants` (4 rows, variant values exactly `{"topcode_499", "pct_99_9"}`), and `test_winsorization_pct_99_9_moves_the_estimate` asserting the mens value near 0.6493 with an assertion message stating this drop is an expected, documented property of the data and not a regression.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_ate.py -q` passes
    - `.venv/Scripts/python.exe -c "import pandas as pd; from dont_email_everyone import config, ate; m=pd.read_parquet(config.PROCESSED/'mens_vs_control.parquet'); b=ate.bootstrap_spend_ate(m); print(round(b['ci_low'],4), round(b['ci_high'],4), b['seed'], b['method'])"` prints approximately `0.4845 1.0558 20260902 percentile`
    - `.venv/Scripts/python.exe -c "import pandas as pd; from dont_email_everyone import config, ate; f={k: pd.read_parquet(config.PROCESSED/f'{k}_vs_control.parquet') for k in config.ARMS}; w=ate.winsorization_robustness(f); print(w[['arm','variant','threshold','n_trimmed','effect']].round(4).to_string(index=False))"` prints 4 rows including a mens `pct_99_9` effect near 0.6493 with `n_trimmed` 43
    - `dont_email_everyone/ate.py` contains `rng=` and contains no `random_state=`
    - `dont_email_everyone/ate.py` contains `method="percentile"` and contains no `"BCa"`
    - `grep -v '^#' dont_email_everyone/ate.py | grep -c "the t-test is wrong"` returns 0
    - `.venv/Scripts/python.exe -m pytest -q -m "not slow"` — full quick suite green
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_ate.py tests/test_no_network.py -q</automated>
  </verify>
  <done>The seeded bootstrap interval agrees with the analytic HC3 interval within $0.02 and is reproducible; two clearly labeled winsorization variants are returned with trim counts, and neither touches the headline path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed Parquet -> estimator | Only input; already gated by the Phase 1 Pandera schema and SHA-256 checksum |
| estimator output -> published dollar claim | This module produces the numbers the README will quote |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Repudiation | headline ATE path | mitigate | Winsorization is confined to a separately labeled `winsorization_robustness` function and never applied inside `ate_table`; both a near-no-op top-code variant and the aggressive 99.9th-percentile stress test are returned with `n_trimmed`, so no single robustness row can be read as the headline |
| T-02-09 | Tampering | pooled-control regression | mitigate | Explicit `if`/`raise ValueError` that each frame has exactly two `segment` values; `test_reproduces_published_figures` and the shared-control base-rate test (visit 0.10617, n_control 21306) are the canaries that fire if a control group is pooled |
| T-02-10 | Repudiation | Holm correction scope | mitigate | `apply_holm` raises unless the table has exactly 6 rows, so an exploratory test cannot be silently folded in and quietly change every adjusted p-value |
| T-02-11 | Repudiation | HC/Welch claim in prose | mitigate | Module docstring states HC2 equals Welch algebraically and HC3 is the conservative same-family member; the literal claim "HC3 is Welch" is grep-forbidden by an acceptance criterion |
| T-02-12 | Information Disclosure | new package module | mitigate | `tests/test_no_network.py` token grep covers `ate.py` automatically and is not excluded |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. statsmodels 0.15.0 / scipy 1.17.1 / numpy 2.4.6 already pinned and audited in Phase 1 |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q -m "not slow"` — full quick suite green
- `ate.py` performs zero file I/O: `grep -v '^#' dont_email_everyone/ate.py | grep -cE "to_parquet|read_parquet|open\(|savefig|print\("` returns 0
- Adjusted fit runs clean under `-W error::FutureWarning -W error::DeprecationWarning`
</verification>

<success_criteria>
- All six unadjusted ATEs reproduce Radcliffe's published figures to four decimals with HC3 95% CIs and raw p-values
- Control base rates are the shared-control values (visit 0.10617) with n_control 21306 for both arms
- Covariate-adjusted estimates sit beside every headline and agree within 5%
- Holm-adjusted p-values computed across exactly six pre-registered tests; a wrong count raises
- Seeded bootstrap interval on spend agrees with the analytic interval within $0.02 and is reproducible
- Two labeled winsorization variants returned with trim counts; the headline path is never winsorized
- `ate.py` writes nothing and prints nothing
</success_criteria>

<output>
Create `.planning/phases/02-experiment-validity/02-03-SUMMARY.md` when done
</output>

---
phase: 02-experiment-validity
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - dont_email_everyone/plots.py
  - dont_email_everyone/pipeline.py
  - tests/test_plots.py
  - tests/test_pipeline.py
autonomous: true
requirements: [VALID-01, VALID-02]

must_haves:
  truths:
    - "A Love plot shows every covariate's standardized mean difference against the visible plus-or-minus 0.1 acceptance band"
    - "An ATE forest plot shows all six effects with their confidence intervals"
    - "One command regenerates every Phase 2 data artifact and figure from the committed Parquet inputs"
    - "The estimation modules remain callable without touching the filesystem; only the orchestrator writes"
  artifacts:
    - path: "dont_email_everyone/plots.py"
      provides: "love_plot and ate_forest figure factories returning Figure objects"
      min_lines: 60
      exports: ["love_plot", "ate_forest"]
    - path: "dont_email_everyone/pipeline.py"
      provides: "argparse orchestrator with ingest / analyze / all subcommands; the only Phase 2 component that writes"
      min_lines: 80
      exports: ["analyze", "main"]
    - path: "tests/test_pipeline.py"
      provides: "Integration proof that analyze() writes the expected artifacts and figures into a redirected directory"
      min_lines: 60
  key_links:
    - from: "dont_email_everyone/pipeline.py"
      to: "config.PROCESSED and config.FIGURES"
      via: "mkdir plus to_parquet and savefig on ROOT-anchored constants"
      pattern: "config\\.(PROCESSED|FIGURES)"
    - from: "dont_email_everyone/pipeline.py"
      to: "ingest.build_all"
      via: "the ingest subcommand delegates rather than reimplementing"
      pattern: "build_all"
    - from: "dont_email_everyone/plots.py"
      to: "matplotlib Agg backend"
      via: "backend selected before the pyplot import"
      pattern: "matplotlib\\.use\\(\"Agg\"\\)"
---

<objective>
Build the two components that turn Phase 2's pure estimators into deliverables: `plots.py`, the figure factory that returns `Figure` objects and never renders, and `pipeline.py`, the thin argparse orchestrator that is the only Phase 2 component permitted to touch the filesystem.

Purpose: `ingest.build_all()` is contractually pinned to four gates and three artifacts by its docstring and by `tests/test_build_all.py` — extending it would break that contract. A separate orchestrator is also the shape ROADMAP Phase 7 success criterion #5 requires (`python -m dont_email_everyone.pipeline all` on a fresh clone), so building it now earns that criterion incrementally instead of retrofitting it after Phases 3-5 each add an ad-hoc entrypoint.
Output: `dont_email_everyone/plots.py`, `dont_email_everyone/pipeline.py`, `tests/test_plots.py`, `tests/test_pipeline.py`. No committed artifacts yet — Plan 02-06 generates and commits those.
</objective>

<scope_deviation_note>
`pipeline.py` is a small scope addition beyond CONTEXT.md, which left the write-entrypoint question open. RESEARCH.md Open Question 1 recommends introducing it now (~30 lines wrapping existing functions) rather than deferring, and PATTERNS.md conflict C5 resolves the same way. Recorded here explicitly so the addition is visible rather than smuggled in.
</scope_deviation_note>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-experiment-validity/02-CONTEXT.md
@.planning/phases/02-experiment-validity/02-RESEARCH.md
@.planning/phases/02-experiment-validity/02-PATTERNS.md

@dont_email_everyone/config.py
@dont_email_everyone/ingest.py
@tests/test_build_all.py

<interfaces>
<!-- Contracts produced by Plans 02-01 through 02-04. Use directly. -->

From dont_email_everyone/config.py (REPORTS/FIGURES added by Plan 02-01):
- ROOT, PROCESSED, REPORTS, FIGURES: pathlib.Path (all ROOT-anchored)
- CONTROL: str; ARMS: MappingProxyType {"mens": "Mens E-Mail", "womens": "Womens E-Mail"}
- PRE_TREATMENT_FEATURES: tuple of 7

From dont_email_everyone/frames.py (Plan 02-01):
- build_arm_vs_arm_frame(df) -> DataFrame  # (42694, 12), no treatment column

From dont_email_everyone/ingest.py (existing, do NOT modify):
- build_all() -> None  # four gates, writes analysis_table / mens_vs_control / womens_vs_control parquet
- ChecksumMismatchError(RuntimeError)

From dont_email_everyone/balance.py (Plan 02-02):
- SMD_THRESHOLD: float = 0.1
- balance_table(df) -> DataFrame  # 33 rows; cols comparison, covariate, mean_a, mean_b, smd, abs_smd
- per_covariate_pvalues(df) -> DataFrame  # 21 rows; cols comparison, covariate, test, statistic, p_value
- omnibus_lr_test(df) -> dict  # {lr_statistic, df, p_value}

From dont_email_everyone/ate.py (Plan 02-03):
- OUTCOMES: MappingProxyType {"visit": "pp", "conversion": "pp", "spend": "$"}
- ate_table(frames, adjusted=True) -> DataFrame  # 6 rows; cols arm, outcome, unit, control_base_rate,
    effect, se, ci_low, ci_high, p_raw, n_treated, n_control, effect_adj, ci_low_adj, ci_high_adj
- apply_holm(table, alpha=0.05) -> DataFrame  # adds p_holm, reject_holm; raises unless len == 6
- bootstrap_spend_ate(frame, n_resamples=4000, seed=20260902) -> dict
- winsorization_robustness(frames) -> DataFrame  # 4 rows; variant in {topcode_499, pct_99_9}

From dont_email_everyone/coverage.py (Plan 02-04):
- CELL_SIZES: tuple = (42613, 4000, 2000, 1000, 400)
- empirical_coverage_table(frame, cells=CELL_SIZES, n_replicates=4000, seed=20260902) -> DataFrame

Existing write pattern to copy verbatim in shape (ingest.py lines 195-203):
  config.PROCESSED.mkdir(parents=True, exist_ok=True)
  validated.to_parquet(config.PROCESSED / "analysis_table.parquet", index=False)
  print(f"[done] wrote 3 parquet artifacts to {config.PROCESSED}")

Existing progress-print convention (ingest.py lines 167-193): numbered stage prints carrying the observed shape,
  e.g. "[gate 1/4] checksum verified: ..." / "[gate 2/4] loaded: shape=..."

Existing test redirection pattern (tests/test_build_all.py lines 17-22):
  processed = tmp_path / "processed"
  monkeypatch.setattr(config, "PROCESSED", processed)
  assert not processed.exists()
  build_all()
</interfaces>
</context>

<environment_critical>
**Every command MUST use `.venv/Scripts/python.exe`.** Bare `python`/`pytest` resolve to system Python 3.9.13.

**`tests/test_no_network.py` greps every package `.py` file — comments and docstrings included — for `requests|urllib|httpx|aiohttp|urlretrieve|socket|ftplib|http.client` AND for the literal string `streamlit`.** This is a live hazard in this plan specifically: the natural docstring sentence "Phase 6's Streamlit app will import this module" would fail the suite. Refer to Phase 6 by phase number only, never by framework name.

**Existing artifact constraint:** `tests/test_artifacts.py::test_artifacts_readable_without_duckdb_or_pandera` spawns a clean subprocess and calls `pd.read_parquet` on every `*.parquet` in `config.PROCESSED`. Any Parquet this orchestrator writes must load with pandas + pyarrow alone — primitive dtypes only, no nested or extension types, `index=False`. A tuple-valued confidence interval must be stored as two float columns.
</environment_critical>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: plots.py — Love plot and ATE forest plot figure factories</name>
  <read_first>
    - dont_email_everyone/frames.py (all 46 lines — module docstring voice)
    - dont_email_everyone/balance.py and dont_email_everyone/ate.py (the exact column names of the frames these functions consume — do not guess)
    - tests/test_build_all.py lines 17-22 (the tmp_path shape for a figure smoke test)
    - .planning/phases/02-experiment-validity/02-RESEARCH.md Code Examples section 6 (Love plot, including the mandatory x-limit) and Anti-Patterns to Avoid (never plt.show, caller owns savefig and close)
    - .planning/phases/02-experiment-validity/02-PATTERNS.md `No Analog Found` table row for plots.py
  </read_first>
  <files>dont_email_everyone/plots.py, tests/test_plots.py</files>
  <behavior>
    - `love_plot(balance_df, threshold=0.1)` returns a `matplotlib.figure.Figure` and calls no rendering function
    - The returned axes have explicit x limits of (-0.12, 0.12) so the plus-and-minus threshold lines are visible
    - The axes contain two dashed threshold lines at -0.1 and +0.1 and a legend with one entry per comparison (3 entries)
    - Every covariate in the input frame appears as a y tick label, including the source data's misspelled `zip_code_Surburban`
    - The x axis label names the standardized mean difference and cites Austin (2009)
    - `ate_forest(ate_df)` returns a Figure with one row per ATE and a horizontal error bar spanning `ci_low` to `ci_high`
    - `ate_forest` does not render the spend effect on the same numeric axis as the two proportion outcomes without labeling units
    - Calling either function leaves no figure registered in pyplot's global state that the caller did not receive
    - Saving either figure to a tmp_path produces a file larger than 5,000 bytes
  </behavior>
  <action>
    Write `tests/test_plots.py` first, then implement `dont_email_everyone/plots.py`.

    Set the backend before the pyplot import: `matplotlib.use("Agg")` on the line immediately preceding `import matplotlib.pyplot as plt`, with a comment stating this makes figure generation work headless and in CI. Import order matters — selecting the backend after pyplot is imported has no effect.

    Module docstring must state the boundary this module lives on, in the `frames.py` voice: these functions return `Figure` objects and never call a rendering or display function; the caller owns both `savefig` and `close`. Name the failure mode: a module that renders or leaves figures open accumulates open figures and emits a resource warning after 20, and it cannot be reused by a later phase that needs the same figure in a different output context. Refer to that later phase by number, never by framework name.

    `love_plot(balance_df, threshold=0.1)`: derive a stable covariate ordering with `dict.fromkeys` over the `covariate` column so all three comparisons share a y position per covariate. Scatter each comparison group with a distinct marker and a legend entry taken from the `comparison` column. Draw a neutral vertical line at 0 and dashed threshold lines at both `-threshold` and `+threshold`.

    **Set `ax.set_xlim(-0.12, 0.12)` explicitly.** This is mandatory, not cosmetic: the maximum observed absolute SMD in this data is 0.0169, so an auto-scaled x axis places the plus-and-minus 0.1 threshold lines outside the visible range and the plot degenerates into a meaningless vertical smear. Seeing how far inside the acceptance band every point sits is the entire purpose of a Love plot here. Add an inline comment at that line saying so.

    Label the x axis as the standardized mean difference with the Austin (2009) attribution, and title the figure with the acceptance criterion stated as `|SMD| < {threshold}` so the criterion is visible on the figure itself rather than only in prose. Accept the threshold as a parameter defaulting to `balance.SMD_THRESHOLD`'s value so the figure and the test agree by construction.

    Consider a display-label mapping for tick labels, but **never alter the underlying data**: the misspelling `zip_code_Surburban` is real, present in the source data, and asserted literally by the Phase 1 Pandera schema. If a mapping is added it must be presentation-only and the raw name must still appear in the balance artifact.

    `ate_forest(ate_df)`: one row per (arm, outcome) with a point marker at `effect` and a horizontal error bar from `ci_low` to `ci_high`, a vertical reference line at zero, and y labels combining arm and outcome. Because spend is in dollars while visit and conversion are proportions, either use separate subplots grouped by the `unit` column or annotate each row's unit in its label — a single shared numeric axis with no unit labeling would render the spend effect as if it were 77 percentage points. Whichever approach is chosen, the `unit` column must drive it rather than a hardcoded outcome name list.

    Tests in `tests/test_plots.py`: assert the return type is a `Figure`; assert `ax.get_xlim()` equals `(-0.12, 0.12)` for the Love plot with a message explaining that auto-scaling hides the threshold lines at max |SMD| = 0.0169; assert three legend entries; assert the y tick labels include `zip_code_Surburban`; assert `savefig` into `tmp_path` produces a file over 5,000 bytes and that `plt.close(fig)` then leaves `plt.get_fignums()` empty; assert `plots.py` source contains no display call. Use the real committed frames via the `analysis_df` / `mens_frame` fixtures plus the Plan 02-02 and 02-03 estimators to build realistic inputs.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_plots.py -q` passes
    - `grep -v '^#' dont_email_everyone/plots.py | grep -cE "plt\.show|st\.pyplot"` returns 0
    - `dont_email_everyone/plots.py` contains `matplotlib.use("Agg")` on a line before `import matplotlib.pyplot`
    - `dont_email_everyone/plots.py` contains `set_xlim(-0.12, 0.12)`
    - `.venv/Scripts/python.exe -c "import matplotlib; matplotlib.use('Agg'); import matplotlib.pyplot as plt, pandas as pd; from dont_email_everyone import config, balance, plots; d=pd.read_parquet(config.PROCESSED/'analysis_table.parquet'); f=plots.love_plot(balance.balance_table(d)); print(type(f).__name__, f.axes[0].get_xlim()); plt.close(f); print(plt.get_fignums())"` prints `Figure (-0.12, 0.12)` and `[]`
    - `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` passes (confirms no framework name leaked into a docstring)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_plots.py tests/test_no_network.py -q</automated>
  </verify>
  <done>`love_plot` and `ate_forest` return Figure objects with the threshold band visible at fixed x limits, render nothing, and leave no open figures once the caller closes them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: pipeline.py — argparse orchestrator with ingest / analyze / all subcommands</name>
  <read_first>
    - dont_email_everyone/ingest.py lines 141-207 in full (the `build_all` docstring enumerating each gate and its failure mode, the numbered progress prints, the write block, and the bare `__main__` guard — copy all four in shape)
    - tests/test_build_all.py in full (the tmp_path + monkeypatch redirection pattern, the `assert not processed.exists()` precondition, and the per-artifact shape loop to extend)
    - dont_email_everyone/config.py (as modified by Plan 02-01 — PROCESSED, REPORTS, FIGURES)
    - dont_email_everyone/balance.py, ate.py, coverage.py, plots.py (exact function signatures and returned column names — do not guess)
    - .planning/phases/02-experiment-validity/02-RESEARCH.md Open Questions 1 and 2, and Common Pitfalls 8 (artifacts must load with pandas alone)
  </read_first>
  <files>dont_email_everyone/pipeline.py, tests/test_pipeline.py</files>
  <behavior>
    - `analyze()` reads the three committed Parquet inputs from `config.PROCESSED` and writes exactly four data artifacts and two figures
    - Data artifacts: `balance.parquet` (33 rows), `ate.parquet` (6 rows), `coverage.parquet` (5 rows), `ate.json`
    - `balance.parquet` also carries the per-covariate p-value information or a companion table; the omnibus result is carried in `ate.json` or a dedicated column — whichever, it must be reachable without rerunning the estimators
    - Figures: `reports/figures/love_plot.png` and `reports/figures/ate_forest.png`, each larger than 5,000 bytes
    - Every Parquet is written with `index=False`; none contains a column named `index`
    - Every written Parquet loads in a clean subprocess with pandas + pyarrow only
    - `ate.json` is valid JSON containing the six point estimates with their intervals, the bootstrap block (interval, seed, method, n_resamples), and the omnibus result
    - With `config.PROCESSED`, `config.REPORTS` and `config.FIGURES` monkeypatched to a tmp_path that does not exist, `analyze()` creates the directories and writes into them
    - `python -m dont_email_everyone.pipeline --help` lists three subcommands: `ingest`, `analyze`, `all`
    - The `ingest` subcommand delegates to `ingest.build_all()` and does not reimplement any gate
    - `analyze()` prints numbered progress lines carrying the observed shape of each stage
  </behavior>
  <action>
    Write `tests/test_pipeline.py` first, then implement `dont_email_everyone/pipeline.py`.

    Module docstring must mirror `ingest.build_all`'s in structure: name each subcommand, state exactly what it writes, and state that no step swallows an exception — a failure in any estimator propagates rather than producing a partial artifact set. Add the boundary statement: this is the only Phase 2 module that touches the filesystem; `balance.py`, `ate.py`, `coverage.py` and `plots.py` are pure so they stay callable on arbitrary in-memory frames in tests.

    Implement `analyze()`:
    - Read `analysis_table.parquet`, `mens_vs_control.parquet`, `womens_vs_control.parquet` from `config.PROCESSED` via pathlib `/` on the constant. Never a CWD-relative string; never `load_raw` — reading the raw CSV here would be a provenance bypass around the Phase 1 checksum gate.
    - Stage 1: `balance.balance_table(analysis)` plus `balance.per_covariate_pvalues(analysis)` plus `balance.omnibus_lr_test(analysis)`. Note that `balance_table` internally covers all three pairwise comparisons including mens-vs-womens via `frames.build_arm_vs_arm_frame`; do not persist the mens-vs-womens frame as a fourth Parquet — it is one line to reconstruct from `analysis_table.parquet`, no later phase consumes it, and committing it adds a staleness surface for no benefit.
    - Stage 2: `ate.apply_holm(ate.ate_table(frames, adjusted=True))`, plus `ate.bootstrap_spend_ate(mens)` and `ate.winsorization_robustness(frames)`.
    - Stage 3: `coverage.empirical_coverage_table(mens)`.
    - Stage 4: build both figures via `plots`, then `fig.savefig(path, dpi=150)` followed by `plt.close(fig)` **in the orchestrator, never in `plots.py`**.

    Write pattern, copied in shape from `ingest.py` lines 195-203: `config.PROCESSED.mkdir(parents=True, exist_ok=True)` and `config.FIGURES.mkdir(parents=True, exist_ok=True)` (which also creates `config.REPORTS` as a parent), then `to_parquet(config.PROCESSED / "<name>.parquet", index=False)` for each table. `index=False` is load-bearing: `tests/test_build_all.py` asserts `"index" not in analysis.columns` and RESEARCH Pitfall 8 requires the new files to load with pandas + pyarrow alone.

    Artifact naming, resolving the CONTEXT.md D-05 discretion item: **Parquet for the tabular results** (`balance.parquet`, `ate.parquet`, `coverage.parquet`) so they participate in the existing `test_artifacts_readable_without_duckdb_or_pandera` glob check and match Phase 1's convention, **plus a small `ate.json`** carrying only the scalar headline block for the future manifest and the Phase 7 README to quote without a Parquet read. Two formats, two distinct jobs. Store any interval as two float columns (`ci_low`, `ci_high`), never a tuple-valued column — a nested dtype would fail the pandas-alone read.

    Decide and document where the per-covariate p-values, the winsorization rows, and the omnibus result land. Keep the total artifact count small: fold the per-covariate p-values into `balance.parquet` as additional rows distinguished by a `metric` column (or as extra columns if the grain allows), fold the winsorization rows into `ate.parquet` distinguished by a `variant` column with `"raw"` for the six headline rows, and put the omnibus result and the bootstrap block into `ate.json`. Whatever is chosen, state it in the module docstring and make every number in the eventual report reachable from a committed artifact — Plan 02-06 and ROADMAP Phase 7 criterion #2 both depend on that.

    Progress prints follow the existing numbered-stage convention with the observed shape, for example `[1/4] balance table: shape=(33, 6)`, ending with a `[done]` line naming the artifact count and the destination directory.

    Add `main()` with `argparse` subcommands `ingest`, `analyze`, and `all`. `ingest` calls `ingest.build_all()` and nothing else — **do not extend `build_all()` itself**; its docstring commits it to four gates and three outputs and `tests/test_build_all.py` asserts that contract. `all` runs `build_all()` then `analyze()`. Close with the repo's bare entrypoint guard calling `main()`.

    Tests in `tests/test_pipeline.py`, copying `test_build_all.py`'s shape exactly: monkeypatch `config.PROCESSED`, `config.REPORTS` and `config.FIGURES` onto tmp_path subdirectories, seed the tmp `PROCESSED` with copies of the three committed input Parquets, assert the output directories do not exist beforehand, call `analyze()`, then loop over the expected artifacts asserting file presence and row counts (33 / 6 / 5), assert `"index" not in df.columns` for each, assert both PNGs exceed 5,000 bytes, assert `ate.json` parses and contains all six effects, and assert a clean-subprocess `pd.read_parquet` over the tmp directory succeeds without importing duckdb or pandera. Add a CLI test asserting `--help` output names all three subcommands.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_pipeline.py -q` passes
    - `.venv/Scripts/python.exe -m dont_email_everyone.pipeline --help` exits 0 and its output contains `ingest`, `analyze`, and `all`
    - `dont_email_everyone/pipeline.py` contains `index=False` on every `to_parquet` call and contains `plt.close(`
    - `grep -v '^#' dont_email_everyone/pipeline.py | grep -c "load_raw"` returns 0
    - `dont_email_everyone/ingest.py` is byte-identical to its state before this plan (`git diff --stat dont_email_everyone/ingest.py` is empty)
    - `dont_email_everyone/pipeline.py` contains `config.PROCESSED` and `config.FIGURES` and contains no string literal starting with `"data/` or `"reports/`
    - `.venv/Scripts/python.exe -m pytest -q -m "not slow"` — full quick suite green
    - `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` passes
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_pipeline.py tests/test_no_network.py -q</automated>
  </verify>
  <done>`pipeline.py analyze` writes four data artifacts and two figures into monkeypatched directories, `ingest` delegates to the untouched `build_all()`, and every Parquet is index-free and pandas-readable in a clean subprocess.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator -> filesystem | The only Phase 2 component that writes; every output path originates here |
| committed Parquet -> orchestrator | Inputs already gated by the Phase 1 SHA-256 checksum and Pandera schema |
| artifact -> Phase 6 runtime | Artifacts written here are read at serve time by a later phase with a restricted dependency set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-18 | Tampering | output path construction | mitigate | Every write derives from `config.PROCESSED` or `config.FIGURES` via pathlib `/`; no CWD-relative literal and no string concatenation. Enforced by the grep acceptance criterion forbidding `"data/` and `"reports/` string literals |
| T-02-19 | Tampering / Repudiation | provenance bypass | mitigate | `analyze()` reads committed Parquet only and never imports `load_raw`, so no Phase 2 code path reaches the raw CSV outside the Phase 1 checksum gate. Enforced by a grep acceptance criterion |
| T-02-20 | Tampering | `ingest.build_all` contract | mitigate | The `ingest` subcommand delegates; `git diff --stat dont_email_everyone/ingest.py` must be empty, so the four-gate contract and `tests/test_build_all.py` remain intact |
| T-02-21 | Denial of Service | figure handles | mitigate | The orchestrator pairs every `savefig` with `plt.close(fig)`; `plots.py` never renders, and a test asserts `plt.get_fignums()` is empty after close |
| T-02-22 | Information Disclosure | new package modules | mitigate | `tests/test_no_network.py` token grep covers `plots.py` and `pipeline.py` automatically; the literal `streamlit` is a live hazard in these docstrings and is grep-enforced. No exclusions added |
| T-02-23 | Tampering | artifact readability at serve time | mitigate | Every Parquet uses primitive dtypes and `index=False`; intervals stored as two float columns, never a nested type. Verified by a clean-subprocess `pd.read_parquet` test that asserts duckdb and pandera stay unimported |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | Zero installs. matplotlib 3.11.1 already pinned and audited in Phase 1; `argparse` and `json` are stdlib |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` — full suite green
- `.venv/Scripts/python.exe -m dont_email_everyone.pipeline --help` exits 0 listing three subcommands
- `git diff --stat dont_email_everyone/ingest.py` is empty
- No `data/processed/` or `reports/` files are committed by this plan — Plan 02-06 owns artifact generation
</verification>

<success_criteria>
- `plots.love_plot` and `plots.ate_forest` return Figure objects, render nothing, and fix the Love plot x limits at (-0.12, 0.12)
- `pipeline.analyze()` writes `balance.parquet`, `ate.parquet`, `coverage.parquet`, `ate.json`, `love_plot.png` and `ate_forest.png` into ROOT-anchored config paths
- `pipeline main()` exposes `ingest`, `analyze` and `all`; `ingest` delegates to the unmodified `build_all()`
- Every Parquet is `index=False` and loads with pandas + pyarrow alone in a clean subprocess
- Every figure handle is closed by the orchestrator
- Zero new dependencies; `requirements.txt` unchanged
</success_criteria>

<output>
Create `.planning/phases/02-experiment-validity/02-05-SUMMARY.md` when done
</output>

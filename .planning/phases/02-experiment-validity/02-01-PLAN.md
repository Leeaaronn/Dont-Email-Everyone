---
phase: 02-experiment-validity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dont_email_everyone/config.py
  - dont_email_everyone/frames.py
  - tests/test_config.py
  - tests/test_frames.py
  - tests/conftest.py
autonomous: true
requirements: [VALID-01, VALID-02]

must_haves:
  truths:
    - "Every Phase 2 write path resolves from a ROOT-anchored config constant, so no output can land outside the repo"
    - "The mens-vs-womens comparison frame can be built by positive membership without any caller writing an isin() call"
    - "Balance and ATE tests can load the committed Parquet artifacts and a synthetic known-effect frame without calling load_raw()"
  artifacts:
    - path: "dont_email_everyone/config.py"
      provides: "REPORTS and FIGURES path constants anchored on ROOT"
      contains: "REPORTS ="
    - path: "dont_email_everyone/frames.py"
      provides: "build_arm_vs_arm_frame positive-membership helper"
      contains: "def build_arm_vs_arm_frame"
    - path: "tests/conftest.py"
      provides: "analysis_df / mens_frame / womens_frame session fixtures and a synthetic_frame factory"
      contains: "def synthetic_frame"
  key_links:
    - from: "tests/conftest.py"
      to: "config.PROCESSED"
      via: "pd.read_parquet of the committed Phase 1 artifacts"
      pattern: "read_parquet\\(config\\.PROCESSED"
    - from: "dont_email_everyone/frames.py"
      to: "config.ARMS"
      via: "positive membership isin on both arm labels"
      pattern: "isin\\(\\[config\\.ARMS"
---

<objective>
Lay the foundation every other Phase 2 plan builds on: the two new ROOT-anchored path constants (`REPORTS`, `FIGURES`) required by CONTEXT.md D-06, the `build_arm_vs_arm_frame` helper that keeps the third (mens-vs-womens) balance comparison inside the one module whose docstring already carries the positive-membership rule, and the pytest fixtures that let Plans 02/03/04 assert against committed Parquet and against synthetic known-effect data.

Purpose: Plans 02, 03 and 04 run in parallel in Wave 2 and all three need these primitives. Building them once here removes the only shared-file conflict in the phase.
Output: Modified `config.py`, `frames.py`, `tests/test_config.py`, `tests/test_frames.py`, `tests/conftest.py`. No new data artifacts, no new modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-experiment-validity/02-CONTEXT.md
@.planning/phases/02-experiment-validity/02-PATTERNS.md

@dont_email_everyone/config.py
@dont_email_everyone/frames.py
@tests/conftest.py
@tests/test_config.py
@tests/test_frames.py

<interfaces>
<!-- Contracts this plan extends. Executor should use these directly. -->

From dont_email_everyone/config.py (existing):
- ROOT: pathlib.Path — repo root, parents[1] of the package dir
- RAW_CSV, CHECKSUM_FILE, PROCESSED: pathlib.Path — all built with `/` on ROOT
- CONTROL: str = "No E-Mail"
- ARMS: types.MappingProxyType = {"mens": "Mens E-Mail", "womens": "Womens E-Mail"}
- PRE_TREATMENT_FEATURES: tuple = ("recency", "history", "mens", "womens", "zip_code", "newbie", "channel")

From dont_email_everyone/frames.py (existing):
- build_frame(df, arm_label: str) -> DataFrame — copy restricted to arm_label + CONTROL, adds int64 `treatment`
- build_all_frames(df) -> dict — {"mens": frame, "womens": frame}

From tests/conftest.py (existing):
- raw_df — session fixture, load_raw() of the 64,000-row vendored CSV
- corrupt — function-scoped factory fixture returning a corrupted copy; unknown kind raises ValueError
- multi_corrupt — frame with three simultaneous violations

Committed Phase 1 artifacts (read-only inputs):
- data/processed/analysis_table.parquet — (64000, 12)
- data/processed/mens_vs_control.parquet — (42613, 13), control count 21306
- data/processed/womens_vs_control.parquet — (42693, 13), control count 21306
</interfaces>
</context>

<environment_critical>
**Every command in this plan MUST use `.venv/Scripts/python.exe` explicitly.** Bare `python`/`pytest` on this machine resolve to system Python 3.9.13 with pandas 2.3.3 / statsmodels 0.14.6, where `tests/test_artifacts.py::test_artifact_dtypes_survive_round_trip` fails outright.

**`tests/test_no_network.py` greps every `dont_email_everyone/**/*.py` file — including comments and docstrings — for the tokens `requests|urllib|httpx|aiohttp|urlretrieve|socket|ftplib|http.client` and for the literal string `streamlit`. Do not write any of those words anywhere in package code, even in prose.**
</environment_critical>

<tasks>

<task type="auto">
  <name>Task 1: Add REPORTS and FIGURES path constants to config.py</name>
  <read_first>
    - dont_email_everyone/config.py (all 41 lines — the module docstring constrains what may be added: "Module-level constants only — no functions, no I/O, no side effects")
    - tests/test_config.py (the `test_paths_are_cwd_independent` shape to extend)
    - .planning/phases/02-experiment-validity/02-PATTERNS.md section `dont_email_everyone/config.py (modify — add REPORTS / FIGURES)`
  </read_first>
  <files>dont_email_everyone/config.py, tests/test_config.py</files>
  <action>
    In `config.py`, immediately after the existing `PROCESSED = ROOT / "data" / "processed"` line, add two constants: `REPORTS = ROOT / "reports"` and `FIGURES = REPORTS / "figures"`. Both must be built with pathlib `/` on a ROOT-anchored constant — never a CWD-relative string, never `os.path.join`, never string concatenation.

    Above the two new lines, add a comment in the exact style of the existing D-09 comment on lines 15-16: state that `reports/` (figures under `reports/figures/`, the write-up at `reports/validity.md`) is CONTEXT.md D-06, a locked user decision that establishes the project-wide reports convention, and that Phase 7's README will embed these PNGs directly.

    Do NOT call `mkdir` here. `config.py`'s docstring forbids side effects; directory creation belongs to the orchestrator (precedent: `ingest.py` line 195 does `config.PROCESSED.mkdir(parents=True, exist_ok=True)` inside `build_all()`).

    In `tests/test_config.py`, extend `test_paths_are_cwd_independent` with four assertions in the existing style: `config.REPORTS.is_absolute()`, `config.REPORTS.as_posix().endswith("reports")`, `config.FIGURES.is_absolute()`, `config.FIGURES.as_posix().endswith("reports/figures")`. Use `.as_posix()` — a raw string compare breaks on Windows separators.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -c "from dont_email_everyone import config; print(config.REPORTS.as_posix(), config.FIGURES.as_posix())"` prints two absolute paths ending in `/reports` and `/reports/figures`
    - `dont_email_everyone/config.py` contains the literal token `REPORTS =` and the literal token `FIGURES =`
    - `dont_email_everyone/config.py` contains no `def `, no `mkdir`, and no `import` beyond the existing `pathlib` and `types`
    - `dont_email_everyone/config.py` contains the string `D-06` in a comment
    - `.venv/Scripts/python.exe -m pytest tests/test_config.py -q` passes with 4 tests
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_config.py -q</automated>
  </verify>
  <done>`config.REPORTS` and `config.FIGURES` exist as absolute ROOT-anchored Paths, are asserted cwd-independent by test, and `config.py` is still constants-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add build_arm_vs_arm_frame to frames.py</name>
  <read_first>
    - dont_email_everyone/frames.py (all 46 lines — copy the module docstring voice, the inline decision comment at lines 33-37, and the `.copy()` no-mutation discipline)
    - tests/test_frames.py (the module docstring pattern and the explanatory-assertion-message pattern)
    - .planning/phases/02-experiment-validity/02-RESEARCH.md Common Pitfalls section, Pitfall 4 ("The mens-vs-womens comparison frame does not exist yet")
  </read_first>
  <files>dont_email_everyone/frames.py, tests/test_frames.py</files>
  <behavior>
    - `build_arm_vs_arm_frame(df)` on the 64,000-row analysis table returns a frame of shape (42694, 12)
    - The returned frame's `segment` value counts are exactly `{"Womens E-Mail": 21387, "Mens E-Mail": 21307}`
    - The returned frame contains NO `treatment` column — there is no control arm and no effect to estimate here
    - The returned frame contains zero rows where `segment == config.CONTROL`
    - Calling it does not mutate the input frame (shape and column list unchanged)
  </behavior>
  <action>
    Write the tests in `tests/test_frames.py` first, then implement.

    Add `build_arm_vs_arm_frame(df)` to `frames.py`, below `build_all_frames`. It selects rows by positive membership on both arm labels read from `config.ARMS`: mask on `df["segment"].isin([config.ARMS["mens"], config.ARMS["womens"]])`, then return `df.loc[mask].copy()`. Never build it as `df[df.segment != config.CONTROL]` — that is the exclusion form the module docstring forbids and it would silently include the control arm if a fourth segment value ever appeared.

    The function docstring must state, in this module's established voice: the frame exists solely for the third pairwise balance comparison required by ROADMAP Phase 2 success criterion #1; it deliberately carries NO `treatment` column because neither arm is a control and no treatment effect is estimable from it; and the verified shape is (42694, 12) with Womens 21387 / Mens 21307.

    Add an inline comment at the `isin` line repeating the positive-membership rule at the point where it would be violated — the same shape as the existing comment at `frames.py` lines 33-37.

    In `tests/test_frames.py`, add four tests: `test_arm_vs_arm_frame_shape` (asserts (42694, 12) with an explanatory message naming 64000 and 42693 as the wrong-construction signatures), `test_arm_vs_arm_frame_has_no_control_rows` (asserts zero rows equal `config.CONTROL` and `segment.nunique() == 2`), `test_arm_vs_arm_frame_has_no_treatment_column` (asserts `"treatment" not in frame.columns`, message: this frame has no control arm, so a treatment indicator here would be meaningless and would invite an ATE to be estimated from it), and extend the existing no-mutation test coverage with `test_arm_vs_arm_frame_does_not_mutate_input`. Use `raw_df` as the input fixture, matching the existing tests in this file.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/test_frames.py -q` passes, with at least 4 new test node IDs containing `arm_vs_arm`
    - `.venv/Scripts/python.exe -c "from dont_email_everyone.ingest import load_raw; from dont_email_everyone.frames import build_arm_vs_arm_frame as f; d=f(load_raw()); print(d.shape, sorted(d['segment'].unique()), 'treatment' in d.columns)"` prints `(42694, 12)`, the two arm labels, and `False`
    - `dont_email_everyone/frames.py` contains `isin([config.ARMS["mens"], config.ARMS["womens"]])` and does NOT contain any `!=` comparison against `config.CONTROL`
    - `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` still passes (no forbidden token introduced)
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/test_frames.py tests/test_no_network.py -q</automated>
  </verify>
  <done>`build_arm_vs_arm_frame` exists in `frames.py`, is built by positive membership, carries no `treatment` column, and is proven at shape (42694, 12) by test.</done>
</task>

<task type="auto">
  <name>Task 3: Add processed-Parquet and synthetic-frame fixtures to conftest.py</name>
  <read_first>
    - tests/conftest.py (all 69 lines — copy the session-scoped-fixture docstring shape at lines 14-23 and the factory-fixture shape at lines 26-33, including the `else: raise ValueError(f"Unknown ...: {kind!r}")` terminator)
    - dont_email_everyone/config.py (as modified by Task 1 — PROCESSED, PRE_TREATMENT_FEATURES, ARMS, CONTROL)
    - .planning/phases/02-experiment-validity/02-PATTERNS.md section `tests/conftest.py (modify — add synthetic fixtures)`
  </read_first>
  <files>tests/conftest.py</files>
  <action>
    Add three session-scoped real-data fixtures that read committed Parquet rather than calling `load_raw()`: `analysis_df` returning `pd.read_parquet(config.PROCESSED / "analysis_table.parquet")`, `mens_frame` returning `pd.read_parquet(config.PROCESSED / "mens_vs_control.parquet")`, and `womens_frame` returning `pd.read_parquet(config.PROCESSED / "womens_vs_control.parquet")`. Each docstring states the same scope rationale the existing `raw_df` fixture uses (loaded once per session; consumers that mutate must `.copy()` first) plus the Phase 2 reason: balance and ATE assertions must run against the *committed* artifacts, so a stale or pooled artifact on disk is caught rather than masked by a fresh rebuild.

    Add a function-scoped factory fixture `synthetic_frame` returning a callable with signature `_synthetic_frame(n=4000, effect=0.0, imbalance=None, seed=20260902)`. It builds a DataFrame with exactly the columns `config.PRE_TREATMENT_FEATURES` plus `segment`, `treatment`, `visit`, `conversion`, `spend`, using `numpy.random.default_rng(seed)`:
    - `treatment`: alternating/randomized int64 0/1 with `n // 2` treated
    - `segment`: `config.ARMS["mens"]` where treatment is 1, `config.CONTROL` where 0 (so the frame is shaped exactly like a real arm-vs-control frame)
    - `recency` (int64 1-12), `history` (float64, e.g. gamma or lognormal draw), `mens`/`womens`/`newbie` (int64 0/1), `zip_code` (str drawn from "Rural"/"Surburban"/"Urban"), `channel` (str drawn from "Phone"/"Web"/"Multichannel") — all drawn identically in both arms so the frame is balanced by construction
    - `spend`: base draw plus `effect` added to treated rows, so the true ATE equals `effect` by construction
    - `visit` and `conversion`: int64 0/1 derived so the frame is structurally realistic; no effect requirement on these
    - `imbalance`: when not None, a string naming which covariate to shift in the treated arm. Support at least `"recency"` (shift treated recency up by a large enough margin that |SMD| >= 0.1), `"history"`, and `"zip_code"` (over-represent one level in the treated arm). An unrecognized value must `raise ValueError(f"Unknown imbalance kind: {imbalance!r}")` — copy the terminator style from the existing `corrupt` fixture.

    The `zip_code` level names must use the source data's real misspelling `"Surburban"` — RESEARCH confirms the misspelling is in the source data and the Pandera schema asserts the literal spelling. Never "fix" it.

    The factory returns a fresh frame on every call and mutates nothing. Column dtypes must be primitive (int64 / float64 / str) so a synthetic frame can be passed to the same estimators as a real one.

    Update the module docstring's scope line — it currently reads "for the ingestion and schema-validation suite" — to also name the Phase 2 estimation fixtures. Do not add any `sys.path` manipulation; `pythonpath = ["."]` in `pyproject.toml` already handles imports.
  </action>
  <acceptance_criteria>
    - `.venv/Scripts/python.exe -m pytest tests/ -q -m "not slow"` passes with no reduction in the existing 44-test count
    - A throwaway test invoking `synthetic_frame(n=4000, effect=1.25)` yields a frame where `df.loc[df.treatment==1,"spend"].mean() - df.loc[df.treatment==0,"spend"].mean()` is within 0.35 of 1.25, and `df.shape[0] == 4000`
    - `synthetic_frame(imbalance="recency")` produces a frame where treated and control `recency` means differ by at least 0.1 standardized-mean-difference units
    - `synthetic_frame(imbalance="nonsense")` raises `ValueError` whose message contains `Unknown imbalance kind`
    - Two calls with the same `seed` produce frames that compare equal via `pandas.testing.assert_frame_equal`
    - `tests/conftest.py` contains the literal `"Surburban"` and does not contain `"Suburban"`
    - `grep -v '^#' tests/conftest.py | grep -c "sys.path"` returns 0
  </acceptance_criteria>
  <verify>
    <automated>.venv/Scripts/python.exe -m pytest tests/ -q -m "not slow"</automated>
  </verify>
  <done>`analysis_df`, `mens_frame`, `womens_frame`, and the `synthetic_frame` factory are available to every Phase 2 test module, seeded and reproducible, with an injected-imbalance mode that raises on an unknown kind.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root -> filesystem | Any path constant added here determines where later plans write |
| committed Parquet -> test fixtures | Fixtures read on-disk artifacts; a tampered artifact enters the test suite here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | `config.REPORTS` / `config.FIGURES` | mitigate | Both constants are built with pathlib `/` on `ROOT`; no CWD-relative string, no concatenation, no value derived from input. Enforced by `tests/test_config.py::test_paths_are_cwd_independent` |
| T-02-02 | Tampering / Repudiation | `conftest.py` real-data fixtures | mitigate | Fixtures read committed Parquet only and never call `load_raw()`, so no code path in Phase 2 tests bypasses the Phase 1 SHA-256 checksum gate |
| T-02-03 | Information Disclosure | new package code | mitigate | `tests/test_no_network.py` token grep stays unmodified and unexcluded; no forbidden token may appear in `frames.py` or `config.py`, comments included |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | RESEARCH.md Package Legitimacy Audit records zero package installs in this phase; every library is already pinned in `requirements.txt` and was audited in Phase 1. Any task proposing a new dependency is a CLAUDE.md scope violation and must be rejected, not gated |
</threat_model>

<verification>
- `.venv/Scripts/python.exe -m pytest -q` — full suite green, test count increased by the new frames/config tests
- `.venv/Scripts/python.exe -c "from dont_email_everyone import config; assert config.FIGURES.parent == config.REPORTS"` exits 0
- `.venv/Scripts/python.exe -m pytest tests/test_no_network.py -q` passes
</verification>

<success_criteria>
- `config.REPORTS` and `config.FIGURES` exist, are absolute, ROOT-anchored, and cwd-independent by test
- `frames.build_arm_vs_arm_frame` returns (42694, 12) built by positive membership with no `treatment` column
- `conftest.py` exposes `analysis_df`, `mens_frame`, `womens_frame`, and a seeded `synthetic_frame(n, effect, imbalance, seed)` factory
- Zero new dependencies added; `requirements.txt` unchanged
- Full test suite green under `.venv/Scripts/python.exe -m pytest -q`
</success_criteria>

<output>
Create `.planning/phases/02-experiment-validity/02-01-SUMMARY.md` when done
</output>

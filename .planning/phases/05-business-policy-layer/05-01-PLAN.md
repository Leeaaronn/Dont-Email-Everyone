---
phase: 05-business-policy-layer
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - reports/policy.md
  - dont_email_everyone/economics.py
  - tests/test_economics.py
  - tests/test_reports.py
autonomous: true
requirements: [C-3, C-5, D-13, D-06, D-07, D-10]

must_haves:
  truths:
    - "The k = 0.20 capacity anchor is committed to git BEFORE any commit that computes a policy value, and its justification is provenance (a Phase 3 default from 9581e84) rather than the shape of the curve."
    - "A capacity function exists that converts a percentage into an absolute email count using truncation, and it takes no cost or margin argument at all."
    - "economics.py performs no file I/O, imports no Streamlit, and contains no accuracy-family token — enforced by a test, not by prose."
    - "The anchor stated in reports/policy.md and the anchor pinned as a module constant cannot drift apart."
  artifacts:
    - path: "reports/policy.md"
      provides: "the pre-registered capacity-anchor section, committed on its own before any policy number exists"
      contains: "Capacity anchor, stated before the policy value was computed"
    - path: "dont_email_everyone/economics.py"
      provides: "HEADLINE_CAPACITY, emails_at_capacity, and the module docstring's decisions block"
      contains: "HEADLINE_CAPACITY"
    - path: "tests/test_economics.py"
      provides: "criterion 5's two purity tests, the truncation pin, and D-13's provenance pin"
      contains: "test_economics_module_is_pure"
  key_links:
    - from: "tests/test_reports.py"
      to: "dont_email_everyone/economics.py"
      via: "reads HEADLINE_CAPACITY and asserts reports/policy.md states the same value"
      pattern: "economics\\.HEADLINE_CAPACITY"
    - from: "tests/test_economics.py"
      to: "git log -S\"k: float = 0.2\""
      via: "subprocess provenance check against dont_email_everyone/evaluation.py"
      pattern: "9581e84"
---

<objective>
Pre-register the headline capacity anchor and create the `economics.py` module boundary
that criterion 5 governs.

Purpose: D-13 chose k = 0.20 on **provenance**, not on the curve — it is
`evaluation.uplift_at_k`'s default, committed in `9581e84` on 2026-09-05, three commits
into Phase 3 and four days before `models.py` existed (`b3c162f`, 2026-09-09). That
argument is only worth something if the anchor is committed before any Phase 5 number is.
This plan is therefore the phase's first commit, mirroring Phase 4's pre-registration
ordering exactly.

Output: `reports/policy.md` carrying only its pre-registration section; a new
`dont_email_everyone/economics.py` with the anchor constant and the capacity function;
`tests/test_economics.py` with criterion 5's two purity tests; `policy.md` added to
`tests/test_reports.py::REPORT_NAMES`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-business-policy-layer/05-CONTEXT.md
@.planning/phases/05-business-policy-layer/05-RESEARCH.md
@dont_email_everyone/config.py
@tests/test_reports.py

<interfaces>
<!-- Extracted from the codebase. Use these directly; no exploration needed. -->

From `dont_email_everyone/evaluation.py` (the anchor's provenance — DO NOT MODIFY in this plan):

    def uplift_at_k(
        score, treatment, outcome, k: float = 0.2, *, seed: int = 20260902
    ) -> float:

    BAND_GRID_POINTS = 101
    BOOTSTRAP_BAND_RESAMPLES = 500
    BOOTSTRAP_BAND_LEVEL = 0.95

From `dont_email_everyone/config.py`:

    ROOT = pathlib.Path(__file__).resolve().parents[1]
    PROCESSED = ROOT / "data" / "processed"
    REPORTS = ROOT / "reports"
    FIGURES = REPORTS / "figures"

From `tests/test_reports.py` (line ~114):

    REPORT_NAMES = ("validity.md", "metric.md", "model.md")
    MIN_REPORT_BYTES = 2_000

From `tests/test_evaluation.py:118` — the purity pattern to COPY, not invent:

    def _evaluation_body():
        return "\n".join(
            line for line in _evaluation_source().splitlines()
            if not line.lstrip().startswith("#")
        )
    # forbidden tokens assembled by string concatenation so the test file
    # does not trip its own check: "to_par" + "quet", "s" + "t.", etc.
    # plus test_evaluation_module_writes_nothing: monkeypatch.chdir(tmp_path),
    # call EVERY public function, assert list(tmp_path.iterdir()) == []
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-register the capacity anchor in reports/policy.md, on its own commit</name>
  <files>reports/policy.md</files>
  <action>
Create `reports/policy.md` containing a title, a one-paragraph statement of what the
document will carry, and then ONE substantive section:
`## Capacity anchor, stated before the policy value was computed`.

Model its shape on `reports/model.md` §"Acceptance criteria, stated before the analysis"
— read that section first and mirror its register, its italic `*Source:*` convention, and
its habit of stating the rule and its timing rather than asserting good faith.

The section MUST state all six of the following, in prose:

1. The anchor is **k = 0.20** of the evaluation frame, reported as a percentage with the
   absolute email count shown alongside (D-07).
2. Its justification is **provenance**: k = 0.20 is the default in
   `evaluation.uplift_at_k`'s signature, introduced in commit `9581e84`
   ("feat(03-02): add uplift_at_k and tie_diagnostics to evaluation.py") on **2026-09-05**,
   before `dont_email_everyone/models.py` existed (added in `b3c162f`, 2026-09-09). It
   therefore could not have been selected to flatter a result that did not yet exist.
   Verify both commit hashes and both dates yourself with
   `git log -S"k: float = 0.2" -- dont_email_everyone/evaluation.py` and
   `git log --diff-filter=A -- dont_email_everyone/models.py` before writing them down —
   the numbers-discipline rule for this phase is measure, never quote.
3. It is **not** claimed to be the best point on the curve. Say plainly that it is not.
4. The **full 101-point grid is published** alongside, so any capacity can be read off it
   (D-07), and the anchor is a reading convention rather than a load-bearing choice.
5. The **disclosure**, stated rather than implied: this phase's research pass measured the
   whole curve before the anchor was fixed, and that curve is published in full. The
   honest claim is "the anchor's justification is prior", NOT "nobody looked". Anything
   else would be false, since `05-RESEARCH.md` contains the grid.
6. A forward pointer that the cost-optimal k\* reported later in this document is selected
   on the evaluation rows and therefore carries exactly the optimism this exogenous anchor
   avoids — which is why k\* is never the headline (D-09).

Write NO policy value, no dollar figure, no contrast and no CI in this file yet. The rest
of the document is authored in plan 05-09, below this section.

Commit this task ALONE, before any other file in this phase changes. The git timestamp is
the record. Suggested message: `docs(05-01): pre-register the k = 0.20 capacity anchor`.
  </action>
  <verify>
    <automated>./.venv/Scripts/python.exe -c "import pathlib,re; s=pathlib.Path('reports/policy.md').read_text(encoding='utf-8'); assert 'Capacity anchor, stated before the policy value was computed' in s; assert '9581e84' in s and '2026-09-05' in s; assert 'models.py' in s; assert re.search(r'0\.20|20%', s); assert not re.search(r'\$[0-9]', s), 'no dollar figure may appear in the pre-registration commit'; print('anchor section OK', len(s), 'bytes')"</automated>
    <automated>git log -1 --name-only --format="%s" | grep -c "reports/policy.md"</automated>
  </verify>
  <done>`reports/policy.md` exists, is over 2,000 bytes, states all six points, contains no dollar figure, and is committed in a commit that touches no other file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create economics.py with the anchor constant, the capacity function, and criterion 5's purity tests</name>
  <files>dont_email_everyone/economics.py, tests/test_economics.py</files>
  <behavior>
    - `economics.HEADLINE_CAPACITY == 0.20` and `== evaluation.uplift_at_k`'s signature default (read via `inspect.signature`, not retyped).
    - `emails_at_capacity(21347, 0.20) == 4269` — truncation, not rounding.
    - `emails_at_capacity(7, 0.5) == 3`; a rounding implementation returns 4. This is the case that makes truncation observable.
    - `emails_at_capacity(n)` with no k defaults to `HEADLINE_CAPACITY`.
    - `emails_at_capacity` raises `ValueError` (never `assert`) on `k <= 0`, `k > 1`, `k` NaN, and on `n_customers < 1`.
    - `inspect.signature(emails_at_capacity)` has no parameter whose name contains `cost` or `margin` — criterion 3's third clause, made structural.
    - `git log -S"k: float = 0.2" -- dont_email_everyone/evaluation.py` yields `9581e84`, and its commit date is strictly earlier than the date `dont_email_everyone/models.py` was added. The provenance ARGUMENT is pinned, not merely the value.
    - The non-comment body of `economics.py` contains none of the forbidden tokens.
    - Calling every public function of `economics.py` from an empty cwd leaves it empty.
  </behavior>
  <action>
Write `tests/test_economics.py` first, then `dont_email_everyone/economics.py` to satisfy it.

**`dont_email_everyone/economics.py`** — new module. Follow `evaluation.py`'s shape: a long
module docstring with a lettered decisions block, module-level constants with rejected
alternatives named in the comment beside them, and `_guard_*` helpers that raise a named
`ValueError` (never `assert`, which compiles out under `python -O`).

The docstring's decisions block must record, at minimum:
  (a) This module owns anything that touches MONEY. `evaluation.py` owns anything that
      touches the randomization or the ranking. That boundary is why this module contains
      no RNG and its tests are closed-form.
  (b) Criterion 5: no file I/O, no Streamlit import, no rendering, no accuracy-family
      metric. The README's numbers and the app's numbers come from this code or from none.
  (c) `HEADLINE_CAPACITY = 0.20`, with its provenance argument in full (the
      `evaluation.uplift_at_k` default from `9581e84`, 2026-09-05, before `models.py`) and
      the rejected alternatives named so a future reader cannot silently retune it:
      k = 0.10 and below is unusable (the evaluation frame carries only ~170 non-zero
      spend rows and one 1,068-row slice carries 14 of the conversions, so the number
      swings by an order of magnitude across that region); k = 0.225 has a larger point
      estimate and k = 0.15 a tighter interval, and BOTH were rejected precisely because
      choosing on the curve is the selection this anchor exists to avoid.
  (d) Cost per email and gross margin have **no defaults anywhere in this module**
      (D-10). Hillstrom carries no cost data; a default would be a fabricated constant
      wearing the authority of code. They arrive as required keyword arguments in plan
      05-05, and they go to work only in the cost-optimal-k exhibit.

Public surface for this plan — exactly one function:

    def emails_at_capacity(n_customers, k: float = HEADLINE_CAPACITY) -> int:
        """Return int(n_customers * k): how many emails a capacity of k percent buys."""

Its docstring must state that the truncation is `int(n * k)` and NOT `round`/`ceil`,
cross-referencing `evaluation.uplift_at_k`'s module docstring decision (f) — because the
report and the app disagreeing by one row over a rounding rule is a real, cheap, embarrassing
failure. It must also state that this function deliberately takes no cost and no margin
argument: it is criterion 3's "capacity framing that requires no cost assumption at all",
and a signature test enforces that.

**`tests/test_economics.py`** — copy `tests/test_evaluation.py`'s purity pattern verbatim
(the `_economics_source()` / `_economics_body()` pair, the concatenation-assembled token
tuple, and the `monkeypatch.chdir(tmp_path)` call-everything test). Do not invent a new
shape. Add the comment `test_evaluation.py` carries above its call list, warning that a
call list which stops growing turns the guarantee into a guarantee about history — plans
05-05 and later must extend it.

Then add the behaviour tests listed above. The provenance test shells out with
`subprocess.run([...], cwd=config.ROOT, capture_output=True, text=True, check=True)`,
following `tests/test_artifacts.py::test_artifacts_exist`'s git invocation exactly.
  </action>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_economics.py -x -q</automated>
    <automated>./.venv/Scripts/python.exe -c "import inspect; from dont_email_everyone import economics, evaluation; assert economics.HEADLINE_CAPACITY == inspect.signature(evaluation.uplift_at_k).parameters['k'].default; assert economics.emails_at_capacity(21347) == 4269; assert economics.emails_at_capacity(7, 0.5) == 3; print('constants OK')"</automated>
  </verify>
  <done>`tests/test_economics.py` passes; `economics.py` has exactly one public function; both purity tests are non-vacuous (they call the function that exists and grep a body that exists).</done>
</task>

<task type="auto">
  <name>Task 3: Bring policy.md under the report suite and pin the anchor against the constant</name>
  <files>tests/test_reports.py</files>
  <action>
Add `"policy.md"` to `REPORT_NAMES`, keeping it a single tuple literal on one line —
`tests/test_reports.py`'s own assertions read the literal up to its first `)`, and 04-09
recorded the deviation that rebinding on a second line breaks. The diff will necessarily
contain one deleted `REPORT_NAMES = (...)` line; that is expected and is the same
disposition 04-09 recorded.

Add `test_policy_anchor_matches_the_constant`:
  - imports `economics` and reads `economics.HEADLINE_CAPACITY`;
  - asserts `reports/policy.md` states the same anchor, in both the decimal and the
    percentage spelling, DERIVED from the constant (`f"{value:.2f}"` and
    `f"{value * 100:.0f}%"`), never as a retyped literal — so retuning the constant fails
    the test rather than silently disagreeing with the report;
  - asserts the anchor section names the provenance commit `9581e84` and states that the
    full grid is published;
  - asserts the report does not present the anchor as the best point on the curve: the
    phrase asserting it is NOT the best point must be present.

Use the existing `_flat()` helper so a substring assertion survives `**bold**` landing
mid-phrase.

The ordering assertion (anchor section strictly before the first policy result) is
deliberately NOT written here — there are no results in the file yet, so it would be
vacuous. Plan 05-09 adds it and proves it non-vacuous by transposition. Leave a comment
saying so, naming plan 05-09, so the gap reads as scheduled rather than forgotten.
  </action>
  <verify>
    <automated>./.venv/Scripts/python.exe -m pytest tests/test_reports.py -x -q</automated>
    <automated>./.venv/Scripts/python.exe -c "import pathlib; s=pathlib.Path('tests/test_reports.py').read_text(encoding='utf-8'); lit=s.split('REPORT_NAMES')[1].split(')')[0]; assert all(n in lit for n in ('validity.md','metric.md','model.md','policy.md')), lit; print('REPORT_NAMES OK')"</automated>
    <automated>./.venv/Scripts/python.exe -m pytest -q -m "not slow"</automated>
  </verify>
  <done>`REPORT_NAMES` carries all four names in one literal; `policy.md` is under the presence, git-tracking and byte-floor assertions; the anchor-agreement test passes and fails if `HEADLINE_CAPACITY` is retuned without the report changing; the fast suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored CSV -> processed artifacts | Phase 1's SHA-256 checksum gate; unchanged and untouched by this plan |
| repository -> public GitHub | This is a public portfolio repo; every file written here is intended to be read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | `economics.HEADLINE_CAPACITY` retuned after a result is seen | mitigate | The constant is pinned against `reports/policy.md` by `test_policy_anchor_matches_the_constant`, and the report's provenance claim is pinned against `git log` — retuning fails two tests |
| T-05-02 | Information disclosure | `reports/policy.md` prose contents | accept | Public portfolio repo; the write-up is the deliverable and the 2008 Hillstrom data carries no personal information (same disposition as T-04-81) |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this plan or this phase; `05-RESEARCH.md` §"Package Legitimacy Audit" records that the gate is not applicable because nothing is installed |
</threat_model>

<verification>
- `./.venv/Scripts/python.exe -m pytest -q -m "not slow"` exits 0.
- `git log --oneline -3` shows the `reports/policy.md` pre-registration commit strictly
  before the commit that adds `economics.py`.
- `git status --short data/processed reports/figures` is empty — this plan touches no
  artifact and no figure.
</verification>

<success_criteria>
- The anchor is committed before any Phase 5 computation, and the commit ordering is
  visible in `git log`.
- `economics.py` exists, is pure by test, and carries the anchor with its rejected
  alternatives named beside it.
- No cost or margin default exists anywhere in the module (D-10).
- `policy.md` is covered by the report suite's presence, tracking and byte-floor assertions.
</success_criteria>

<output>
Create `.planning/phases/05-business-policy-layer/05-01-SUMMARY.md` when done.
Record the two commit hashes and dates you measured for the provenance argument, and the
exact byte size of `reports/policy.md` at the pre-registration commit.
</output>
